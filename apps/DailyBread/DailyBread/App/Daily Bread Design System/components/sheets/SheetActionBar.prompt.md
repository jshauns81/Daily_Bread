One-line: every designed sheet ends with this pair; never split the actions.

```jsx
<SheetActionBar saveTitle={isEditing ? 'Save' : 'Create'} canSave={canSave}
  saving={saving} onCancel={close} onSave={save} />
```
