One-line: the sheet's own title row — sheets don't use a navigation bar title.

```jsx
<SheetHeader title="New Task" />
```

The title names the *kind*: "New Task" / "Edit Task" / "New Routine" / "Edit Routine".
