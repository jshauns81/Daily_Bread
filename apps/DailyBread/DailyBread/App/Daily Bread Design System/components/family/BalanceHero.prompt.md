One-line: the kid's money, big and rounded and gold.

```jsx
<BalanceHero balance="$48.75" />
```

Don't reuse the rounded face anywhere else. Don't add a cash-out button here — cashing
out lives in the ledger flow.
