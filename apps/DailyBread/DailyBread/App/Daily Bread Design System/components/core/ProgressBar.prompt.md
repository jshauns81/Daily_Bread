One-line: a linear progress track; `tone="progress"` is the warm glow gradient.

```jsx
<ProgressBar value={goal.progressPercent} total={100} />
<ProgressBar value={done} total={total} height={10} tone="progress" />
```
