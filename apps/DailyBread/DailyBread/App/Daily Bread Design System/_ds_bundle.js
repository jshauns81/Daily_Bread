/* @ds-bundle: {"format":4,"namespace":"DailyBreadDesignSystem_2a4964","components":[{"name":"Button","sourcePath":"components/core/Button.jsx"},{"name":"Chip","sourcePath":"components/core/Chip.jsx"},{"name":"Tag","sourcePath":"components/core/Chip.jsx"},{"name":"EmptyState","sourcePath":"components/core/EmptyState.jsx"},{"name":"GlassCard","sourcePath":"components/core/GlassCard.jsx"},{"name":"SF_TO_LUCIDE","sourcePath":"components/core/Icon.jsx"},{"name":"Icon","sourcePath":"components/core/Icon.jsx"},{"name":"InlineError","sourcePath":"components/core/InlineError.jsx"},{"name":"ProgressBar","sourcePath":"components/core/ProgressBar.jsx"},{"name":"ProgressRing","sourcePath":"components/core/ProgressRing.jsx"},{"name":"SectionHeader","sourcePath":"components/core/SectionHeader.jsx"},{"name":"StatTile","sourcePath":"components/core/StatTile.jsx"},{"name":"KID_TABS","sourcePath":"components/core/TabBar.jsx"},{"name":"PARENT_TABS","sourcePath":"components/core/TabBar.jsx"},{"name":"TabBar","sourcePath":"components/core/TabBar.jsx"},{"name":"RARITY","sourcePath":"components/family/AchievementCard.jsx"},{"name":"AchievementCard","sourcePath":"components/family/AchievementCard.jsx"},{"name":"AchievementDefRow","sourcePath":"components/family/AchievementDefRow.jsx"},{"name":"ApprovalRow","sourcePath":"components/family/ApprovalRow.jsx"},{"name":"AtRiskCard","sourcePath":"components/family/AtRiskCard.jsx"},{"name":"BalanceHero","sourcePath":"components/family/BalanceHero.jsx"},{"name":"CalendarMonth","sourcePath":"components/family/CalendarMonth.jsx"},{"name":"ChildPicker","sourcePath":"components/family/ChildPicker.jsx"},{"name":"ChildRow","sourcePath":"components/family/ChildRow.jsx"},{"name":"ChildTile","sourcePath":"components/family/ChildRow.jsx"},{"name":"ChoreRow","sourcePath":"components/family/ChoreRow.jsx"},{"name":"DrivingTotals","sourcePath":"components/family/DrivingLogRow.jsx"},{"name":"DrivingLogRow","sourcePath":"components/family/DrivingLogRow.jsx"},{"name":"ExpandRow","sourcePath":"components/family/ExpandRow.jsx"},{"name":"ConfirmRow","sourcePath":"components/family/ExpandRow.jsx"},{"name":"FamilyMemberRow","sourcePath":"components/family/FamilyMemberRow.jsx"},{"name":"GoalCard","sourcePath":"components/family/GoalCard.jsx"},{"name":"HelpRow","sourcePath":"components/family/HelpRow.jsx"},{"name":"LEVEL_TIERS","sourcePath":"components/family/LevelBadge.jsx"},{"name":"TierFor","sourcePath":"components/family/LevelBadge.jsx"},{"name":"LevelBadge","sourcePath":"components/family/LevelBadge.jsx"},{"name":"XPBar","sourcePath":"components/family/LevelBadge.jsx"},{"name":"PlannerChoreRow","sourcePath":"components/family/PlannerChoreRow.jsx"},{"name":"PlannerGrid","sourcePath":"components/family/PlannerGrid.jsx"},{"name":"ScreenTimeCard","sourcePath":"components/family/ScreenTimeCard.jsx"},{"name":"DB_THEMES","sourcePath":"components/family/ThemeSwatch.jsx"},{"name":"ThemeSwatch","sourcePath":"components/family/ThemeSwatch.jsx"},{"name":"ThemeRow","sourcePath":"components/family/ThemeSwatch.jsx"},{"name":"TransactionRow","sourcePath":"components/family/TransactionRow.jsx"},{"name":"WeekStrip","sourcePath":"components/family/WeekStrip.jsx"},{"name":"YearHeatmapCard","sourcePath":"components/family/YearHeatmapCard.jsx"},{"name":"ALL_DAYS","sourcePath":"components/sheets/DayPicker.jsx"},{"name":"DayPicker","sourcePath":"components/sheets/DayPicker.jsx"},{"name":"SegmentedPicker","sourcePath":"components/sheets/SegmentedPicker.jsx"},{"name":"SheetActionBar","sourcePath":"components/sheets/SheetActionBar.jsx"},{"name":"SheetCard","sourcePath":"components/sheets/SheetCard.jsx"},{"name":"SheetField","sourcePath":"components/sheets/SheetField.jsx"},{"name":"SheetHeader","sourcePath":"components/sheets/SheetHeader.jsx"},{"name":"Slider","sourcePath":"components/sheets/Slider.jsx"},{"name":"Stepper","sourcePath":"components/sheets/Stepper.jsx"},{"name":"TextField","sourcePath":"components/sheets/TextField.jsx"},{"name":"ToggleRow","sourcePath":"components/sheets/ToggleRow.jsx"}],"sourceHashes":{"components/core/Button.jsx":"385a13cbf479","components/core/Chip.jsx":"39ebbf704d43","components/core/EmptyState.jsx":"4e90b426d693","components/core/GlassCard.jsx":"923e95c61135","components/core/Icon.jsx":"b5d9346ea42d","components/core/InlineError.jsx":"cde976967a07","components/core/ProgressBar.jsx":"bb4af61e456b","components/core/ProgressRing.jsx":"785afcfb5732","components/core/SectionHeader.jsx":"1fe08c5ffa92","components/core/StatTile.jsx":"8e038e8c425a","components/core/TabBar.jsx":"fbf7663baec3","components/family/AchievementCard.jsx":"d08cee89702c","components/family/AchievementDefRow.jsx":"cf2c3c252359","components/family/ApprovalRow.jsx":"eae26ed26555","components/family/AtRiskCard.jsx":"328fd7f2a380","components/family/BalanceHero.jsx":"cc950493c4c5","components/family/CalendarMonth.jsx":"b34dd38f59a2","components/family/ChildPicker.jsx":"6881131f66f6","components/family/ChildRow.jsx":"856dfdf07506","components/family/ChoreRow.jsx":"b725d810ce5c","components/family/DrivingLogRow.jsx":"03522bba604d","components/family/ExpandRow.jsx":"353db0c9a547","components/family/FamilyMemberRow.jsx":"dbb5bb8b65ea","components/family/GoalCard.jsx":"142f85ed63a7","components/family/HelpRow.jsx":"192da3898017","components/family/LevelBadge.jsx":"c934dd1eb472","components/family/PlannerChoreRow.jsx":"62c750116018","components/family/PlannerGrid.jsx":"399f0b0df53c","components/family/ScreenTimeCard.jsx":"bad99b16f969","components/family/ThemeSwatch.jsx":"a1b97b3a8d4b","components/family/TransactionRow.jsx":"b98db73d7a66","components/family/WeekStrip.jsx":"b97f4415b126","components/family/YearHeatmapCard.jsx":"79cbffb3e24d","components/sheets/DayPicker.jsx":"49b55b80f053","components/sheets/SegmentedPicker.jsx":"b5c8e74c8b01","components/sheets/SheetActionBar.jsx":"3f8342240456","components/sheets/SheetCard.jsx":"7c89a6df4907","components/sheets/SheetField.jsx":"fbb6069ea390","components/sheets/SheetHeader.jsx":"b248a3d8b1f3","components/sheets/Slider.jsx":"8b652bbb5b67","components/sheets/Stepper.jsx":"f03c6e245713","components/sheets/TextField.jsx":"f396be8d9997","components/sheets/ToggleRow.jsx":"05944c23fa7b","ui_kits/kid_app/Celebration.jsx":"7a48dce382da","ui_kits/kid_app/KidScreens.jsx":"00cb7da7aba1","ui_kits/kid_app/tweaks-panel.jsx":"d259e3a86f73","ui_kits/mac_app/MacShell.jsx":"8903a9d6a8c0","ui_kits/parent_app/ParentScreens.jsx":"46ab41f32e3e","ui_kits/widgets/Widgets.jsx":"5a584c5e6987","ui_kits/widgets/tweaks-panel.jsx":"d259e3a86f73"},"inlinedExternals":[],"unexposedExports":[]} */

(() => {

const __ds_ns = (window.DailyBreadDesignSystem_2a4964 = window.DailyBreadDesignSystem_2a4964 || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// components/core/Chip.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Chip({
  tone,
  emphasized = true,
  children,
  style,
  ...rest
}) {
  const c = emphasized && tone ? tone : 'var(--ds-label-2)';
  return /*#__PURE__*/React.createElement("span", _extends({}, rest, {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 4,
      fontSize: 'var(--ds-text-caption)',
      fontWeight: 'var(--ds-weight-semibold)',
      padding: '5px 9px',
      borderRadius: 'var(--ds-radius-capsule)',
      background: 'color-mix(in srgb, ' + c + ' 13%, transparent)',
      color: c,
      whiteSpace: 'nowrap',
      ...style
    }
  }), children);
}

/** The small solid capsule used for HELP, Off, Routine, Cash out ready. */
function Tag({
  tone = 'var(--ds-label-2)',
  solid,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", _extends({}, rest, {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      fontSize: 'var(--ds-text-caption2)',
      fontWeight: 'var(--ds-weight-heavy)',
      padding: '3px 7px',
      borderRadius: 'var(--ds-radius-capsule)',
      background: solid ? tone : 'color-mix(in srgb, ' + tone + ' 15%, transparent)',
      color: solid ? '#fff' : tone,
      whiteSpace: 'nowrap',
      ...style
    }
  }), children);
}
Object.assign(__ds_scope, { Chip, Tag });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Chip.jsx", error: String((e && e.message) || e) }); }

// components/core/GlassCard.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * The `.glassCard()` modifier. Opaque theme card colour, 20px continuous
 * radius, 0.5px stroke, one close soft shadow. There is no blur — the "glass"
 * is the stroke plus the shadow.
 */
function GlassCard({
  padding = 'var(--ds-card-padding)',
  tone,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      background: 'var(--ds-card)',
      borderRadius: 'var(--ds-radius-card)',
      boxShadow: 'var(--ds-card-shadow), 0 0 0 .5px var(--ds-card-stroke)' + (tone ? ', inset 0 0 0 1px color-mix(in srgb, ' + tone + ' 35%, transparent)' : ''),
      padding,
      boxSizing: 'border-box',
      position: 'relative',
      ...style
    }
  }), children);
}
Object.assign(__ds_scope, { GlassCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/GlassCard.jsx", error: String((e && e.message) || e) }); }

// components/core/Icon.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * SF Symbols stand-in. The real app uses SF Symbols, which cannot ship to the
 * web — this renders the closest Lucide glyph instead (a flagged substitution;
 * see readme ICONOGRAPHY). Load Lucide from CDN before the bundle:
 *   <script src="https://unpkg.com/lucide@0.454.0/dist/umd/lucide.min.js"></script>
 */
const SF_TO_LUCIDE = {
  house: 'house',
  'sun.max': 'sun',
  'dollarsign.circle': 'circle-dollar-sign',
  trophy: 'trophy',
  checklist: 'list-checks',
  'checkmark.circle': 'circle-check',
  gearshape: 'settings',
  'checkmark.circle.fill': 'circle-check-big',
  circle: 'circle',
  checkmark: 'check',
  'checkmark.seal': 'badge-check',
  'checkmark.seal.fill': 'badge-check',
  'hand.thumbsup': 'thumbs-up',
  'questionmark.circle': 'circle-help',
  'exclamationmark.circle': 'circle-alert',
  'exclamationmark.circle.fill': 'circle-alert',
  'flame.fill': 'flame',
  'clock.badge.exclamationmark': 'clock-alert',
  'lock.fill': 'lock',
  'minus.circle': 'circle-minus',
  'arrow.uturn.up': 'redo-2',
  'clock.arrow.circlepath': 'history',
  'chevron.right': 'chevron-right',
  'chevron.up': 'chevron-up',
  'chevron.down': 'chevron-down',
  'chevron.up.chevron.down': 'chevrons-up-down',
  plus: 'plus',
  'chevron.left': 'chevron-left',
  'moon.stars': 'moon-star',
  key: 'key',
  'ellipsis.circle': 'ellipsis',
  'list.bullet': 'list',
  'square.grid.2x2': 'grid-2x2',
  'slider.horizontal.3': 'sliders-horizontal',
  'party.popper': 'party-popper',
  calendar: 'calendar',
  gift: 'gift',
  target: 'target',
  car: 'car',
  'person.2': 'users',
  'wifi.exclamationmark': 'wifi-off',
  'exclamationmark.triangle': 'triangle-alert'
};

/**
 * Paint one glyph into `host` using Lucide's documented createIcons() scan.
 * The host's children are managed imperatively and never by React, so letting
 * Lucide replace the placeholder <i> is safe.
 *
 * Returns 'ok' | 'unknown' (glyph absent from this build — stop trying) |
 * 'waiting' (Lucide not loaded yet — worth a retry).
 */
function paint(host, glyph, size, strokeWidth) {
  const L = window.lucide;
  if (!L || typeof L.createIcons !== 'function') return 'waiting';
  try {
    host.innerHTML = '<i data-lucide="' + glyph + '"></i>';
    L.createIcons();
    const svg = host.querySelector('svg');
    if (!svg) {
      host.innerHTML = '';
      return 'unknown';
    }
    svg.setAttribute('width', size);
    svg.setAttribute('height', size);
    svg.setAttribute('stroke-width', strokeWidth);
    svg.style.display = 'block';
    return 'ok';
  } catch (e) {
    host.innerHTML = '';
    return 'unknown';
  }
}
function Icon({
  name,
  size = 17,
  color,
  strokeWidth = 2,
  style,
  ...rest
}) {
  const ref = React.useRef(null);
  const glyph = SF_TO_LUCIDE[name] || name;
  React.useEffect(() => {
    const host = ref.current;
    if (!host) return undefined;
    // 'unknown' stops here too: retrying a glyph this build doesn't have would
    // just spam the console 40 times per icon.
    if (paint(host, glyph, size, strokeWidth) !== 'waiting') return undefined;
    let tries = 0;
    const id = setInterval(() => {
      if (paint(host, glyph, size, strokeWidth) !== 'waiting' || ++tries > 40) clearInterval(id);
    }, 50);
    return () => clearInterval(id);
  }, [glyph, size, strokeWidth]);
  return /*#__PURE__*/React.createElement("span", _extends({}, rest, {
    ref: ref,
    "aria-hidden": "true",
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: size,
      height: size,
      flex: '0 0 auto',
      color: color || 'currentColor',
      ...style
    }
  }));
}
Object.assign(__ds_scope, { SF_TO_LUCIDE, Icon });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Icon.jsx", error: String((e && e.message) || e) }); }

// components/core/Button.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * The four button treatments the app uses:
 *   prominent  → .borderedProminent tinted accent
 *   gold       → .borderedProminent tinted DB.gold (Approve — the Blessing)
 *   bordered   → .bordered, secondary tint
 *   capsule    → the Help/Done pair on the kid's Next Up card
 */
const shapes = {
  prominent: {
    background: 'var(--ds-accent)',
    color: 'var(--ds-on-accent)',
    borderRadius: 'var(--ds-radius-swatch)',
    fontWeight: 'var(--ds-weight-semibold)'
  },
  gold: {
    background: 'var(--ds-gold)',
    color: 'rgb(0 0 0 / .8)',
    borderRadius: 'var(--ds-radius-swatch)',
    fontWeight: 'var(--ds-weight-bold)'
  },
  bordered: {
    background: 'var(--ds-fill-strong)',
    color: 'var(--ds-label)',
    borderRadius: 'var(--ds-radius-swatch)',
    fontWeight: 'var(--ds-weight-medium)'
  },
  destructive: {
    background: 'var(--ds-help)',
    color: '#fff',
    borderRadius: 'var(--ds-radius-swatch)',
    fontWeight: 'var(--ds-weight-semibold)'
  },
  capsuleAccent: {
    background: 'var(--ds-accent)',
    color: '#fff',
    borderRadius: 'var(--ds-radius-capsule)',
    fontWeight: 'var(--ds-weight-semibold)'
  },
  capsuleHelp: {
    background: 'color-mix(in srgb, var(--ds-help) 16%, transparent)',
    color: 'var(--ds-help)',
    borderRadius: 'var(--ds-radius-capsule)',
    fontWeight: 'var(--ds-weight-semibold)'
  },
  plain: {
    background: 'transparent',
    color: 'var(--ds-accent)',
    borderRadius: 'var(--ds-radius-capsule)',
    fontWeight: 'var(--ds-weight-semibold)'
  }
};
function Button({
  variant = 'prominent',
  icon,
  block,
  disabled,
  busy,
  size = 'md',
  children,
  onClick,
  style,
  ...rest
}) {
  const pad = size === 'sm' ? '6px 12px' : size === 'lg' ? '13px 20px' : '11px 16px';
  const font = size === 'sm' ? 'var(--ds-text-caption)' : 'var(--ds-text-subheadline)';
  return /*#__PURE__*/React.createElement("button", _extends({}, rest, {
    type: "button",
    disabled: disabled || busy,
    onClick: onClick,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      gap: 6,
      border: 'none',
      cursor: disabled || busy ? 'default' : 'pointer',
      fontFamily: 'inherit',
      fontSize: font,
      padding: pad,
      minHeight: size === 'sm' ? 32 : 'var(--ds-touch-target)',
      width: block ? '100%' : undefined,
      whiteSpace: 'nowrap',
      transition: 'background var(--ds-ease-in-out), opacity var(--ds-ease-in-out)',
      ...shapes[variant],
      ...(disabled ? {
        background: 'color-mix(in srgb, var(--ds-label-2) 30%, transparent)',
        color: 'var(--ds-label-2)'
      } : null),
      ...style
    }
  }), busy ? '…' : icon ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon,
    size: size === 'sm' ? 14 : 16
  }) : null, busy ? null : children);
}
Object.assign(__ds_scope, { Button });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Button.jsx", error: String((e && e.message) || e) }); }

// components/core/EmptyState.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function EmptyState({
  icon = 'house',
  title,
  description,
  action,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      textAlign: 'center',
      gap: 8,
      padding: '40px 24px',
      ...style
    }
  }), /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon,
    size: 44,
    color: "var(--ds-label-3)",
    strokeWidth: 1.5
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-headline)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-label)',
      marginTop: 4
    }
  }, title), description ? /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      color: 'var(--ds-label-2)',
      maxWidth: 340,
      lineHeight: 1.45,
      textWrap: 'pretty'
    }
  }, description) : null, action ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 10
    }
  }, action) : null);
}
Object.assign(__ds_scope, { EmptyState });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/EmptyState.jsx", error: String((e && e.message) || e) }); }

// components/core/InlineError.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function InlineError({
  icon = 'wifi.exclamationmark',
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      fontSize: 'var(--ds-text-footnote)',
      color: 'var(--ds-label-2)',
      ...style
    }
  }), /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon,
    size: 13
  }), /*#__PURE__*/React.createElement("span", null, children));
}
Object.assign(__ds_scope, { InlineError });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/InlineError.jsx", error: String((e && e.message) || e) }); }

// components/core/ProgressBar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function ProgressBar({
  value = 0,
  total = 100,
  height = 6,
  tone = 'accent',
  style,
  ...rest
}) {
  const pct = total > 0 ? Math.max(0, Math.min(100, value / total * 100)) : 0;
  const fill = tone === 'progress' ? 'var(--ds-progress)' : tone === 'gold' ? 'var(--ds-gold)' : tone === 'success' ? 'var(--ds-success)' : 'var(--ds-accent)';
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      height,
      borderRadius: 'var(--ds-radius-capsule)',
      background: 'var(--ds-fill)',
      overflow: 'hidden',
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      width: pct + '%',
      height: '100%',
      background: fill,
      borderRadius: 'var(--ds-radius-capsule)',
      transition: 'width var(--ds-spring-slow)'
    }
  }));
}
Object.assign(__ds_scope, { ProgressBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/ProgressBar.jsx", error: String((e && e.message) || e) }); }

// components/core/ProgressRing.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function ProgressRing({
  progress = 0,
  label,
  size = 64,
  stroke = 7,
  color,
  style,
  ...rest
}) {
  const p = Math.max(0, Math.min(1, progress));
  const r = (size - stroke) / 2;
  const circ = 2 * Math.PI * r;
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      position: 'relative',
      width: size,
      height: size,
      flex: '0 0 auto',
      ...style
    }
  }), /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    style: {
      transform: 'rotate(-90deg)',
      display: 'block'
    }
  }, /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--ds-fill)",
    strokeWidth: stroke
  }), /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: color || 'var(--ds-accent)',
    strokeWidth: stroke,
    strokeLinecap: "round",
    strokeDasharray: circ,
    strokeDashoffset: circ - circ * p,
    style: {
      transition: 'stroke-dashoffset var(--ds-snappy)'
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      display: 'grid',
      placeItems: 'center',
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-bold)',
      color: 'var(--ds-label)'
    }
  }, label));
}
Object.assign(__ds_scope, { ProgressRing });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/ProgressRing.jsx", error: String((e && e.message) || e) }); }

// components/core/SectionHeader.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function SectionHeader({
  kerning = 'section',
  color,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      fontSize: 'var(--ds-text-caption)',
      fontWeight: 'var(--ds-weight-bold)',
      textTransform: 'uppercase',
      letterSpacing: kerning === 'eyebrow' ? 'var(--ds-kerning-eyebrow)' : 'var(--ds-kerning-section)',
      color: color || 'var(--ds-label-2)',
      ...style
    }
  }), children);
}
Object.assign(__ds_scope, { SectionHeader });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/SectionHeader.jsx", error: String((e && e.message) || e) }); }

// components/core/StatTile.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function StatTile({
  value,
  label,
  color = 'var(--ds-label)',
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.GlassCard, _extends({}, rest, {
    padding: "var(--ds-card-padding-tight)",
    style: {
      flex: 1,
      minWidth: 0,
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: 3
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-title3)',
      fontWeight: 'var(--ds-weight-bold)',
      color,
      lineHeight: 1.15,
      whiteSpace: 'nowrap',
      maxWidth: '100%',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, value), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption2)',
      color: 'var(--ds-label-2)',
      textAlign: 'center',
      lineHeight: 1.25
    }
  }, label)));
}
Object.assign(__ds_scope, { StatTile });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/StatTile.jsx", error: String((e && e.message) || e) }); }

// components/core/TabBar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const KID_TABS = [{
  id: 'kidHome',
  label: 'Home',
  icon: 'house'
}, {
  id: 'today',
  label: 'Today',
  icon: 'sun.max'
}, {
  id: 'earnings',
  label: 'Earnings',
  icon: 'dollarsign.circle'
}, {
  id: 'awards',
  label: 'Awards',
  icon: 'trophy'
}, {
  id: 'settings',
  label: 'Settings',
  icon: 'gearshape'
}];
const PARENT_TABS = [{
  id: 'home',
  label: 'Home',
  icon: 'house'
}, {
  id: 'planner',
  label: 'Planner',
  icon: 'checklist'
}, {
  id: 'approvals',
  label: 'Approvals',
  icon: 'checkmark.circle'
}, {
  id: 'settings',
  label: 'Settings',
  icon: 'gearshape'
}];
function TabBar({
  tabs = PARENT_TABS,
  active,
  badges = {},
  onSelect,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("nav", _extends({}, rest, {
    style: {
      display: 'flex',
      alignItems: 'stretch',
      minHeight: 'var(--ds-tabbar-height)',
      borderTop: '.5px solid var(--ds-hairline)',
      background: 'color-mix(in srgb, var(--db-bg-bottom) 88%, transparent)',
      backdropFilter: 'saturate(180%) blur(20px)',
      WebkitBackdropFilter: 'saturate(180%) blur(20px)',
      ...style
    }
  }), tabs.map(t => {
    const on = t.id === active;
    const badge = badges[t.id];
    return /*#__PURE__*/React.createElement("button", {
      key: t.id,
      type: "button",
      onClick: () => onSelect && onSelect(t.id),
      style: {
        flex: 1,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        gap: 2,
        padding: '5px 2px 4px',
        background: 'transparent',
        border: 'none',
        cursor: 'pointer',
        fontFamily: 'inherit',
        position: 'relative',
        color: on ? 'var(--ds-accent)' : 'var(--ds-label-2)'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'relative'
      }
    }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: t.icon,
      size: 22,
      strokeWidth: on ? 2.4 : 2
    }), badge ? /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'absolute',
        top: -5,
        left: 12,
        minWidth: 16,
        height: 16,
        padding: '0 4px',
        borderRadius: 999,
        background: 'var(--ds-help)',
        color: '#fff',
        fontSize: 10,
        fontWeight: 700,
        display: 'grid',
        placeItems: 'center',
        boxSizing: 'border-box'
      }
    }, badge) : null), /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 10,
        fontWeight: 500
      }
    }, t.label));
  }));
}
Object.assign(__ds_scope, { KID_TABS, PARENT_TABS, TabBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/TabBar.jsx", error: String((e && e.message) || e) }); }

// components/family/AchievementCard.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Rarity → hue. Exported so AchievementDefRow reads a badge identically. */
const RARITY = {
  legendary: 'var(--ds-gold)',
  epic: 'var(--db-rarity-epic)',
  rare: 'var(--ds-accent)',
  uncommon: 'var(--ds-success)',
  common: 'var(--ds-label-2)'
};
function AchievementCard({
  icon = '🏆',
  name,
  description,
  points,
  rarity = 'common',
  earned,
  progress,
  target,
  style,
  ...rest
}) {
  const c = RARITY[String(rarity).toLowerCase()] || RARITY.common;
  return /*#__PURE__*/React.createElement(__ds_scope.GlassCard, _extends({}, rest, {
    padding: "var(--ds-card-padding-tight)",
    tone: earned ? 'var(--ds-gold)' : undefined,
    style: {
      minHeight: 128,
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: 6,
      textAlign: 'center'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 34,
      lineHeight: 1,
      fontFamily: 'var(--ds-font-emoji)',
      filter: earned ? 'none' : 'grayscale(1)',
      opacity: earned ? 1 : 0.55
    }
  }, icon), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-label)',
      maxWidth: '100%',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, name), description ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption2)',
      color: 'var(--ds-label-2)',
      lineHeight: 1.35,
      display: '-webkit-box',
      WebkitLineClamp: 2,
      WebkitBoxOrient: 'vertical',
      overflow: 'hidden'
    }
  }, description) : null, earned ? /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 4,
      fontSize: 'var(--ds-text-caption2)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontWeight: 'var(--ds-weight-bold)',
      color: c,
      textTransform: 'capitalize'
    }
  }, rarity), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ds-gold)'
    }
  }, "\xB7 ", points, " pts")) : progress != null && target > 0 ? /*#__PURE__*/React.createElement("span", {
    style: {
      width: '100%',
      display: 'flex',
      flexDirection: 'column',
      gap: 3
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.ProgressBar, {
    value: progress,
    total: target,
    height: 4
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption2)',
      color: 'var(--ds-label-3)'
    }
  }, progress, "/", target)) : /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption2)',
      color: 'var(--ds-label-3)'
    }
  }, points, " pts")));
}
Object.assign(__ds_scope, { RARITY, AchievementCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/AchievementCard.jsx", error: String((e && e.message) || e) }); }

// components/family/AchievementDefRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Parent-side achievement definition row: what the badge is, how it's earned,
 * and whether it's active. Kids never see this surface.
 */
function AchievementDefRow({
  icon,
  name,
  rarity = 'common',
  points,
  condition,
  enabled = true,
  onToggle,
  style,
  ...rest
}) {
  const tint = __ds_scope.RARITY[String(rarity).toLowerCase()] || __ds_scope.RARITY.common;
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      padding: '10px 0',
      minHeight: 44,
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 'var(--ds-icon-tile-lg)',
      flex: '0 0 auto',
      textAlign: 'center',
      fontSize: 22,
      lineHeight: 1,
      fontFamily: 'var(--ds-font-emoji)',
      opacity: enabled ? 1 : 0.4
    }
  }, icon), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: enabled ? 'var(--ds-label)' : 'var(--ds-label-2)'
    }
  }, name), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 6,
      fontSize: 'var(--ds-text-caption)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: tint,
      fontWeight: 'var(--ds-weight-semibold)',
      textTransform: 'capitalize'
    }
  }, rarity), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ds-label-3)'
    }
  }, "\xB7"), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ds-gold)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, points, " pts")), condition ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-3)'
    }
  }, condition) : null), /*#__PURE__*/React.createElement("button", {
    type: "button",
    role: "switch",
    "aria-checked": enabled,
    "aria-label": name,
    onClick: () => onToggle && onToggle(!enabled),
    style: {
      width: 51,
      height: 31,
      flex: '0 0 auto',
      padding: 2,
      cursor: 'pointer',
      borderRadius: 'var(--ds-radius-capsule)',
      border: 'none',
      background: enabled ? 'var(--ds-accent)' : 'color-mix(in srgb, var(--ds-label) 20%, transparent)',
      transition: 'background var(--ds-snappy)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      width: 27,
      height: 27,
      borderRadius: '50%',
      background: '#fff',
      boxShadow: '0 1px 3px rgb(0 0 0 / .2)',
      transform: enabled ? 'translateX(20px)' : 'translateX(0)',
      transition: 'transform var(--ds-snappy)'
    }
  })));
}
Object.assign(__ds_scope, { AchievementDefRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/AchievementDefRow.jsx", error: String((e && e.message) || e) }); }

// components/family/ApprovalRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function ApprovalRow({
  choreName,
  childName,
  value,
  approved,
  busy,
  onApprove,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      padding: '10px 12px',
      margin: '0 -12px',
      borderRadius: 'var(--ds-radius-swatch)',
      background: approved ? 'var(--ds-blessing-wash)' : 'transparent',
      transition: 'background var(--ds-ease-out)',
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontWeight: 'var(--ds-weight-medium)',
      color: 'var(--ds-label)',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, choreName), childName ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, childName) : null), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-gold)',
      flex: '0 0 auto'
    }
  }, value), /*#__PURE__*/React.createElement(__ds_scope.Button, {
    variant: "gold",
    size: "sm",
    busy: busy,
    disabled: approved,
    icon: approved ? 'checkmark' : 'hand.thumbsup',
    onClick: onApprove
  }, approved ? 'Approved' : 'Approve'));
}
Object.assign(__ds_scope, { ApprovalRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/ApprovalRow.jsx", error: String((e && e.message) || e) }); }

// components/family/AtRiskCard.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const URGENCY = {
  DueTonight: {
    icon: 'exclamationmark.circle.fill',
    tint: 'var(--ds-help)'
  },
  MustDoDaily: {
    icon: 'flame.fill',
    tint: 'var(--ds-help)'
  },
  GettingTight: {
    icon: 'clock.badge.exclamationmark',
    tint: 'var(--ds-gold)'
  }
};
function AtRiskCard({
  items = [],
  previewLine,
  totalMoney,
  totalMinutes,
  style,
  ...rest
}) {
  if (items.length === 0) {
    return /*#__PURE__*/React.createElement(__ds_scope.GlassCard, _extends({}, rest, {
      style: style
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        flexDirection: 'column',
        gap: 4
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 'var(--ds-text-caption)',
        color: 'var(--ds-label-2)'
      }
    }, "Nothing at risk today \u270C\uFE0F"), previewLine ? /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 'var(--ds-text-caption)',
        color: 'var(--ds-label-3)'
      }
    }, previewLine) : null));
  }
  const parts = [];
  if (totalMoney) parts.push(totalMoney);
  if (totalMinutes) parts.push(totalMinutes + ' min');
  return /*#__PURE__*/React.createElement(__ds_scope.GlassCard, _extends({}, rest, {
    style: style
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.SectionHeader, null, "At risk today"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, items.map((item, i) => {
    const u = URGENCY[item.urgency] || URGENCY.GettingTight;
    return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 10
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 24,
        display: 'grid',
        placeItems: 'center',
        flex: '0 0 auto'
      }
    }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: u.icon,
      size: 15,
      color: u.tint
    })), /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 0,
        display: 'flex',
        flexDirection: 'column',
        gap: 2
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 'var(--ds-text-subheadline)',
        fontWeight: 'var(--ds-weight-medium)'
      }
    }, item.name), /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 'var(--ds-text-caption)',
        color: 'var(--ds-label-2)'
      }
    }, item.detail)), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'flex-end',
        gap: 2,
        flex: '0 0 auto'
      }
    }, item.money ? /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 'var(--ds-text-caption)',
        fontWeight: 'var(--ds-weight-bold)',
        color: 'var(--ds-gold)'
      }
    }, item.money) : null, item.minutes ? /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 'var(--ds-text-caption)',
        fontWeight: 'var(--ds-weight-bold)',
        color: 'var(--ds-help)'
      }
    }, "\u2212", item.minutes, " min") : null));
  })), items.length >= 2 && parts.length ? /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-footnote)',
      fontWeight: 'var(--ds-weight-bold)',
      paddingTop: 2
    }
  }, "On the line today: ", parts.join(' + ')) : null));
}
Object.assign(__ds_scope, { AtRiskCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/AtRiskCard.jsx", error: String((e && e.message) || e) }); }

// components/family/BalanceHero.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function BalanceHero({
  balance = '—',
  label = 'Balance',
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.GlassCard, _extends({}, rest, {
    padding: "var(--ds-card-padding-roomy)",
    style: style
  }), /*#__PURE__*/React.createElement(__ds_scope.SectionHeader, {
    kerning: "eyebrow"
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: 'var(--ds-font-rounded)',
      fontSize: 'var(--ds-text-hero)',
      fontWeight: 'var(--ds-weight-bold)',
      color: 'var(--ds-gold)',
      lineHeight: 1.08,
      marginTop: 2,
      fontVariantNumeric: 'tabular-nums'
    }
  }, balance));
}
Object.assign(__ds_scope, { BalanceHero });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/BalanceHero.jsx", error: String((e && e.message) || e) }); }

// components/family/CalendarMonth.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const DAY_LETTERS = ['S', 'M', 'T', 'W', 'T', 'F', 'S'];
const STATUS = {
  allDone: {
    tint: 'var(--ds-success)',
    label: 'All done'
  },
  inProgress: {
    tint: 'var(--ds-accent)',
    label: 'In progress'
  },
  missed: {
    tint: 'var(--ds-help)',
    label: 'Missed'
  }
};

/**
 * Month grid where each day carries a single status dot. Three states only —
 * the calendar answers "how did that day go", not "what happened".
 */
function CalendarMonth({
  monthLabel,
  days = [],
  selected,
  firstWeekday = 0,
  onSelect,
  onPrev,
  onNext,
  style,
  ...rest
}) {
  const cells = [];
  for (let i = 0; i < firstWeekday; i += 1) cells.push(null);
  days.forEach(d => cells.push(d));
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14,
      ...style
    }
  }), /*#__PURE__*/React.createElement(__ds_scope.GlassCard, {
    padding: "10px 14px"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Previous month",
    onClick: onPrev,
    style: {
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      padding: 6,
      color: 'var(--ds-accent)',
      display: 'grid',
      placeItems: 'center'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevron.left",
    size: 15
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      textAlign: 'center',
      fontSize: 'var(--ds-text-headline)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, monthLabel), /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Next month",
    onClick: onNext,
    style: {
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      padding: 6,
      color: 'var(--ds-accent)',
      display: 'grid',
      placeItems: 'center'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevron.right",
    size: 15
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(7, 1fr)',
      gap: 6
    }
  }, DAY_LETTERS.map((l, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      textAlign: 'center',
      fontSize: 'var(--ds-text-caption2)',
      fontWeight: 'var(--ds-weight-bold)',
      letterSpacing: '0.6px',
      color: 'var(--ds-label-3)',
      paddingBottom: 2
    }
  }, l)), cells.map((d, i) => {
    if (!d) return /*#__PURE__*/React.createElement("span", {
      key: 'pad' + i
    });
    const st = STATUS[d.status];
    const isSel = selected === d.day;
    return /*#__PURE__*/React.createElement("button", {
      key: d.day,
      type: "button",
      onClick: () => onSelect && onSelect(d.day),
      style: {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        gap: 4,
        padding: '9px 0 7px',
        minHeight: 52,
        cursor: 'pointer',
        fontFamily: 'inherit',
        borderRadius: 'var(--ds-radius-swatch)',
        background: d.outside ? 'transparent' : 'color-mix(in srgb, var(--ds-fill) 55%, transparent)',
        border: isSel ? '1.5px solid var(--ds-accent)' : '1.5px solid transparent',
        opacity: d.outside ? 0.4 : 1,
        transition: 'border-color var(--ds-snappy)'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 'var(--ds-text-subheadline)',
        fontWeight: isSel ? 'var(--ds-weight-bold)' : 'var(--ds-weight-medium)',
        color: 'var(--ds-label)',
        fontVariantNumeric: 'tabular-nums'
      }
    }, d.day), /*#__PURE__*/React.createElement("span", {
      style: {
        width: 6,
        height: 6,
        borderRadius: '50%',
        background: st ? st.tint : 'color-mix(in srgb, var(--ds-label) 18%, transparent)'
      }
    }));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'center',
      gap: 18,
      flexWrap: 'wrap',
      paddingTop: 2
    }
  }, Object.keys(STATUS).map(k => /*#__PURE__*/React.createElement("span", {
    key: k,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 7,
      height: 7,
      borderRadius: '50%',
      background: STATUS[k].tint
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, STATUS[k].label)))));
}
Object.assign(__ds_scope, { CalendarMonth });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/CalendarMonth.jsx", error: String((e && e.message) || e) }); }

// components/family/ChildPicker.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Scoped picker for whose data a shared card is showing.
 *
 * SINGLE-CHILD RULE: with one child this renders NOTHING. A picker implies a
 * choice, and a choice implies other children. Callers may render it
 * unconditionally — the guard lives here so it cannot be forgotten.
 */
function ChildPicker({
  children: names = [],
  value,
  onChange,
  style,
  ...rest
}) {
  const [open, setOpen] = React.useState(false);
  if (names.length <= 1) return null;
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      position: 'relative',
      display: 'inline-flex',
      ...style
    }
  }), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => setOpen(o => !o),
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7,
      minHeight: 44,
      padding: '8px 14px',
      cursor: 'pointer',
      fontFamily: 'inherit',
      borderRadius: 'var(--ds-radius-capsule)',
      border: 'none',
      background: 'color-mix(in srgb, var(--ds-accent) 12%, transparent)',
      color: 'var(--ds-accent)',
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevron.up.chevron.down",
    size: 13
  }), value, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: open ? 'chevron.up' : 'chevron.down',
    size: 12
  })), open ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    onClick: () => setOpen(false),
    style: {
      position: 'fixed',
      inset: 0,
      zIndex: 40
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      top: 'calc(100% + 6px)',
      zIndex: 41,
      minWidth: 190,
      padding: 6,
      borderRadius: 'var(--ds-radius-card)',
      background: 'color-mix(in srgb, var(--ds-card) 88%, transparent)',
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)',
      boxShadow: '0 8px 30px var(--ds-card-shadow-color)',
      border: '.5px solid var(--ds-hairline)',
      display: 'flex',
      flexDirection: 'column'
    }
  }, names.map(n => /*#__PURE__*/React.createElement("button", {
    key: n,
    type: "button",
    onClick: () => {
      setOpen(false);
      if (onChange) onChange(n);
    },
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      width: '100%',
      minHeight: 44,
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      textAlign: 'left',
      fontFamily: 'inherit',
      padding: '10px 12px',
      borderRadius: 8,
      fontSize: 'var(--ds-text-body)',
      color: 'var(--ds-label)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 16,
      flex: '0 0 auto'
    }
  }, n === value ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "checkmark",
    size: 14,
    color: "var(--ds-accent)"
  }) : null), n)))) : null);
}
Object.assign(__ds_scope, { ChildPicker });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/ChildPicker.jsx", error: String((e && e.message) || e) }); }

// components/family/ChildRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function statusLine(child) {
  if (child.helpRequests > 0) {
    return {
      text: child.helpRequests + ' help request' + (child.helpRequests === 1 ? '' : 's'),
      color: 'var(--ds-help)'
    };
  }
  if (child.pending > 0) return {
    text: child.pending + ' left today',
    color: 'var(--ds-label-2)'
  };
  return {
    text: 'All done ✨',
    color: 'var(--ds-gold)'
  };
}
function ChildRow({
  child,
  onTap,
  style,
  ...rest
}) {
  const s = statusLine(child);
  const p = child.total ? child.done / child.total : 0;
  return /*#__PURE__*/React.createElement(__ds_scope.GlassCard, _extends({}, rest, {
    style: {
      cursor: onTap ? 'pointer' : undefined,
      ...style
    },
    onClick: onTap
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.ProgressRing, {
    progress: p,
    label: child.done + '/' + child.total,
    size: 44
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontWeight: 'var(--ds-weight-medium)'
    }
  }, child.name), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: s.color
    }
  }, s.text))));
}
function ChildTile({
  child,
  onTap,
  style,
  ...rest
}) {
  const s = statusLine(child);
  const p = child.total ? child.done / child.total : 0;
  return /*#__PURE__*/React.createElement(__ds_scope.GlassCard, _extends({}, rest, {
    padding: "var(--ds-card-padding-tight)",
    style: {
      cursor: onTap ? 'pointer' : undefined,
      ...style
    },
    onClick: onTap
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.ProgressRing, {
    progress: p,
    label: child.done + '/' + child.total,
    size: 48
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)',
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      maxWidth: '100%'
    }
  }, child.name), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: s.color,
      whiteSpace: 'nowrap'
    }
  }, s.text)));
}
Object.assign(__ds_scope, { ChildRow, ChildTile });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/ChildRow.jsx", error: String((e && e.message) || e) }); }

// components/family/ChoreRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function ChoreRow({
  name,
  icon = '🧺',
  value,
  done,
  help,
  approvedBy,
  weekly,
  allowHelp = true,
  onToggle,
  onHelp,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      padding: '10px 0',
      minHeight: 'var(--ds-touch-target)',
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      width: 'var(--ds-icon-tile)',
      height: 'var(--ds-icon-tile)',
      flex: '0 0 auto',
      borderRadius: 'var(--ds-radius-field)',
      background: 'var(--ds-fill)',
      display: 'grid',
      placeItems: 'center',
      fontSize: 20,
      lineHeight: 1,
      fontFamily: 'var(--ds-font-emoji)'
    }
  }, icon), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontWeight: 'var(--ds-weight-medium)',
      color: done ? 'var(--ds-label-2)' : 'var(--ds-label)',
      textDecoration: done ? 'line-through' : 'none',
      textDecorationColor: 'var(--ds-label-2)'
    }
  }, name), help ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-help)'
    }
  }, "Help raised \u2014 protected") : approvedBy ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-gold)'
    }
  }, "Approved by ", approvedBy) : weekly ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, weekly.done, " of ", weekly.target, " this week") : null), value != null ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-gold)',
      opacity: done ? 0.5 : 1,
      flex: '0 0 auto'
    }
  }, value) : null, help ? /*#__PURE__*/React.createElement(__ds_scope.Tag, {
    tone: "var(--ds-help)",
    solid: true
  }, "HELP") : /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onToggle,
    "aria-label": done ? 'Undo' : 'Done',
    style: {
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      padding: 0,
      display: 'grid',
      placeItems: 'center',
      flex: '0 0 auto',
      color: done ? 'var(--ds-accent)' : 'var(--ds-label-2)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: done ? 'checkmark.circle.fill' : 'circle',
    size: 22
  })), allowHelp && !done && !help && onHelp ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onHelp,
    "aria-label": "Raise Help",
    style: {
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      padding: 0,
      display: 'grid',
      placeItems: 'center',
      flex: '0 0 auto',
      color: 'var(--ds-help)'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "questionmark.circle",
    size: 20
  })) : null);
}
Object.assign(__ds_scope, { ChoreRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/ChoreRow.jsx", error: String((e && e.message) || e) }); }

// components/family/DrivingLogRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Totals header for the driving log — hours toward a learner's permit. */
function DrivingTotals({
  totalHours = 0,
  nightHours = 0,
  style,
  ...rest
}) {
  const Row = ({
    label,
    value
  }) => /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      fontWeight: 'var(--ds-weight-bold)',
      textTransform: 'uppercase',
      letterSpacing: '0.8px',
      color: 'var(--ds-label-2)'
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-headline)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-accent)',
      fontVariantNumeric: 'tabular-nums'
    }
  }, value, " h"));
  return /*#__PURE__*/React.createElement(__ds_scope.GlassCard, _extends({}, rest, {
    style: style
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(Row, {
    label: "Total hours",
    value: totalHours
  }), /*#__PURE__*/React.createElement(Row, {
    label: "Night hours",
    value: nightHours
  })));
}

/** One logged supervised drive. Night drives get the moon; pending ones a tag. */
function DrivingLogRow({
  date,
  duration,
  from,
  to,
  supervisor,
  isNight = false,
  status = 'Approved',
  note,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4,
      padding: '10px 0',
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 22,
      flex: '0 0 auto',
      display: 'grid',
      placeItems: 'center',
      paddingTop: 2
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: isNight ? 'moon.stars' : 'sun.max',
    size: 16,
    color: "var(--ds-accent)"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, date, " \xB7 ", duration), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, from, "\u2013", to, supervisor ? ' · with ' + supervisor : '')), status === 'Pending' ? /*#__PURE__*/React.createElement(__ds_scope.Tag, null, "Pending") : null), note ? /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)',
      paddingLeft: 34
    }
  }, note) : null);
}
Object.assign(__ds_scope, { DrivingTotals, DrivingLogRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/DrivingLogRow.jsx", error: String((e && e.message) || e) }); }

// components/family/ExpandRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function ExpandRow({
  expanded,
  moreCount,
  label,
  color = 'var(--ds-accent)',
  onToggle,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("button", _extends({}, rest, {
    type: "button",
    onClick: onToggle,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      width: '100%',
      padding: '10px 0',
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      fontFamily: 'inherit',
      textAlign: 'left',
      minHeight: 'var(--ds-touch-target)',
      color,
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, expanded ? 'Show fewer' : moreCount + ' ' + label), /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: expanded ? 'chevron.up' : 'chevron.down',
    size: 12,
    strokeWidth: 3
  }));
}
function ConfirmRow({
  message,
  confirmTitle = 'Delete',
  keepTitle = 'Keep',
  tone = 'var(--ds-help)',
  onConfirm,
  onKeep,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10,
      padding: '10px 12px',
      margin: '0 -12px',
      borderRadius: 'var(--ds-radius-swatch)',
      background: 'color-mix(in srgb, ' + tone + ' 8%, transparent)',
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-medium)'
    }
  }, message), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onKeep,
    style: {
      padding: '7px 14px',
      borderRadius: 'var(--ds-radius-field)',
      border: 'none',
      background: 'var(--ds-fill-strong)',
      color: 'var(--ds-label)',
      cursor: 'pointer',
      fontFamily: 'inherit',
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, keepTitle), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onConfirm,
    style: {
      padding: '7px 14px',
      borderRadius: 'var(--ds-radius-field)',
      border: 'none',
      background: tone,
      color: '#fff',
      cursor: 'pointer',
      fontFamily: 'inherit',
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, confirmTitle)));
}
Object.assign(__ds_scope, { ExpandRow, ConfirmRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/ExpandRow.jsx", error: String((e && e.message) || e) }); }

// components/family/FamilyMemberRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Avatar hue carries the ROLE: accent for parents, gold for children.
 *  (FamilyMembersView.swift — the only place gold is used non-monetarily,
 *  because a child IS the earning party.) */
function FamilyMemberRow({
  name,
  role = 'Child',
  isYou = false,
  locked = false,
  onResetPassword,
  onToggleLock,
  style,
  ...rest
}) {
  const [open, setOpen] = React.useState(false);
  const isParent = role === 'Parent';
  const hue = isParent ? 'var(--ds-accent)' : 'var(--ds-gold)';
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      position: 'relative',
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      padding: '10px 0',
      minHeight: 44,
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 'var(--ds-avatar)',
      height: 'var(--ds-avatar)',
      flex: '0 0 auto',
      borderRadius: '50%',
      display: 'grid',
      placeItems: 'center',
      background: hue,
      color: 'var(--ds-on-accent)',
      fontSize: 'var(--ds-text-headline)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, name.slice(0, 1).toUpperCase()), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 7
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, name), isYou ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-3)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, "You") : null, locked ? /*#__PURE__*/React.createElement(__ds_scope.Tag, {
    tone: "var(--ds-help)"
  }, "Locked") : null), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, role)), /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": 'Manage ' + name,
    onClick: () => setOpen(o => !o),
    style: {
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      padding: 6,
      color: 'var(--ds-accent)',
      display: 'grid',
      placeItems: 'center'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "ellipsis.circle",
    size: 17
  })), open ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    onClick: () => setOpen(false),
    style: {
      position: 'fixed',
      inset: 0,
      zIndex: 40
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      right: 0,
      top: '76%',
      zIndex: 41,
      minWidth: 210,
      padding: 6,
      borderRadius: 'var(--ds-radius-card)',
      background: 'color-mix(in srgb, var(--ds-card) 88%, transparent)',
      backdropFilter: 'blur(20px)',
      WebkitBackdropFilter: 'blur(20px)',
      boxShadow: '0 8px 30px var(--ds-card-shadow-color)',
      border: '.5px solid var(--ds-hairline)',
      display: 'flex',
      flexDirection: 'column'
    }
  }, [{
    label: 'Reset password',
    icon: 'key',
    tint: 'var(--ds-label)',
    fn: onResetPassword
  }, {
    label: locked ? 'Unlock account' : 'Lock account',
    icon: 'lock.fill',
    tint: 'var(--ds-help)',
    fn: onToggleLock
  }].map(a => /*#__PURE__*/React.createElement("button", {
    key: a.label,
    type: "button",
    onClick: () => {
      setOpen(false);
      if (a.fn) a.fn();
    },
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      width: '100%',
      minHeight: 44,
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      textAlign: 'left',
      fontFamily: 'inherit',
      padding: '10px 12px',
      borderRadius: 8,
      fontSize: 'var(--ds-text-body)',
      color: a.tint
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: a.icon,
    size: 16,
    color: "var(--ds-accent)"
  }), a.label)))) : null);
}
Object.assign(__ds_scope, { FamilyMemberRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/FamilyMemberRow.jsx", error: String((e && e.message) || e) }); }

// components/family/GoalCard.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function GoalCard({
  name,
  current,
  target,
  percent,
  empty,
  onTap,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.GlassCard, _extends({}, rest, {
    style: {
      cursor: onTap ? 'pointer' : undefined,
      ...style
    },
    onClick: onTap
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 5
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "target",
    size: 12,
    color: "var(--ds-label-2)"
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      fontWeight: 'var(--ds-weight-bold)',
      textTransform: 'uppercase',
      letterSpacing: 'var(--ds-kerning-section)',
      color: 'var(--ds-label-2)'
    }
  }, empty ? 'Goals' : 'Goal')), empty ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, "Set a savings goal!"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-accent)'
    }
  }, "Something to work toward \u2192")) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, name), /*#__PURE__*/React.createElement(__ds_scope.ProgressBar, {
    value: percent,
    total: 100
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, current, " of ", target))));
}
Object.assign(__ds_scope, { GoalCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/GoalCard.jsx", error: String((e && e.message) || e) }); }

// components/family/HelpRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function HelpRow({
  choreName,
  reason,
  childName,
  onOpen,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("button", _extends({}, rest, {
    type: "button",
    onClick: onOpen,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      width: '100%',
      padding: '10px 0',
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      fontFamily: 'inherit',
      textAlign: 'left',
      minHeight: 'var(--ds-touch-target)',
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      flex: '0 0 auto',
      borderRadius: '50%',
      background: 'var(--ds-help)'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontWeight: 'var(--ds-weight-medium)',
      color: 'var(--ds-label)'
    }
  }, choreName), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)',
      display: '-webkit-box',
      WebkitLineClamp: 2,
      WebkitBoxOrient: 'vertical',
      overflow: 'hidden'
    }
  }, reason || childName + ' needs a hand')), /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevron.right",
    size: 12,
    color: "var(--ds-label-3)"
  }));
}
Object.assign(__ds_scope, { HelpRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/HelpRow.jsx", error: String((e && e.message) || e) }); }

// components/family/LevelBadge.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const LEVEL_TIERS = [{
  level: 0,
  title: 'STARTER',
  icon: '🌱',
  color: 'var(--db-level-0)'
}, {
  level: 1,
  title: 'COMMON',
  icon: '⭐',
  color: 'var(--db-level-1)'
}, {
  level: 2,
  title: 'UNCOMMON',
  icon: '⚡',
  color: 'var(--db-level-2)'
}, {
  level: 3,
  title: 'RARE',
  icon: '🔥',
  color: 'var(--db-level-3)'
}, {
  level: 4,
  title: 'EPIC',
  icon: '💎',
  color: 'var(--db-level-4)'
}, {
  level: 5,
  title: 'LEGENDARY',
  icon: '👑',
  color: 'var(--db-level-5)'
}];

/** % of today's chores done → tier. Mirrors KidHomeView.tier(for:) exactly.
 *  Capitalised so the compiler exposes it on the window namespace. */
function TierFor(percent) {
  if (percent >= 100) return LEVEL_TIERS[5];
  if (percent >= 75) return LEVEL_TIERS[4];
  if (percent >= 50) return LEVEL_TIERS[3];
  if (percent >= 25) return LEVEL_TIERS[2];
  if (percent >= 1) return LEVEL_TIERS[1];
  return LEVEL_TIERS[0];
}
function LevelBadge({
  percent = 0,
  style,
  ...rest
}) {
  const t = TierFor(percent);
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: 6,
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      padding: '12px 24px',
      borderRadius: 'var(--ds-radius-capsule)',
      background: 'linear-gradient(135deg, ' + t.color + ', color-mix(in srgb, ' + t.color + ' 65%, transparent))',
      boxShadow: 'var(--ds-level-glow) color-mix(in srgb, ' + t.color + ' 50%, transparent)',
      transition: 'background var(--ds-spring), box-shadow var(--ds-spring)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 30,
      lineHeight: 1,
      fontFamily: 'var(--ds-font-emoji)'
    }
  }, t.icon), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-headline)',
      fontWeight: 'var(--ds-weight-bold)',
      color: 'rgb(255 255 255 / .9)'
    }
  }, "LVL"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-title)',
      fontWeight: 'var(--ds-weight-heavy)',
      color: '#fff',
      lineHeight: 1
    }
  }, t.level))), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      fontWeight: 'var(--ds-weight-bold)',
      color: t.color,
      letterSpacing: 'var(--ds-kerning-level)'
    }
  }, t.title));
}
function XPBar({
  done = 0,
  total = 0,
  percent,
  style,
  ...rest
}) {
  const pct = percent != null ? percent : total ? Math.round(done / total * 100) : 0;
  const t = TierFor(pct);
  const milestones = [['⚡', 25], ['🔥', 50], ['💎', 75], ['👑', 100]];
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10,
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      fontWeight: 'var(--ds-weight-bold)',
      textTransform: 'uppercase',
      letterSpacing: '0.6px',
      color: 'var(--ds-label-2)'
    }
  }, "Today's progress"), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-bold)',
      color: 'var(--ds-accent)',
      fontVariantNumeric: 'tabular-nums'
    }
  }, done, " / ", total, " XP")), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 'var(--ds-xp-bar-height)',
      borderRadius: 'var(--ds-radius-capsule)',
      background: 'color-mix(in srgb, var(--ds-label) 10%, transparent)',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 'max(12px, ' + pct + '%)',
      height: '100%',
      borderRadius: 'var(--ds-radius-capsule)',
      background: 'linear-gradient(to right, var(--ds-accent), ' + t.color + ')',
      transition: 'width var(--ds-spring-slow)'
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between'
    }
  }, milestones.map(([ic, at]) => {
    const reached = pct >= at;
    return /*#__PURE__*/React.createElement("span", {
      key: at,
      style: {
        fontSize: 15,
        lineHeight: 1,
        fontFamily: 'var(--ds-font-emoji)',
        opacity: reached ? 1 : 0.28,
        filter: reached ? 'none' : 'grayscale(1)',
        transform: reached ? 'none' : 'scale(0.85)',
        transition: 'all var(--ds-spring)'
      }
    }, ic);
  })));
}
Object.assign(__ds_scope, { LEVEL_TIERS, TierFor, LevelBadge, XPBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/LevelBadge.jsx", error: String((e && e.message) || e) }); }

// components/family/PlannerChoreRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function PlannerChoreRow({
  name,
  icon,
  value,
  isTask = true,
  schedule,
  assignee,
  importance,
  active = true,
  onTap,
  style,
  ...rest
}) {
  const glyph = icon || (isTask ? '💰' : '✅');
  const caption = [schedule, assignee].filter(Boolean).join(' · ');
  return /*#__PURE__*/React.createElement("button", _extends({}, rest, {
    type: "button",
    onClick: onTap,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      width: '100%',
      padding: '10px 0',
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      fontFamily: 'inherit',
      textAlign: 'left',
      minHeight: 'var(--ds-touch-target)',
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      width: 'var(--ds-icon-tile)',
      height: 'var(--ds-icon-tile)',
      flex: '0 0 auto',
      borderRadius: 'var(--ds-radius-field)',
      background: 'var(--ds-fill)',
      display: 'grid',
      placeItems: 'center',
      fontSize: 20,
      lineHeight: 1,
      fontFamily: 'var(--ds-font-emoji)',
      opacity: active ? 1 : 0.45
    }
  }, glyph), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontWeight: 'var(--ds-weight-medium)',
      color: active ? 'var(--ds-label)' : 'var(--ds-label-2)',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, name), !active ? /*#__PURE__*/React.createElement(__ds_scope.Tag, null, "Off") : null), caption ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, caption) : null), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'flex-end',
      gap: 3,
      flex: '0 0 auto'
    }
  }, isTask ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-gold)',
      opacity: active ? 1 : 0.5
    }
  }, value) : /*#__PURE__*/React.createElement(__ds_scope.Tag, null, "Routine"), importance > 0 ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption2)',
      color: 'var(--ds-label-2)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--ds-font-emoji)'
    }
  }, "\uD83D\uDCFA"), " ", importance) : null));
}
Object.assign(__ds_scope, { PlannerChoreRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/PlannerChoreRow.jsx", error: String((e && e.message) || e) }); }

// components/family/PlannerGrid.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const DAYS = [['Sunday', 'S'], ['Monday', 'M'], ['Tuesday', 'T'], ['Wednesday', 'W'], ['Thursday', 'T'], ['Friday', 'F'], ['Saturday', 'S']];
function PlannerGrid({
  chores = [],
  showAssignee,
  onToggle,
  onEdit,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      flexDirection: 'column',
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--ds-day-gap)',
      padding: '8px 4px',
      position: 'sticky',
      top: 0,
      zIndex: 2,
      background: 'color-mix(in srgb, var(--db-bg-top) 92%, transparent)',
      backdropFilter: 'blur(12px)',
      WebkitBackdropFilter: 'blur(12px)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), DAYS.map(([full, letter], i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      width: 'var(--ds-day-cell)',
      textAlign: 'center',
      flex: '0 0 auto',
      fontSize: 'var(--ds-text-caption)',
      fontWeight: 'var(--ds-weight-bold)',
      color: 'var(--ds-label-2)'
    }
  }, letter))), chores.map((c, ci) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: c.id != null ? c.id : ci
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--ds-day-gap)',
      padding: '7px 4px'
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => onEdit && onEdit(c),
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      fontFamily: 'inherit',
      textAlign: 'left',
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 17,
      lineHeight: 1,
      fontFamily: 'var(--ds-font-emoji)',
      flex: '0 0 auto'
    }
  }, c.icon || (c.isTask === false ? '✅' : '💰')), /*#__PURE__*/React.createElement("span", {
    style: {
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 1
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-medium)',
      color: c.active === false ? 'var(--ds-label-2)' : 'var(--ds-label)',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, c.name), showAssignee && c.assignee ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption2)',
      color: 'var(--ds-label-2)'
    }
  }, c.assignee) : c.isTask !== false && c.value ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption2)',
      color: 'var(--ds-gold)'
    }
  }, c.value) : null)), c.weeklyTarget ? /*#__PURE__*/React.createElement("span", {
    style: {
      width: 'calc(var(--ds-day-cell) * 7 + var(--ds-day-gap) * 6)',
      flex: '0 0 auto',
      textAlign: 'center',
      fontSize: 'var(--ds-text-caption2)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-label-2)'
    }
  }, c.weeklyTarget, "\xD7/wk") : DAYS.map(([full], i) => {
    const on = (c.days || []).includes(full);
    return /*#__PURE__*/React.createElement("button", {
      key: i,
      type: "button",
      onClick: () => onToggle && onToggle(c, full),
      "aria-pressed": on,
      "aria-label": c.name + ' ' + full,
      style: {
        width: 'var(--ds-day-cell)',
        height: 'var(--ds-day-cell)',
        flex: '0 0 auto',
        borderRadius: 'var(--ds-radius-day-cell)',
        cursor: 'pointer',
        border: on ? 'none' : '.5px solid color-mix(in srgb, var(--ds-label) 6%, transparent)',
        background: on ? 'var(--ds-accent)' : 'color-mix(in srgb, var(--ds-label) 12%, transparent)',
        display: 'grid',
        placeItems: 'center',
        padding: 0,
        transition: 'background var(--ds-snappy)'
      }
    }, on ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
      name: "checkmark",
      size: 12,
      color: "#fff",
      strokeWidth: 3
    }) : null);
  })), ci < chores.length - 1 ? /*#__PURE__*/React.createElement("div", {
    style: {
      height: '.5px',
      background: 'var(--ds-hairline)',
      marginLeft: 4
    }
  }) : null)));
}
Object.assign(__ds_scope, { PlannerGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/PlannerGrid.jsx", error: String((e && e.message) || e) }); }

// components/family/ScreenTimeCard.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Pool({
  name,
  tag,
  minutes,
  floor,
  atRisk
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      padding: 10,
      background: 'color-mix(in srgb, var(--ds-fill) 50%, transparent)',
      borderRadius: 'var(--ds-radius-swatch)',
      display: 'flex',
      flexDirection: 'column',
      gap: 4
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      fontWeight: 'var(--ds-weight-bold)'
    }
  }, name), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption2)',
      color: 'var(--ds-label-3)'
    }
  }, tag)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-title3)',
      fontWeight: 'var(--ds-weight-bold)',
      color: 'var(--ds-accent)',
      lineHeight: 1.15
    }
  }, minutes), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-caption2)',
      color: 'var(--ds-label-2)'
    }
  }, "in your pool"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 4,
      paddingTop: 2
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "lock.fill",
    size: 11,
    color: "var(--ds-label-3)"
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption2)',
      color: 'var(--ds-label-2)'
    }
  }, "Always keeps ", /*#__PURE__*/React.createElement("strong", {
    style: {
      color: 'var(--ds-label)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, floor))), atRisk ? /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-caption2)',
      color: 'var(--ds-help)'
    }
  }, "Up to ", /*#__PURE__*/React.createElement("strong", {
    style: {
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, atRisk, " min"), " on the line") : null);
}
function ScreenTimeCard({
  weekday,
  weekend,
  prices = [],
  onHistory,
  onSettings,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.GlassCard, _extends({}, rest, {
    style: style
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.SectionHeader, null, "Screen time this week"), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), onHistory ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onHistory,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 3,
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      padding: 0,
      fontFamily: 'inherit',
      fontSize: 'var(--ds-text-caption)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-accent)'
    }
  }, "History", /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "chevron.right",
    size: 11
  })) : null, onSettings ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onSettings,
    "aria-label": "Adjust screen time settings",
    style: {
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      padding: 0,
      color: 'var(--ds-accent)',
      display: 'grid',
      placeItems: 'center'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "slider.horizontal.3",
    size: 14
  })) : null), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      gap: 10
    }
  }, weekday ? /*#__PURE__*/React.createElement(Pool, _extends({
    name: "Weekdays",
    tag: "Mon\u2013Fri"
  }, weekday)) : null, weekend ? /*#__PURE__*/React.createElement(Pool, _extends({
    name: "Weekend",
    tag: "Sat\u2013Sun"
  }, weekend)) : null), prices.length === 0 ? /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, "No chores are priced this week \u2014 nothing at risk. \uD83D\uDE0E") : /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-label-2)'
    }
  }, "What each chore is worth"), prices.map(p => /*#__PURE__*/React.createElement("div", {
    key: p.name,
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)'
    }
  }, p.name), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-help)',
      whiteSpace: 'nowrap'
    }
  }, "Miss once: \u2212", p.minutes, " min"))))));
}
Object.assign(__ds_scope, { ScreenTimeCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/ScreenTimeCard.jsx", error: String((e && e.message) || e) }); }

// components/family/ThemeSwatch.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const DB_THEMES = [{
  key: 'sunroom',
  name: 'Sunroom',
  mood: 'warm · light',
  top: '#FFFDF9',
  bottom: '#FBF1E2',
  card: '#FFFFFF',
  accent: '#C7284F',
  gold: '#C98A1E',
  dark: false
}, {
  key: 'sky',
  name: 'Sky',
  mood: 'calm · light',
  top: '#FBFCFF',
  bottom: '#EAF1FE',
  card: '#FFFFFF',
  accent: '#3D7BE0',
  gold: '#C98A1E',
  dark: false
}, {
  key: 'rosewater',
  name: 'Rosewater',
  mood: 'soft · light',
  top: '#FFF9FB',
  bottom: '#FBEAF1',
  card: '#FFFFFF',
  accent: '#D24E86',
  gold: '#C98A1E',
  dark: false
}, {
  key: 'meadow',
  name: 'Meadow',
  mood: 'fresh · light',
  top: '#F8FBF6',
  bottom: '#E9F4E7',
  card: '#FFFFFF',
  accent: '#3E9E6B',
  gold: '#C98A1E',
  dark: false
}, {
  key: 'mulberry',
  name: 'Mulberry',
  mood: 'cozy · dark',
  top: '#3E1B30',
  bottom: '#2A1220',
  card: '#4A2237',
  accent: '#EA6E92',
  gold: '#E7B44A',
  dark: true
}, {
  key: 'harbor',
  name: 'Harbor',
  mood: 'quiet · dark',
  top: '#223049',
  bottom: '#161E2C',
  card: '#2A3852',
  accent: '#5B9BE0',
  gold: '#E7B44A',
  dark: true
}];

/** A live preview of a theme: its real background, a floating card with the
 *  accent and a gold coin, and the progress glow. What she taps is what the app
 *  becomes. */
function ThemeSwatch({
  theme,
  width = 76,
  height = 52,
  style,
  ...rest
}) {
  const t = typeof theme === 'string' ? DB_THEMES.find(x => x.key === theme) : theme;
  if (!t) return null;
  const stroke = t.dark ? 'rgb(255 255 255 / .07)' : 'rgb(0 0 0 / .05)';
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      width,
      height,
      flex: '0 0 auto',
      borderRadius: 'var(--ds-radius-swatch)',
      background: 'linear-gradient(to bottom, ' + t.top + ', ' + t.bottom + (t.dark ? ' 60%' : '') + ')',
      boxShadow: 'inset 0 0 0 .5px ' + stroke,
      position: 'relative',
      padding: 7,
      boxSizing: 'border-box',
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'space-between',
      gap: 4,
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 20,
      borderRadius: 6,
      background: t.card,
      boxShadow: 'inset 0 0 0 .5px ' + stroke,
      display: 'flex',
      alignItems: 'center',
      gap: 4,
      padding: '0 5px'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: '50%',
      background: t.accent
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: '50%',
      background: t.gold
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 5,
      borderRadius: 999,
      background: 'linear-gradient(to right, ' + t.accent + ', #E7A83C)'
    }
  }));
}
function ThemeRow({
  theme,
  selected,
  onSelect,
  style,
  ...rest
}) {
  const t = typeof theme === 'string' ? DB_THEMES.find(x => x.key === theme) : theme;
  if (!t) return null;
  return /*#__PURE__*/React.createElement("button", _extends({}, rest, {
    type: "button",
    onClick: () => onSelect && onSelect(t.key),
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 14,
      width: '100%',
      padding: '4px 0',
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      fontFamily: 'inherit',
      textAlign: 'left',
      minHeight: 'var(--ds-touch-target)',
      ...style
    }
  }), /*#__PURE__*/React.createElement(ThemeSwatch, {
    theme: t
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-label)'
    }
  }, t.name), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, t.mood)), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 22,
      height: 22,
      flex: '0 0 auto',
      borderRadius: '50%',
      display: 'grid',
      placeItems: 'center',
      boxSizing: 'border-box',
      border: selected ? '6px solid ' + t.accent : '1.5px solid var(--ds-label-3)'
    }
  }, selected ? /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: "checkmark",
    size: 10,
    color: "#fff",
    strokeWidth: 4
  }) : null));
}
Object.assign(__ds_scope, { DB_THEMES, ThemeSwatch, ThemeRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/ThemeSwatch.jsx", error: String((e && e.message) || e) }); }

// components/family/TransactionRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function TransactionRow({
  description,
  date,
  amount,
  negative,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      padding: '9px 0',
      minHeight: 'var(--ds-touch-target)',
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      color: 'var(--ds-label)',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, description), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, date)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: negative ? 'var(--ds-label-2)' : 'var(--ds-gold)',
      flex: '0 0 auto',
      fontVariantNumeric: 'tabular-nums'
    }
  }, amount));
}
Object.assign(__ds_scope, { TransactionRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/TransactionRow.jsx", error: String((e && e.message) || e) }); }

// components/family/WeekStrip.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function WeekStrip({
  days = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      gap: 6,
      ...style
    }
  }), days.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      flex: 1,
      minWidth: 0,
      textAlign: 'center',
      padding: '8px 2px',
      background: 'color-mix(in srgb, var(--ds-fill) 50%, transparent)',
      borderRadius: 'var(--ds-radius-field)',
      display: 'flex',
      flexDirection: 'column',
      gap: 3
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption2)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-label-2)'
    }
  }, d.letter), d.amount ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption2)',
      fontWeight: 'var(--ds-weight-bold)',
      color: 'var(--ds-gold)',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, d.amount) : /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption2)',
      color: 'var(--ds-label-3)'
    }
  }, "\u2014"))));
}
Object.assign(__ds_scope, { WeekStrip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/WeekStrip.jsx", error: String((e && e.message) || e) }); }

// components/family/YearHeatmapCard.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* The rainbow year — IMPLEMENTATION.md §1.2.
   Hue is the REAL day-of-year, never position within the window, so a given day is
   the same colour on every surface (card, full year, wall, widget). Intensity is the
   required-chore ratio on a LINEAR ramp from a 34% saturation floor (the `forgiving`
   curve, decided after testing three against a full year). A bad day is a drained
   colour, never a grey hole. Optionals only ever ADD bloom — they never withhold
   colour, because dimming a square for declining an optional punishes a kid for
   taking a choice you gave him. */

const SIZES = {
  card: {
    weeks: 12,
    cell: 18,
    gap: 3,
    scroll: false
  },
  full: {
    weeks: 52,
    cell: 11,
    gap: 2.5,
    scroll: true
  },
  wall: {
    weeks: 52,
    cell: 14,
    gap: 3,
    scroll: true
  }
};
const LEGACY = {
  AllComplete: {
    requiredDone: 1,
    requiredTotal: 1,
    optionalDone: 1,
    optionalTotal: 1
  },
  PartialComplete: {
    requiredDone: 2,
    requiredTotal: 3,
    optionalDone: 0,
    optionalTotal: 0
  },
  NoneComplete: {
    requiredDone: 0,
    requiredTotal: 3,
    optionalDone: 0,
    optionalTotal: 0
  },
  none: {
    isFuture: true
  }
};
function dayOfYear(d) {
  return Math.floor((d - new Date(d.getFullYear(), 0, 0)) / 86400000);
}

/** Which way the lightness ramp runs. Read from the live theme's card surface rather
    than a `dark` prop, so every one of the six themes gets it right unasked. */
function isDarkSurface() {
  if (typeof window === 'undefined') return false;
  const v = getComputedStyle(document.documentElement).getPropertyValue('--ds-card').trim();
  let r, g, b;
  const hex = v.match(/^#([0-9a-f]{3,8})$/i);
  if (hex) {
    const h = hex[1].length < 6 ? hex[1].split('').map(c => c + c).join('') : hex[1];
    r = parseInt(h.slice(0, 2), 16);
    g = parseInt(h.slice(2, 4), 16);
    b = parseInt(h.slice(4, 6), 16);
  } else {
    const n = v.match(/[\d.]+/g);
    if (!n || n.length < 3) return false;
    [r, g, b] = n.map(Number);
  }
  return (0.2126 * r + 0.7152 * g + 0.0722 * b) / 255 < 0.5;
}

/** Deterministic sample so specimens render identically every time. */
function sample(n) {
  const out = [];
  for (let i = 0; i < n; i++) {
    const h = i * 2654435761 % 100;
    const requiredTotal = 3 + i % 3;
    const requiredDone = h > 62 ? requiredTotal : h > 34 ? Math.max(1, requiredTotal - 1) : h > 22 ? 1 : 0;
    out.push({
      requiredDone,
      requiredTotal,
      optionalDone: h > 88 ? 2 : 0,
      optionalTotal: 2
    });
  }
  // The current week isn't over: the rest of it renders as future outlines.
  for (let i = Math.max(0, n - 3); i < n; i++) out[i] = {
    isFuture: true
  };
  return out;
}
function normalise(d) {
  if (typeof d === 'string') return LEGACY[d] || LEGACY.none;
  return d || LEGACY.none;
}
function YearHeatmapCard({
  title = 'Your year',
  size = 'card',
  days,
  perfectDays,
  onDay,
  cell,
  weeks,
  style,
  ...rest
}) {
  const s = SIZES[size] || SIZES.card;
  const edge = cell != null ? cell : s.cell;
  const cols = weeks != null ? weeks : s.weeks;
  const raw = days && days.length ? days : sample(cols * 7);
  const cells = raw.slice(-cols * 7).map(normalise);
  const perfect = perfectDays != null ? perfectDays : cells.filter(d => !d.isFuture && d.requiredTotal > 0 && d.requiredDone >= d.requiredTotal).length;
  const todayDoy = dayOfYear(new Date());
  const dark = isDarkSurface();
  const columns = [];
  for (let i = 0; i < cells.length; i += 7) columns.push(cells.slice(i, i + 7));
  const grid = /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: s.gap,
      direction: 'ltr'
    }
  }, columns.map((week, wi) => /*#__PURE__*/React.createElement("div", {
    key: wi,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: s.gap
    }
  }, week.map((d, di) => {
    const index = wi * 7 + di;
    const doy = todayDoy - (cells.length - 1 - index);
    const hue = (doy % 365 + 365) % 365 / 365 * 360;
    const ratio = d.requiredTotal > 0 ? Math.min(1, d.requiredDone / d.requiredTotal) : 0;
    const complete = !d.isFuture && d.requiredTotal > 0 && d.requiredDone >= d.requiredTotal;
    const bloom = complete && d.optionalTotal > 0 && d.optionalDone > 0;
    const box = {
      width: edge,
      height: edge,
      borderRadius: 'var(--ds-radius-heatmap)',
      cursor: onDay && !d.isFuture ? 'pointer' : 'default'
    };
    if (d.isFuture) {
      box.background = 'transparent';
      box.boxShadow = 'inset 0 0 0 1px color-mix(in srgb, var(--ds-label) 9%, transparent)';
    } else {
      // Opaque by construction. Mixing toward a translucent fill let the card show
      // through and dropped drained days to ~15% saturation — the grey hole the
      // spec forbids — and made the same day a different colour on every surface.
      const sat = 34 + ratio * 52; // 34% → 86%
      const light = dark ? 16 + ratio * 40 : 84 - ratio * 40;
      box.background = `hsl(${hue.toFixed(1)} ${sat.toFixed(0)}% ${light.toFixed(0)}%)`;
      // bloom scales with the cell or it smears into its neighbours on a year wall
      if (bloom) box.boxShadow = `0 0 ${(edge * 0.55).toFixed(1)}px hsl(${hue.toFixed(1)} 90% 55% / .7)`;
    }
    return /*#__PURE__*/React.createElement("div", {
      key: di,
      onClick: () => onDay && !d.isFuture && onDay({
        ...d,
        dayOfYear: doy
      }),
      style: box
    });
  }))));
  return /*#__PURE__*/React.createElement(__ds_scope.GlassCard, _extends({}, rest, {
    style: style
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.SectionHeader, null, title), s.scroll ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      overflowX: 'auto',
      direction: 'rtl'
    }
  }, grid) : grid, perfect > 0 ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      color: 'var(--ds-label)'
    }
  }, perfect), " perfect day", perfect === 1 ? '' : 's', " this year \u2728") : null));
}
Object.assign(__ds_scope, { YearHeatmapCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/family/YearHeatmapCard.jsx", error: String((e && e.message) || e) }); }

// components/sheets/DayPicker.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const ALL_DAYS = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
const LETTERS = ['S', 'M', 'T', 'W', 'T', 'F', 'S'];
function DayPicker({
  selected = [],
  onChange,
  style,
  ...rest
}) {
  const set = new Set(selected);
  const toggle = day => {
    const next = new Set(set);
    if (next.has(day)) next.delete(day);else next.add(day);
    onChange && onChange(ALL_DAYS.filter(d => next.has(d)));
  };
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      gap: 6,
      ...style
    }
  }), ALL_DAYS.map((day, i) => {
    const on = set.has(day);
    return /*#__PURE__*/React.createElement("button", {
      key: day,
      type: "button",
      onClick: () => toggle(day),
      "aria-pressed": on,
      "aria-label": day,
      style: {
        flex: 1,
        display: 'grid',
        placeItems: 'center',
        width: 'var(--ds-day-pill)',
        height: 'var(--ds-day-pill)',
        maxWidth: 'var(--ds-day-pill)',
        margin: '0 auto',
        borderRadius: '50%',
        border: 'none',
        cursor: 'pointer',
        fontFamily: 'inherit',
        fontSize: 'var(--ds-text-subheadline)',
        fontWeight: 'var(--ds-weight-semibold)',
        background: on ? 'var(--ds-accent)' : 'color-mix(in srgb, var(--ds-label) 14%, transparent)',
        color: on ? '#fff' : 'var(--ds-label)',
        transition: 'background var(--ds-snappy), color var(--ds-snappy)'
      }
    }, LETTERS[i]);
  }));
}
Object.assign(__ds_scope, { ALL_DAYS, DayPicker });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/sheets/DayPicker.jsx", error: String((e && e.message) || e) }); }

// components/sheets/SegmentedPicker.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function SegmentedPicker({
  options = [],
  value,
  onChange,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    role: "tablist",
    style: {
      display: 'flex',
      padding: 2,
      gap: 2,
      background: 'color-mix(in srgb, var(--ds-fill) 70%, transparent)',
      borderRadius: 'var(--ds-radius-field)',
      ...style
    }
  }), options.map(o => {
    const v = typeof o === 'string' ? o : o.value;
    const l = typeof o === 'string' ? o : o.label;
    const on = v === value;
    return /*#__PURE__*/React.createElement("button", {
      key: v,
      type: "button",
      role: "tab",
      "aria-selected": on,
      onClick: () => onChange && onChange(v),
      style: {
        flex: 1,
        minHeight: 30,
        border: 'none',
        cursor: 'pointer',
        fontFamily: 'inherit',
        fontSize: 'var(--ds-text-subheadline)',
        fontWeight: on ? 'var(--ds-weight-semibold)' : 'var(--ds-weight-regular)',
        borderRadius: 7,
        color: 'var(--ds-label)',
        background: on ? 'var(--ds-card)' : 'transparent',
        boxShadow: on ? '0 1px 3px rgb(0 0 0 / .12)' : 'none',
        transition: 'all var(--ds-ease-in-out)'
      }
    }, l);
  }));
}
Object.assign(__ds_scope, { SegmentedPicker });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/sheets/SegmentedPicker.jsx", error: String((e && e.message) || e) }); }

// components/sheets/SheetActionBar.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function SheetActionBar({
  saveTitle = 'Save',
  saving,
  canSave = true,
  cancelTitle = 'Cancel',
  onCancel,
  onSave,
  style,
  ...rest
}) {
  const base = {
    flex: 1,
    minHeight: 'var(--ds-touch-target)',
    border: 'none',
    borderRadius: 'var(--ds-radius-swatch)',
    cursor: 'pointer',
    fontFamily: 'inherit',
    fontSize: 'var(--ds-text-body)'
  };
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      gap: 12,
      ...style
    }
  }), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onCancel,
    disabled: saving,
    style: {
      ...base,
      background: 'color-mix(in srgb, var(--ds-fill-strong) 50%, transparent)',
      color: 'var(--ds-label)',
      fontWeight: 'var(--ds-weight-medium)'
    }
  }, cancelTitle), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onSave,
    disabled: saving || !canSave,
    style: {
      ...base,
      background: canSave ? 'var(--ds-accent)' : 'color-mix(in srgb, var(--ds-label-2) 30%, transparent)',
      color: '#fff',
      fontWeight: 'var(--ds-weight-semibold)',
      cursor: canSave && !saving ? 'pointer' : 'default'
    }
  }, saving ? '…' : saveTitle));
}
Object.assign(__ds_scope, { SheetActionBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/sheets/SheetActionBar.jsx", error: String((e && e.message) || e) }); }

// components/sheets/SheetCard.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function SheetCard({
  title,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.GlassCard, _extends({}, rest, {
    style: style
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'stretch',
      gap: 14
    }
  }, title ? /*#__PURE__*/React.createElement(__ds_scope.SectionHeader, null, title) : null, children));
}
Object.assign(__ds_scope, { SheetCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/sheets/SheetCard.jsx", error: String((e && e.message) || e) }); }

// components/sheets/SheetField.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function SheetField({
  label,
  value,
  valueColor,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8,
      ...style
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-medium)',
      color: 'var(--ds-label)'
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), value != null ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: valueColor || 'var(--ds-label-2)',
      fontVariantNumeric: 'tabular-nums'
    }
  }, value) : null), children);
}
Object.assign(__ds_scope, { SheetField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/sheets/SheetField.jsx", error: String((e && e.message) || e) }); }

// components/sheets/SheetHeader.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function SheetHeader({
  title,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      alignItems: 'center',
      padding: '18px 16px 6px',
      fontSize: 'var(--ds-text-headline)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-label)',
      ...style
    }
  }), title);
}
Object.assign(__ds_scope, { SheetHeader });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/sheets/SheetHeader.jsx", error: String((e && e.message) || e) }); }

// components/sheets/Slider.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Slider({
  value = 0,
  min = 0,
  max = 10,
  step = 1,
  onChange,
  style,
  ...rest
}) {
  const pct = max > min ? (value - min) / (max - min) * 100 : 0;
  return /*#__PURE__*/React.createElement("input", _extends({}, rest, {
    type: "range",
    min: min,
    max: max,
    step: step,
    value: value,
    onChange: e => onChange && onChange(Number(e.target.value)),
    style: {
      width: '100%',
      height: 28,
      margin: 0,
      appearance: 'none',
      background: 'transparent',
      cursor: 'pointer',
      '--pct': pct + '%',
      backgroundImage: 'linear-gradient(to right, var(--ds-accent) 0 ' + pct + '%, var(--ds-fill-strong) ' + pct + '% 100%)',
      backgroundSize: '100% 4px',
      backgroundRepeat: 'no-repeat',
      backgroundPosition: 'center',
      borderRadius: 999,
      ...style
    }
  }));
}
Object.assign(__ds_scope, { Slider });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/sheets/Slider.jsx", error: String((e && e.message) || e) }); }

// components/sheets/Stepper.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Stepper({
  label,
  value = 1,
  min = 1,
  max = 7,
  onChange,
  style,
  ...rest
}) {
  const btn = (delta, icon, disabled) => /*#__PURE__*/React.createElement("button", {
    type: "button",
    disabled: disabled,
    onClick: () => onChange && onChange(Math.max(min, Math.min(max, value + delta))),
    style: {
      width: 40,
      height: 30,
      border: 'none',
      cursor: disabled ? 'default' : 'pointer',
      background: 'transparent',
      color: disabled ? 'var(--ds-label-3)' : 'var(--ds-label)',
      display: 'grid',
      placeItems: 'center'
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Icon, {
    name: icon,
    size: 15
  }));
  return /*#__PURE__*/React.createElement("div", _extends({}, rest, {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      minHeight: 'var(--ds-touch-target)',
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0,
      fontSize: 'var(--ds-text-body)',
      color: 'var(--ds-label)',
      fontVariantNumeric: 'tabular-nums'
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      borderRadius: 'var(--ds-radius-field)',
      background: 'var(--ds-fill-strong)',
      overflow: 'hidden'
    }
  }, btn(-1, 'minus', value <= min), /*#__PURE__*/React.createElement("span", {
    style: {
      width: '.5px',
      height: 18,
      background: 'var(--ds-label-3)'
    }
  }), btn(1, 'plus', value >= max)));
}
Object.assign(__ds_scope, { Stepper });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/sheets/Stepper.jsx", error: String((e && e.message) || e) }); }

// components/sheets/TextField.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function TextField({
  value,
  onChange,
  placeholder,
  prefix,
  prefixColor,
  multiline,
  rows = 2,
  align,
  width,
  style,
  ...rest
}) {
  const shell = {
    width: width || '100%',
    boxSizing: 'border-box',
    background: 'color-mix(in srgb, var(--ds-fill) 50%, transparent)',
    borderRadius: 'var(--ds-radius-field)',
    padding: '10px 12px',
    display: 'flex',
    alignItems: 'center',
    gap: 6
  };
  const input = {
    flex: 1,
    minWidth: 0,
    background: 'transparent',
    border: 'none',
    outline: 'none',
    fontFamily: 'inherit',
    fontSize: 'var(--ds-text-body)',
    color: 'var(--ds-label)',
    textAlign: align || 'left',
    resize: 'none'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      ...shell,
      ...style
    }
  }, prefix ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: prefixColor || 'var(--ds-gold)'
    }
  }, prefix) : null, multiline ? /*#__PURE__*/React.createElement("textarea", _extends({}, rest, {
    rows: rows,
    value: value,
    placeholder: placeholder,
    onChange: e => onChange && onChange(e.target.value),
    style: input
  })) : /*#__PURE__*/React.createElement("input", _extends({}, rest, {
    value: value,
    placeholder: placeholder,
    onChange: e => onChange && onChange(e.target.value),
    style: input
  })));
}
Object.assign(__ds_scope, { TextField });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/sheets/TextField.jsx", error: String((e && e.message) || e) }); }

// components/sheets/ToggleRow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function ToggleRow({
  label,
  description,
  checked,
  onChange,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("label", _extends({}, rest, {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      cursor: 'pointer',
      minHeight: 'var(--ds-touch-target)',
      ...style
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      color: 'var(--ds-label)'
    }
  }, label), description ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, description) : null), /*#__PURE__*/React.createElement("span", {
    onClick: e => {
      e.preventDefault();
      onChange && onChange(!checked);
    },
    style: {
      width: 51,
      height: 31,
      flex: '0 0 auto',
      borderRadius: 999,
      background: checked ? 'var(--ds-success)' : 'var(--ds-fill-strong)',
      transition: 'background var(--ds-ease-in-out)',
      position: 'relative'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      top: 2,
      left: checked ? 22 : 2,
      width: 27,
      height: 27,
      borderRadius: '50%',
      background: '#fff',
      boxShadow: '0 2px 5px rgb(0 0 0 / .18)',
      transition: 'left var(--ds-ease-in-out)'
    }
  })));
}
Object.assign(__ds_scope, { ToggleRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/sheets/ToggleRow.jsx", error: String((e && e.message) || e) }); }

// ui_kits/kid_app/Celebration.jsx
try { (() => {
// Celebration.jsx — three-tier celebration ladder (IMPLEMENTATION.md §1.3, tier 3)
// Canvas particle system: gravity + drag, varied geometry, depth via scale/blur/parallax,
// theme tint + money-gold, radial bloom, rising chime.
// Exports to window: CelebrationLayer, CELEB_PRESETS, celebSwiftConfig
// Imperative API installed on window.DBCelebrate: { perfect(), threshold(from,toEl), pop(point) }
//
// Perf notes (learned the hard way — do not undo):
//   * Canvas2D applies ctx.filter PER DRAW CALL, so blurring 1200 particles individually
//     costs ~1 fps. Each depth band renders unblurred into its own offscreen canvas, then
//     the three bands are composited with one filtered draw each: 3 blurred draws/frame.
//   * Particles come from a fixed pre-allocated pool. Allocating 1200 objects inside the
//     fire handler cost a 320ms frame on its own.

const MAX_PARTICLES = 2600;
const MIX = {
  balanced: {
    coin: 3,
    ribbon: 3,
    star: 2,
    spark: 2
  },
  coinHeavy: {
    coin: 6,
    ribbon: 2,
    star: 1,
    spark: 1
  },
  ribbons: {
    coin: 1,
    ribbon: 7,
    star: 1,
    spark: 1
  },
  sparks: {
    coin: 1,
    ribbon: 1,
    star: 1,
    spark: 7
  }
};
const CELEB_PRESETS = {
  full: {
    count: 1200,
    duration: 2500,
    gravity: 1150,
    drag: 0.55,
    burstVelocity: 900,
    spin: 7,
    emitter: 'both',
    mix: 'balanced',
    depthBlur: 3.5,
    bloom: 0.9,
    wobble: 34,
    chime: true
  },
  modest: {
    count: 260,
    duration: 1400,
    gravity: 1300,
    drag: 0.8,
    burstVelocity: 520,
    spin: 5,
    emitter: 'rain',
    mix: 'coinHeavy',
    depthBlur: 1.5,
    bloom: 0.35,
    wobble: 18,
    chime: false
  },
  off: {
    count: 0,
    duration: 0,
    gravity: 0,
    drag: 0,
    burstVelocity: 0,
    spin: 0,
    emitter: 'rain',
    mix: 'balanced',
    depthBlur: 0,
    bloom: 0,
    wobble: 0,
    chime: false
  }
};
function celebSwiftConfig(t) {
  const f = n => (Math.round(n * 100) / 100).toFixed(2);
  return `// Paste into DailyBreadKit — drives the tier-3 particle system.
struct CelebrationConfig {
    var particleCount: Int      = ${t.count}
    var duration: TimeInterval  = ${f(t.duration / 1000)}
    var gravity: CGFloat        = ${f(t.gravity)}     // points/s²
    var drag: CGFloat           = ${f(t.drag)}     // velocity damping per second
    var burstVelocity: CGFloat  = ${f(t.burstVelocity)}     // radial impulse from the bloom origin
    var spin: CGFloat           = ${f(t.spin)}     // radians/s, randomised ±40%
    var emitter: Emitter        = .${t.emitter}
    var geometryMix: Mix        = .${t.mix}
    var depthBlur: CGFloat      = ${f(t.depthBlur)}     // max blur on the farthest layer
    var bloomStrength: CGFloat  = ${f(t.bloom)}
    var wobble: CGFloat         = ${f(t.wobble)}     // lateral drift amplitude
    var playsChime: Bool        = ${t.chime}

    // Invariant: money-gold always participates, whatever the theme.
    static let goldDeep  = Color(hex: "#C98A1E")
    static let goldLight = Color(hex: "#E7B44A")
}
// Render depth bands into three separate layers and blur each layer once — never per particle.
// Respect Reduce Motion regardless of this config, and honour enableConfetti.`;
}
const clamp = (v, a, b) => Math.max(a, Math.min(b, v));
const rand = (a, b) => a + Math.random() * (b - a);
function readVar(name, fb) {
  const v = getComputedStyle(document.documentElement).getPropertyValue(name).trim();
  return v || fb;
}
function pickWeighted(mix) {
  const keys = Object.keys(mix);
  let total = 0;
  for (const k of keys) total += mix[k];
  let r = Math.random() * total;
  for (const k of keys) {
    r -= mix[k];
    if (r <= 0) return k;
  }
  return keys[0];
}
function chime(rising) {
  try {
    const Ctx = window.AudioContext || window.webkitAudioContext;
    if (!Ctx) return;
    const ac = new Ctx();
    const notes = rising ? [523.25, 659.25, 783.99, 1046.5] : [783.99, 1046.5];
    notes.forEach((f, i) => {
      const o = ac.createOscillator(),
        g = ac.createGain();
      o.type = 'triangle';
      o.frequency.value = f;
      const t0 = ac.currentTime + i * 0.085;
      g.gain.setValueAtTime(0, t0);
      g.gain.linearRampToValueAtTime(0.16, t0 + 0.012);
      g.gain.exponentialRampToValueAtTime(0.0001, t0 + 0.55);
      o.connect(g);
      g.connect(ac.destination);
      o.start(t0);
      o.stop(t0 + 0.6);
    });
    setTimeout(() => ac.close(), 1400);
  } catch (e) {/* audio is a bonus, never a failure */}
}
function pulse(el) {
  el.animate([{
    transform: 'scale(1)'
  }, {
    transform: 'scale(1.28)'
  }, {
    transform: 'scale(1)'
  }], {
    duration: 460,
    easing: 'cubic-bezier(.2,.9,.2,1)'
  });
}
const BANDS = [[0, 0.34], [0.34, 0.68], [0.68, 1.01]];
function CelebrationLayer({
  cfg
}) {
  const canvasRef = React.useRef(null);
  const cfgRef = React.useRef(cfg);
  cfgRef.current = cfg;
  React.useEffect(() => {
    const canvas = canvasRef.current,
      ctx = canvas.getContext('2d');
    let w = 0,
      h = 0;
    const dpr = Math.min(window.devicePixelRatio || 1, 2);

    // fixed particle pool — never grows, never allocates during a burst
    const pool = new Array(MAX_PARTICLES);
    for (let i = 0; i < MAX_PARTICLES; i++) {
      pool[i] = {
        alive: false,
        x: 0,
        y: 0,
        vx: 0,
        vy: 0,
        depth: 0,
        shape: 'coin',
        size: 6,
        r: 0,
        vr: 0,
        col: '#E7B44A',
        wob: 0,
        wobf: 1,
        life: 1000,
        age: 0
      };
    }
    let cursor = 0,
      alive = 0;
    const blooms = [],
      rings = [],
      coins = [];
    let raf = 0,
      last = 0;

    // one offscreen canvas per depth band
    const bandCanvas = BANDS.map(() => document.createElement('canvas'));
    const bandCtx = bandCanvas.map(c => c.getContext('2d'));
    const resize = () => {
      const r = canvas.parentElement.getBoundingClientRect();
      w = Math.max(1, r.width);
      h = Math.max(1, r.height);
      canvas.width = Math.round(w * dpr);
      canvas.height = Math.round(h * dpr);
      canvas.style.width = w + 'px';
      canvas.style.height = h + 'px';
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
      bandCanvas.forEach((c, i) => {
        c.width = canvas.width;
        c.height = canvas.height;
        bandCtx[i].setTransform(dpr, 0, 0, dpr, 0, 0);
      });
    };
    resize();
    const ro = new ResizeObserver(resize);
    ro.observe(canvas.parentElement);
    const palette = () => {
      const t = cfgRef.current;
      const gold = [readVar('--ds-gold', '#E7B44A'), '#C98A1E', '#F0C868'];
      if (!t.themeTint) return gold;
      return gold.concat([readVar('--ds-accent', '#3B82D6'), readVar('--ds-success', '#2E9E63'), readVar('--db-level-4', '#7A5AF8')]);
    };

    // claim a slot from the pool; oldest slot wins if the pool is saturated
    const claim = () => {
      const p = pool[cursor];
      cursor = (cursor + 1) % MAX_PARTICLES;
      if (!p.alive) alive++;
      p.alive = true;
      return p;
    };
    const emitSpark = (x, y, col, vx, vy, size, life) => {
      const p = claim();
      p.x = x;
      p.y = y;
      p.vx = vx;
      p.vy = vy;
      p.depth = 0.88;
      p.shape = 'spark';
      p.size = size;
      p.r = 0;
      p.vr = 0;
      p.col = col;
      p.wob = 0;
      p.wobf = 1;
      p.life = life;
      p.age = 0;
    };
    const spawn = (n, origin) => {
      const t = cfgRef.current,
        mix = MIX[t.mix] || MIX.balanced,
        cols = palette();
      const capped = Math.min(n, MAX_PARTICLES);
      for (let i = 0; i < capped; i++) {
        const p = claim();
        p.shape = pickWeighted(mix);
        const fromBurst = origin && (t.emitter === 'burst' || t.emitter === 'both' && Math.random() < 0.55);
        if (fromBurst) {
          const a = rand(0, Math.PI * 2),
            sp = t.burstVelocity * rand(0.35, 1);
          p.x = origin.x + rand(-8, 8);
          p.y = origin.y + rand(-8, 8);
          p.vx = Math.cos(a) * sp;
          p.vy = Math.sin(a) * sp - t.burstVelocity * 0.35;
        } else {
          p.x = rand(-0.05, 1.05) * w;
          p.y = rand(-0.35, -0.02) * h;
          p.vx = rand(-70, 70);
          p.vy = rand(40, 220);
        }
        p.depth = Math.random();
        p.size = (p.shape === 'ribbon' ? rand(5, 9) : rand(4.5, 9)) * (0.55 + p.depth * 0.95);
        p.r = rand(0, Math.PI * 2);
        p.vr = rand(-1, 1) * t.spin * rand(0.6, 1.4);
        p.col = cols[Math.random() * cols.length | 0];
        p.wob = rand(0, Math.PI * 2);
        p.wobf = rand(0.8, 2.4);
        p.life = t.duration * rand(0.7, 1.15);
        p.age = 0;
      }
    };
    const drawShape = (c, p) => {
      const s = p.size;
      c.fillStyle = p.col;
      if (p.shape === 'coin') {
        const sx = Math.max(0.12, Math.abs(Math.cos(p.r)));
        c.save();
        c.scale(sx, 1);
        c.beginPath();
        c.arc(0, 0, s, 0, Math.PI * 2);
        c.fill();
        c.globalAlpha *= 0.45;
        c.fillStyle = '#FFF6DC';
        c.beginPath();
        c.arc(-s * 0.22, -s * 0.24, s * 0.42, 0, Math.PI * 2);
        c.fill();
        c.restore();
      } else if (p.shape === 'ribbon') {
        const sx = Math.max(0.1, Math.abs(Math.cos(p.r * 0.7)));
        c.save();
        c.scale(sx, 1);
        const rw = s * 0.62,
          rh = s * 2.6;
        c.beginPath();
        c.roundRect(-rw / 2, -rh / 2, rw, rh, rw * 0.5);
        c.fill();
        c.restore();
      } else if (p.shape === 'star') {
        c.beginPath();
        for (let i = 0; i < 10; i++) {
          const rr = i % 2 ? s * 0.46 : s * 1.05,
            a = i / 10 * Math.PI * 2 - Math.PI / 2;
          const px = Math.cos(a) * rr,
            py = Math.sin(a) * rr;
          i ? c.lineTo(px, py) : c.moveTo(px, py);
        }
        c.closePath();
        c.fill();
      } else {
        c.beginPath();
        c.arc(0, 0, s * 1.1, 0, Math.PI * 2);
        c.fill();
      }
    };
    const tick = now => {
      const t = cfgRef.current;
      const dt = Math.min(0.05, (now - (last || now)) / 1000);
      last = now;
      ctx.clearRect(0, 0, w, h);

      // radial bloom behind everything
      for (let i = blooms.length - 1; i >= 0; i--) {
        const b = blooms[i];
        b.age += dt;
        const k = b.age / b.life;
        if (k >= 1) {
          blooms.splice(i, 1);
          continue;
        }
        const rr = (0.15 + k * 0.85) * Math.max(w, h) * 0.85;
        const g = ctx.createRadialGradient(b.x, b.y, 0, b.x, b.y, rr);
        const a = (1 - k) * 0.5 * t.bloom;
        g.addColorStop(0, `rgba(255,232,168,${a})`);
        g.addColorStop(0.55, `rgba(255,206,102,${a * 0.35})`);
        g.addColorStop(1, 'rgba(255,206,102,0)');
        ctx.fillStyle = g;
        ctx.fillRect(0, 0, w, h);
      }
      if (alive) {
        // 1. draw each band unblurred into its own layer
        for (let bi = 0; bi < BANDS.length; bi++) bandCtx[bi].clearRect(0, 0, w, h);
        for (let i = 0; i < MAX_PARTICLES; i++) {
          const p = pool[i];
          if (!p.alive) continue;
          const bi = p.depth < 0.34 ? 0 : p.depth < 0.68 ? 1 : 2;
          const c = bandCtx[bi];
          c.save();
          c.globalAlpha = clamp(0.45 + p.depth * 0.55, 0, 1) * clamp(1 - p.age / p.life, 0, 1) ** 0.6;
          c.translate(p.x, p.y);
          c.rotate(p.r);
          drawShape(c, p);
          c.restore();
        }
        // 2. composite the three layers — one filtered draw each, far to near
        for (let bi = 0; bi < BANDS.length; bi++) {
          const blur = t.depthBlur * (1 - bi / 2);
          ctx.filter = blur > 0.05 ? `blur(${blur.toFixed(2)}px)` : 'none';
          ctx.drawImage(bandCanvas[bi], 0, 0, w, h);
        }
        ctx.filter = 'none';
      }

      // integrate
      for (let i = 0; i < MAX_PARTICLES; i++) {
        const p = pool[i];
        if (!p.alive) continue;
        p.age += dt * 1000;
        const par = 0.55 + p.depth * 0.75; // parallax by depth
        p.wob += dt * p.wobf;
        p.vy += t.gravity * par * dt;
        p.vx += Math.sin(p.wob) * t.wobble * dt;
        const d = Math.exp(-t.drag * dt);
        p.vx *= d;
        p.vy *= d;
        p.x += p.vx * dt;
        p.y += p.vy * dt;
        p.r += p.vr * dt;
        if (p.age > p.life || p.y > h + 80) {
          p.alive = false;
          alive--;
        }
      }

      // tier-1 rings
      for (let i = rings.length - 1; i >= 0; i--) {
        const g2 = rings[i];
        g2.age += dt;
        const k = g2.age / g2.life;
        if (k >= 1) {
          rings.splice(i, 1);
          continue;
        }
        ctx.save();
        ctx.globalAlpha = (1 - k) * 0.9;
        ctx.strokeStyle = g2.col;
        ctx.lineWidth = 2.5 * (1 - k * 0.6);
        ctx.beginPath();
        ctx.arc(g2.x, g2.y, 14 + k * 22, 0, Math.PI * 2);
        ctx.stroke();
        ctx.restore();
      }

      // tier-2 coin arcs
      for (let i = coins.length - 1; i >= 0; i--) {
        const c = coins[i];
        c.age += dt;
        const k = clamp(c.age / c.life, 0, 1);
        const e = 1 - Math.pow(1 - k, 2.2);
        const mx = (c.x0 + c.x1) / 2,
          my = Math.min(c.y0, c.y1) - 130;
        const x = (1 - e) ** 2 * c.x0 + 2 * (1 - e) * e * mx + e * e * c.x1;
        const y = (1 - e) ** 2 * c.y0 + 2 * (1 - e) * e * my + e * e * c.y1;
        const gold = readVar('--ds-gold', '#E7B44A');
        ctx.save();
        ctx.globalAlpha = k > 0.88 ? (1 - k) / 0.12 : 1;
        ctx.translate(x, y);
        ctx.rotate(c.age * 14);
        drawShape(ctx, {
          shape: 'coin',
          size: 11,
          r: c.age * 14,
          col: gold
        });
        ctx.restore();
        if (k >= 1) {
          coins.splice(i, 1);
          if (c.onArrive) c.onArrive();
          for (let j = 0; j < 10; j++) {
            emitSpark(c.x1, c.y1, gold, rand(-160, 160), rand(-200, 40), rand(2.5, 4.5), 520);
          }
        }
      }
      const busy = alive || blooms.length || rings.length || coins.length;
      raf = busy ? requestAnimationFrame(tick) : 0;
    };
    const start = () => {
      if (!raf) {
        last = 0;
        raf = requestAnimationFrame(tick);
      }
    };
    const reduced = () => cfgRef.current.reduceMotion || window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    window.DBCelebrate = {
      perfect() {
        const t = cfgRef.current;
        if (t.tier === 'off') return;
        if (reduced()) {
          blooms.push({
            x: w / 2,
            y: h * 0.42,
            age: 0,
            life: 0.7
          });
          start();
          return;
        }
        blooms.push({
          x: w / 2,
          y: h * 0.42,
          age: 0,
          life: t.duration / 1000 * 0.45
        });
        spawn(t.count, {
          x: w / 2,
          y: h * 0.42
        });
        if (t.chime) chime(true);
        start();
      },
      threshold(from, toEl) {
        const t = cfgRef.current;
        if (t.tier === 'off') return;
        const box = canvas.parentElement.getBoundingClientRect();
        const target = toEl && toEl.getBoundingClientRect();
        const x1 = target ? target.left + target.width / 2 - box.left : w - 40;
        const y1 = target ? target.top + target.height / 2 - box.top : 40;
        if (reduced()) {
          if (toEl) pulse(toEl);
          return;
        }
        coins.push({
          x0: from.x,
          y0: from.y,
          x1,
          y1,
          age: 0,
          life: 0.62,
          onArrive: () => {
            if (toEl) pulse(toEl);
          }
        });
        if (t.chime) chime(false);
        start();
      },
      pop(pt, gold) {
        const t = cfgRef.current;
        if (t.tier === 'off' || reduced()) return;
        const col = gold ? readVar('--ds-gold', '#E7B44A') : readVar('--ds-accent', '#3B82D6');
        rings.push({
          x: pt.x,
          y: pt.y,
          age: 0,
          life: 0.34,
          col
        });
        if (t.tier === 'full') {
          for (let i = 0; i < 8; i++) {
            const a = rand(0, Math.PI * 2);
            emitSpark(pt.x, pt.y, col, Math.cos(a) * rand(60, 190), Math.sin(a) * rand(60, 190) - 60, rand(2, 3.6), 460);
          }
        }
        start();
      },
      // exposed for perf measurement
      _stats() {
        return {
          alive,
          pool: MAX_PARTICLES
        };
      }
    };
    return () => {
      ro.disconnect();
      cancelAnimationFrame(raf);
      raf = 0;
      delete window.DBCelebrate;
    };
  }, []);
  return /*#__PURE__*/React.createElement("canvas", {
    ref: canvasRef,
    style: {
      position: 'absolute',
      inset: 0,
      pointerEvents: 'none',
      zIndex: 400
    }
  });
}
Object.assign(window, {
  CelebrationLayer,
  CELEB_PRESETS,
  celebSwiftConfig,
  DB_CELEB_MIX: MIX
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/kid_app/Celebration.jsx", error: String((e && e.message) || e) }); }

// ui_kits/kid_app/KidScreens.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const DS = window.DailyBreadDesignSystem_2a4964;
const {
  GlassCard,
  SectionHeader,
  Chip,
  Tag,
  StatTile,
  ProgressRing,
  ProgressBar,
  Button,
  EmptyState,
  InlineError,
  Icon,
  ChoreRow,
  AtRiskCard,
  ScreenTimeCard,
  YearHeatmapCard,
  LevelBadge,
  XPBar,
  TierFor,
  AchievementCard,
  BalanceHero,
  GoalCard,
  TransactionRow,
  ThemeRow,
  DB_THEMES,
  ToggleRow,
  SheetHeader,
  SheetCard,
  SheetField,
  SheetActionBar,
  TextField
} = DS;
const money = n => '$' + n.toFixed(2);

// ================= KID HOME =================
function KidHome({
  state,
  onDone,
  onHelp,
  onNav
}) {
  const {
    chores,
    balance,
    streak,
    earnedToday,
    goal,
    badges,
    name
  } = state;
  const total = chores.length;
  const done = chores.filter(c => c.done).length;
  const pending = chores.filter(c => !c.done && !c.help).length;
  const pct = total === 0 ? 0 : Math.round(done / total * 100);
  const tier = TierFor(pct);
  const nextUp = chores.find(c => !c.done && !c.help) || chores.find(c => c.help);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-title2)',
      fontWeight: 'var(--ds-weight-bold)'
    }
  }, "Good evening, ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ds-accent)'
    }
  }, name)), /*#__PURE__*/React.createElement("div", {
    style: {
      borderRadius: 'var(--ds-radius-hero)',
      padding: 20,
      background: 'linear-gradient(to bottom, color-mix(in srgb, ' + tier.color + ' 20%, var(--ds-card)), color-mix(in srgb, ' + tier.color + ' 2%, var(--ds-card)))',
      boxShadow: 'var(--ds-card-shadow), inset 0 0 0 1px color-mix(in srgb, ' + tier.color + ' 28%, transparent)',
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement(LevelBadge, {
    percent: pct,
    style: {
      alignSelf: 'center'
    }
  }), /*#__PURE__*/React.createElement(XPBar, {
    done: done,
    total: total
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)',
      textAlign: 'center'
    }
  }, total === 0 ? '😎 Rest day — no quests' : pending === 0 ? '🎉 Every quest done today!' : pending + ' quest' + (pending === 1 ? '' : 's') + ' remaining'), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 1,
      background: 'color-mix(in srgb, var(--ds-label) 8%, transparent)'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 18,
      fontSize: 'var(--ds-text-subheadline)',
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 5
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--ds-font-emoji)'
    }
  }, "\uD83D\uDCB0"), /*#__PURE__*/React.createElement("b", {
    "data-coin-target": true,
    style: {
      color: 'var(--ds-gold)'
    }
  }, money(balance))), streak > 0 ? /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 5
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--ds-font-emoji)'
    }
  }, "\uD83D\uDD25"), /*#__PURE__*/React.createElement("b", {
    style: {
      color: 'var(--db-streak)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, streak, " day streak")) : null), earnedToday > 0 ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 4,
      fontSize: 'var(--ds-text-subheadline)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--ds-font-emoji)'
    }
  }, "\u2728"), /*#__PURE__*/React.createElement("b", {
    style: {
      color: 'var(--ds-gold)',
      fontWeight: 'var(--ds-weight-heavy)'
    }
  }, "+", money(earnedToday)), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ds-label-2)'
    }
  }, "today")) : null), /*#__PURE__*/React.createElement(GoalCard, {
    name: goal.name,
    current: money(balance),
    target: money(goal.target),
    percent: Math.min(100, Math.round(balance / goal.target * 100)),
    onTap: () => onNav('earnings')
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 12,
      alignItems: 'stretch'
    }
  }, /*#__PURE__*/React.createElement(GlassCard, {
    style: {
      flex: 1
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 5
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "trophy",
    size: 12,
    color: "var(--ds-label-2)"
  }), /*#__PURE__*/React.createElement(SectionHeader, null, "Latest")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 20,
      fontFamily: 'var(--ds-font-emoji)'
    }
  }, "\uD83D\uDD25"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, "Seven days")), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      fontWeight: 'var(--ds-weight-bold)',
      color: 'var(--ds-gold)'
    }
  }, "+50 pts"))), /*#__PURE__*/React.createElement(GlassCard, {
    padding: "var(--ds-card-padding-tight)",
    style: {
      minWidth: 96,
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement("div", {
    onClick: () => onNav('awards'),
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-title)',
      fontWeight: 'var(--ds-weight-heavy)',
      color: 'var(--ds-accent)',
      lineHeight: 1
    }
  }, badges.earned), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-label-2)'
    }
  }, "/ ", badges.total)), /*#__PURE__*/React.createElement(SectionHeader, {
    kerning: "eyebrow",
    style: {
      fontSize: 'var(--ds-text-caption2)'
    }
  }, "Badges")))), nextUp ? /*#__PURE__*/React.createElement(GlassCard, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(SectionHeader, {
    kerning: "eyebrow",
    color: "var(--ds-accent)"
  }, "Next up"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 'var(--ds-icon-tile-lg)',
      height: 'var(--ds-icon-tile-lg)',
      flex: '0 0 auto',
      borderRadius: 'var(--ds-radius-icon-tile)',
      background: 'var(--ds-fill)',
      display: 'grid',
      placeItems: 'center',
      fontSize: 22,
      fontFamily: 'var(--ds-font-emoji)'
    }
  }, nextUp.icon), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, nextUp.name), nextUp.help ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-help)'
    }
  }, "Help raised \u2014 waiting on a grown-up") : nextUp.weekly ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, nextUp.weekly.done, " of ", nextUp.weekly.target, " this week") : nextUp.value ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-gold)'
    }
  }, money(nextUp.value)) : null)), !nextUp.help ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "capsuleHelp",
    icon: "questionmark.circle",
    style: {
      flex: 1
    },
    onClick: () => onHelp(nextUp.id)
  }, "Help"), /*#__PURE__*/React.createElement(Button, {
    variant: "capsuleAccent",
    icon: "checkmark.circle.fill",
    style: {
      flex: 1
    },
    onClick: () => onDone(nextUp.id)
  }, "Done")) : null)) : total > 0 ? /*#__PURE__*/React.createElement(GlassCard, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: 8,
      padding: '8px 0'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 40,
      fontFamily: 'var(--ds-font-emoji)'
    }
  }, "\uD83C\uDF89"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-headline)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-success)'
    }
  }, "Every quest done today!"))) : null);
}

// ================= TODAY =================
function KidToday({
  state,
  onDone,
  onHelp
}) {
  const {
    chores,
    balance,
    streak,
    earnedToday,
    name
  } = state;
  const total = chores.length;
  const done = chores.filter(c => c.done).length;
  const atRisk = chores.filter(c => !c.done && !c.help).slice(0, 3).map((c, i) => ({
    name: c.name,
    detail: i === 0 ? 'Due tonight' : c.weekly ? c.weekly.done + ' of ' + c.weekly.target + ' — getting tight' : 'Must happen every day',
    urgency: i === 0 ? 'DueTonight' : c.weekly ? 'GettingTight' : 'MustDoDaily',
    money: c.value ? money(c.value) : undefined,
    minutes: c.minutes
  }));
  const totalMin = atRisk.reduce((s, i) => s + (i.minutes || 0), 0);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement(GlassCard, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(ProgressRing, {
    progress: total ? done / total : 0,
    label: done + '/' + total
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-headline)'
    }
  }, "Good evening, ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ds-accent)'
    }
  }, name), "."), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      color: 'var(--ds-label-2)'
    }
  }, "Friday, July 24, 2026"), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 6,
      fontSize: 'var(--ds-text-subheadline)',
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("b", {
    style: {
      color: 'var(--ds-gold)'
    }
  }, money(earnedToday)), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ds-label-2)'
    }
  }, "today"), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ds-label-3)'
    }
  }, "\xB7"), /*#__PURE__*/React.createElement("b", {
    "data-coin-target": true,
    style: {
      color: 'var(--ds-gold)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, money(balance)), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ds-label-2)'
    }
  }, "saved")), streak > 1 ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      fontWeight: 'var(--ds-weight-bold)',
      color: 'var(--ds-gold)',
      paddingTop: 1
    }
  }, "\uD83D\uDD25 ", streak, "-day streak") : null))), /*#__PURE__*/React.createElement(AtRiskCard, {
    items: atRisk,
    totalMinutes: totalMin,
    totalMoney: atRisk.some(i => i.money) ? money(atRisk.reduce((s, i) => s + (i.money ? parseFloat(i.money.slice(1)) : 0), 0)) : undefined,
    previewLine: "Trash goes out tomorrow."
  }), /*#__PURE__*/React.createElement(GlassCard, null, chores.map(c => /*#__PURE__*/React.createElement(ChoreRow, {
    key: c.id,
    icon: c.icon,
    name: c.name,
    value: c.value ? money(c.value) : undefined,
    done: c.done,
    help: c.help,
    approvedBy: c.approvedBy,
    weekly: c.weekly,
    onToggle: () => onDone(c.id),
    onHelp: () => onHelp(c.id)
  }))), total > 0 && done === total ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      gap: 7,
      padding: '13px 16px',
      borderRadius: 'var(--ds-radius-card)',
      background: 'color-mix(in srgb, var(--ds-gold) 10%, transparent)',
      color: 'var(--ds-gold)',
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "party.popper",
    size: 15
  }), "Day complete \u2014 every chore done \u2728") : null, /*#__PURE__*/React.createElement(ScreenTimeCard, {
    weekday: {
      minutes: '2h 30m',
      floor: '1h 30m',
      atRisk: 60
    },
    weekend: {
      minutes: '4h',
      floor: '3h',
      atRisk: 60
    },
    prices: [{
      name: 'Take out the trash',
      minutes: 15
    }, {
      name: 'Dishes',
      minutes: 20
    }, {
      name: 'Mow the lawn',
      minutes: 30
    }],
    onHistory: () => {}
  }), /*#__PURE__*/React.createElement(YearHeatmapCard, {
    title: "Your year"
  }));
}

// ================= EARNINGS =================
function KidEarnings({
  state,
  onNav
}) {
  const {
    balance,
    goal
  } = state;
  const bars = [3, 0, 6, 2, 8, 4, 5, 0, 7, 3, 9, 2, 6, 4];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement(BalanceHero, {
    balance: money(balance)
  }), /*#__PURE__*/React.createElement(GlassCard, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(SectionHeader, {
    kerning: "eyebrow"
  }, "Last 14 days"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-end',
      gap: 6,
      height: 120
    }
  }, bars.map((v, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      flex: 1,
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'flex-end',
      alignItems: 'center',
      gap: 5
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      height: Math.max(3, v * 12),
      background: 'var(--ds-gold)',
      borderRadius: 3
    }
  }), i % 3 === 0 ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption2)',
      color: 'var(--ds-label-3)'
    }
  }, 'SMTWTFS'[i % 7]) : /*#__PURE__*/React.createElement("span", {
    style: {
      height: 12
    }
  })))))), /*#__PURE__*/React.createElement(SectionHeader, null, "Goals"), /*#__PURE__*/React.createElement(GlassCard, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, goal.name), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      color: 'var(--ds-label-2)'
    }
  }, money(balance), " of ", money(goal.target))), /*#__PURE__*/React.createElement(ProgressBar, {
    value: Math.min(100, balance / goal.target * 100),
    total: 100
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, Math.round(balance / goal.target * 100), "% there"), /*#__PURE__*/React.createElement("div", {
    style: {
      height: '.5px',
      background: 'var(--ds-hairline)'
    }
  }), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => {},
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      fontFamily: 'inherit',
      padding: '6px 0',
      fontSize: 'var(--ds-text-body)',
      color: 'var(--ds-label)',
      minHeight: 44
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "target",
    size: 16,
    color: "var(--ds-accent)"
  }), "Manage goals", /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement(Icon, {
    name: "chevron.right",
    size: 12,
    color: "var(--ds-label-3)"
  })))), /*#__PURE__*/React.createElement(SectionHeader, null, "Recent"), /*#__PURE__*/React.createElement(GlassCard, null, /*#__PURE__*/React.createElement(TransactionRow, {
    description: "Dishes",
    date: "Jul 24",
    amount: "+$2.00"
  }), /*#__PURE__*/React.createElement(TransactionRow, {
    description: "Take out the trash",
    date: "Jul 24",
    amount: "+$1.50"
  }), /*#__PURE__*/React.createElement(TransactionRow, {
    description: "Read 20 minutes",
    date: "Jul 23",
    amount: "+$1.00"
  }), /*#__PURE__*/React.createElement(TransactionRow, {
    description: "Missed: Brush teeth",
    date: "Jul 22",
    amount: "\u2212$0.50",
    negative: true
  }), /*#__PURE__*/React.createElement(TransactionRow, {
    description: "Cash out",
    date: "Jul 20",
    amount: "\u2212$25.00",
    negative: true
  })));
}

// ================= AWARDS =================
const BADGES = [{
  icon: '🌱',
  name: 'First chore',
  description: 'Do your very first one',
  points: 10,
  rarity: 'common',
  earned: true
}, {
  icon: '👣',
  name: 'Three in a row',
  description: 'Three days, all done',
  points: 20,
  rarity: 'common',
  earned: true
}, {
  icon: '⚡',
  name: 'Perfect day',
  description: 'Every chore, one day',
  points: 30,
  rarity: 'uncommon',
  earned: true
}, {
  icon: '🔥',
  name: 'Seven days',
  description: 'A full week, every chore',
  points: 50,
  rarity: 'rare',
  earned: true
}, {
  icon: '💰',
  name: 'First dollar',
  description: 'Earn your first dollar',
  points: 15,
  rarity: 'common',
  earned: true
}, {
  icon: '🥇',
  name: 'Twenty-five',
  description: 'Earn $25 all together',
  points: 60,
  rarity: 'rare',
  earned: true
}, {
  icon: '💎',
  name: 'Fifty dollars',
  description: 'Earn $50 all together',
  points: 80,
  rarity: 'epic',
  progress: 48,
  target: 50
}, {
  icon: '👑',
  name: 'Centurion',
  description: 'Earn $100 all together',
  points: 150,
  rarity: 'legendary',
  progress: 48,
  target: 100
}, {
  icon: '🌙',
  name: 'Thirty days',
  description: 'A month without missing',
  points: 200,
  rarity: 'legendary',
  progress: 7,
  target: 30
}];
function KidAwards() {
  const earned = BADGES.filter(b => b.earned);
  const locked = BADGES.filter(b => !b.earned);
  const points = earned.reduce((s, b) => s + b.points, 0);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(StatTile, {
    value: points,
    label: "points",
    color: "var(--ds-gold)"
  }), /*#__PURE__*/React.createElement(StatTile, {
    value: earned.length + '/' + BADGES.length,
    label: "earned",
    color: "var(--ds-accent)"
  })), /*#__PURE__*/React.createElement(SectionHeader, null, "Earned"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 10
    }
  }, earned.map(b => /*#__PURE__*/React.createElement(AchievementCard, _extends({
    key: b.name
  }, b)))), /*#__PURE__*/React.createElement(SectionHeader, null, "Still out there"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 10
    }
  }, locked.map(b => /*#__PURE__*/React.createElement(AchievementCard, _extends({
    key: b.name
  }, b)))));
}

// ================= SETTINGS =================
function AppSettings({
  theme,
  onTheme,
  isParent,
  features,
  onFeature,
  childName,
  onManage
}) {
  const [open, setOpen] = React.useState(false);
  const current = DB_THEMES.find(t => t.key === theme) || DB_THEMES[0];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement(GlassCard, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 'var(--ds-avatar)',
      height: 'var(--ds-avatar)',
      flex: '0 0 auto',
      borderRadius: '50%',
      display: 'grid',
      placeItems: 'center',
      background: 'linear-gradient(135deg, var(--ds-accent), color-mix(in srgb, var(--ds-accent) 60%, transparent))',
      color: '#fff',
      fontSize: 'var(--ds-text-headline)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, (isParent ? 'Shaun' : childName).slice(0, 1).toUpperCase()), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, isParent ? 'shaun' : childName.toLowerCase()), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, isParent ? 'Parent' : 'Child')))), /*#__PURE__*/React.createElement(GlassCard, null, /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => setOpen(o => !o),
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 14,
      width: '100%',
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      fontFamily: 'inherit',
      textAlign: 'left',
      padding: 0,
      minHeight: 44
    }
  }, /*#__PURE__*/React.createElement(DS.ThemeSwatch, {
    theme: current,
    width: 64,
    height: 44
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, "Theme"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, current.name)), /*#__PURE__*/React.createElement(Icon, {
    name: open ? 'chevron.up' : 'chevron.down',
    size: 13,
    color: "var(--ds-label-2)"
  })), open ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 10,
      borderTop: '.5px solid var(--ds-hairline)',
      paddingTop: 4
    }
  }, DB_THEMES.map(t => /*#__PURE__*/React.createElement(ThemeRow, {
    key: t.key,
    theme: t,
    selected: t.key === theme,
    onSelect: k => {
      onTheme(k);
      setOpen(false);
    }
  }))) : null), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)',
      padding: '0 4px',
      lineHeight: 1.5
    }
  }, "Pick the look you like. It changes everywhere, on every device \u2014 switch whenever you feel like a change."), isParent ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(SectionHeader, null, "Family features"), /*#__PURE__*/React.createElement(GlassCard, null, /*#__PURE__*/React.createElement(ToggleRow, {
    label: "Savings goals",
    description: 'Show goals to ' + childName,
    checked: features.goals,
    onChange: v => onFeature('goals', v)
  }), /*#__PURE__*/React.createElement(ToggleRow, {
    label: "Confetti",
    description: "Celebrate completed days",
    checked: features.confetti,
    onChange: v => onFeature('confetti', v)
  }), /*#__PURE__*/React.createElement(ToggleRow, {
    label: "Streaks",
    description: "Show streak counters",
    checked: features.streaks,
    onChange: v => onFeature('streaks', v)
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)',
      padding: '0 4px'
    }
  }, "These apply on every device."), /*#__PURE__*/React.createElement(SectionHeader, null, "Manage"), /*#__PURE__*/React.createElement(GlassCard, null, [['achievements', 'trophy', 'Achievements'], ['drivingApprovals', 'car', 'Driving approvals'], ['family', 'person.2', 'Family']].map(([key, ic, l]) => /*#__PURE__*/React.createElement("button", {
    key: key,
    type: "button",
    onClick: () => onManage && onManage(key),
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      width: '100%',
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      fontFamily: 'inherit',
      textAlign: 'left',
      padding: '9px 0',
      fontSize: 'var(--ds-text-body)',
      color: 'var(--ds-label)',
      minHeight: 44
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: ic,
    size: 17,
    color: "var(--ds-accent)"
  }), l, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement(Icon, {
    name: "chevron.right",
    size: 12,
    color: "var(--ds-label-3)"
  }))))) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(SectionHeader, null, "Mine"), /*#__PURE__*/React.createElement(GlassCard, null, /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => onManage && onManage('driving'),
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      width: '100%',
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      fontFamily: 'inherit',
      textAlign: 'left',
      padding: '9px 0',
      fontSize: 'var(--ds-text-body)',
      color: 'var(--ds-label)',
      minHeight: 44
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "car",
    size: 17,
    color: "var(--ds-accent)"
  }), "Driving log", /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement(Icon, {
    name: "chevron.right",
    size: 12,
    color: "var(--ds-label-3)"
  })))), /*#__PURE__*/React.createElement(SectionHeader, null, "Server"), /*#__PURE__*/React.createElement(GlassCard, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      padding: '6px 0',
      fontSize: 'var(--ds-text-body)'
    }
  }, /*#__PURE__*/React.createElement("span", null, "Connected to"), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ds-label-2)'
    }
  }, "bread.home.arpa")), /*#__PURE__*/React.createElement("div", {
    style: {
      height: '.5px',
      background: 'var(--ds-hairline)',
      margin: '4px 0'
    }
  }), /*#__PURE__*/React.createElement("button", {
    type: "button",
    style: {
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      fontFamily: 'inherit',
      padding: '9px 0',
      fontSize: 'var(--ds-text-body)',
      color: 'var(--ds-help)',
      textAlign: 'left',
      width: '100%',
      minHeight: 44
    }
  }, "Change server")), /*#__PURE__*/React.createElement(GlassCard, null, /*#__PURE__*/React.createElement("button", {
    type: "button",
    style: {
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      fontFamily: 'inherit',
      padding: '9px 0',
      fontSize: 'var(--ds-text-body)',
      color: 'var(--ds-help)',
      textAlign: 'left',
      width: '100%',
      minHeight: 44
    }
  }, "Sign Out")));
}

// ================= HELP SHEET =================
function HelpSheet({
  choreName,
  voiceParent,
  onSubmit,
  onCancel
}) {
  const [reason, setReason] = React.useState('');
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      height: '100%'
    }
  }, /*#__PURE__*/React.createElement(SheetHeader, {
    title: "Raise Help"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      overflowY: 'auto',
      padding: '4px 16px 12px',
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement(SheetCard, null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      lineHeight: 1.5
    }
  }, "Raising Help on ", /*#__PURE__*/React.createElement("b", null, choreName), " protects it from tonight's penalty until ", voiceParent, " responds.")), /*#__PURE__*/React.createElement(SheetCard, {
    title: "What's going on?"
  }, /*#__PURE__*/React.createElement(TextField, {
    multiline: true,
    rows: 3,
    value: reason,
    onChange: setReason,
    placeholder: "I need a hand because\u2026"
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 16
    }
  }, /*#__PURE__*/React.createElement(SheetActionBar, {
    saveTitle: "Raise Help",
    canSave: reason.trim().length > 0,
    onCancel: onCancel,
    onSave: () => onSubmit(reason)
  })));
}
Object.assign(window, {
  KidHome,
  KidToday,
  KidEarnings,
  KidAwards,
  AppSettings,
  HelpSheet,
  money,
  BADGES
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/kid_app/KidScreens.jsx", error: String((e && e.message) || e) }); }

// ui_kits/kid_app/tweaks-panel.jsx
try { (() => {
// @ds-adherence-ignore -- omelette starter scaffold (raw elements/hex/px by design)
// Copied omelette starter. Re-running copy_starter_component with this kind overwrites this file with the latest version (page content is unaffected).

/* BEGIN USAGE */
// tweaks-panel.jsx
// Reusable Tweaks shell + form-control helpers.
// Exports (to window): useTweaks, TweaksPanel, TweakSection, TweakRow, TweakSlider,
//   TweakToggle, TweakRadio, TweakSelect, TweakText, TweakNumber, TweakColor, TweakButton.
//
// Owns the host protocol (listens for __activate_edit_mode / __deactivate_edit_mode,
// posts __edit_mode_available / __edit_mode_set_keys / __edit_mode_dismissed) so
// individual prototypes don't re-roll it. Ships a consistent set of controls so you
// don't hand-draw <input type="range">, segmented radios, steppers, etc.
//
// Usage (in an HTML file that loads React + Babel):
//
//   const TWEAK_DEFAULTS = /*EDITMODE-BEGIN*/{
//     "primaryColor": "#D97757",
//     "palette": ["#D97757", "#29261b", "#f6f4ef"],
//     "fontSize": 16,
//     "density": "regular",
//     "dark": false
//   }/*EDITMODE-END*/;
//
//   function App() {
//     const [t, setTweak] = useTweaks(TWEAK_DEFAULTS);
//     return (
//       <div style={{ fontSize: t.fontSize, color: t.primaryColor }}>
//         Hello
//         <TweaksPanel>
//           <TweakSection label="Typography" />
//           <TweakSlider label="Font size" value={t.fontSize} min={10} max={32} unit="px"
//                        onChange={(v) => setTweak('fontSize', v)} />
//           <TweakRadio  label="Density" value={t.density}
//                        options={['compact', 'regular', 'comfy']}
//                        onChange={(v) => setTweak('density', v)} />
//           <TweakSection label="Theme" />
//           <TweakColor  label="Primary" value={t.primaryColor}
//                        options={['#D97757', '#2A6FDB', '#1F8A5B', '#7A5AE0']}
//                        onChange={(v) => setTweak('primaryColor', v)} />
//           <TweakColor  label="Palette" value={t.palette}
//                        options={[['#D97757', '#29261b', '#f6f4ef'],
//                                  ['#475569', '#0f172a', '#f1f5f9']]}
//                        onChange={(v) => setTweak('palette', v)} />
//           <TweakToggle label="Dark mode" value={t.dark}
//                        onChange={(v) => setTweak('dark', v)} />
//         </TweaksPanel>
//       </div>
//     );
//   }
//
// TweakRadio is the segmented control for 2–3 short options (auto-falls-back to
// TweakSelect past ~16/~10 chars per label); reach for TweakSelect directly when
// options are many or long. For color tweaks always curate 3-4 options rather than
// a free picker; an option can also be a whole 2–5 color palette (the stored value
// is the array). The Tweak* controls are a floor, not a ceiling — build custom
// controls inside the panel if a tweak calls for UI they don't cover.
/* END USAGE */
// ─────────────────────────────────────────────────────────────────────────────

const __TWEAKS_STYLE = `
  .twk-panel{position:fixed;right:16px;bottom:16px;z-index:2147483646;width:280px;
    max-height:calc(100vh - 32px);display:flex;flex-direction:column;
    transform:scale(var(--dc-inv-zoom,1));transform-origin:bottom right;
    background:rgba(250,249,247,.78);color:#29261b;
    -webkit-backdrop-filter:blur(24px) saturate(160%);backdrop-filter:blur(24px) saturate(160%);
    border:.5px solid rgba(255,255,255,.6);border-radius:14px;
    box-shadow:0 1px 0 rgba(255,255,255,.5) inset,0 12px 40px rgba(0,0,0,.18);
    font:11.5px/1.4 ui-sans-serif,system-ui,-apple-system,sans-serif;overflow:hidden}
  .twk-hd{display:flex;align-items:center;justify-content:space-between;
    padding:10px 8px 10px 14px;cursor:move;user-select:none}
  .twk-hd b{font-size:12px;font-weight:600;letter-spacing:.01em}
  .twk-x{appearance:none;border:0;background:transparent;color:rgba(41,38,27,.55);
    width:22px;height:22px;border-radius:6px;cursor:default;font-size:13px;line-height:1}
  .twk-x:hover{background:rgba(0,0,0,.06);color:#29261b}
  .twk-body{padding:2px 14px 14px;display:flex;flex-direction:column;gap:10px;
    overflow-y:auto;overflow-x:hidden;min-height:0;
    scrollbar-width:thin;scrollbar-color:rgba(0,0,0,.15) transparent}
  .twk-body::-webkit-scrollbar{width:8px}
  .twk-body::-webkit-scrollbar-track{background:transparent;margin:2px}
  .twk-body::-webkit-scrollbar-thumb{background:rgba(0,0,0,.15);border-radius:4px;
    border:2px solid transparent;background-clip:content-box}
  .twk-body::-webkit-scrollbar-thumb:hover{background:rgba(0,0,0,.25);
    border:2px solid transparent;background-clip:content-box}
  .twk-row{display:flex;flex-direction:column;gap:5px}
  .twk-row-h{flex-direction:row;align-items:center;justify-content:space-between;gap:10px}
  .twk-lbl{display:flex;justify-content:space-between;align-items:baseline;
    color:rgba(41,38,27,.72)}
  .twk-lbl>span:first-child{font-weight:500}
  .twk-val{color:rgba(41,38,27,.5);font-variant-numeric:tabular-nums}

  .twk-sect{font-size:10px;font-weight:600;letter-spacing:.06em;text-transform:uppercase;
    color:rgba(41,38,27,.45);padding:10px 0 0}
  .twk-sect:first-child{padding-top:0}

  .twk-field{appearance:none;box-sizing:border-box;width:100%;min-width:0;height:26px;padding:0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;
    background:rgba(255,255,255,.6);color:inherit;font:inherit;outline:none}
  .twk-field:focus{border-color:rgba(0,0,0,.25);background:rgba(255,255,255,.85)}
  select.twk-field{padding-right:22px;
    background-image:url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='10' height='6' viewBox='0 0 10 6'><path fill='rgba(0,0,0,.5)' d='M0 0h10L5 6z'/></svg>");
    background-repeat:no-repeat;background-position:right 8px center}

  .twk-slider{appearance:none;-webkit-appearance:none;width:100%;height:4px;margin:6px 0;
    border-radius:999px;background:rgba(0,0,0,.12);outline:none}
  .twk-slider::-webkit-slider-thumb{-webkit-appearance:none;appearance:none;
    width:14px;height:14px;border-radius:50%;background:#fff;
    border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}
  .twk-slider::-moz-range-thumb{width:14px;height:14px;border-radius:50%;
    background:#fff;border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}

  .twk-seg{position:relative;display:flex;padding:2px;border-radius:8px;
    background:rgba(0,0,0,.06);user-select:none}
  .twk-seg-thumb{position:absolute;top:2px;bottom:2px;border-radius:6px;
    background:rgba(255,255,255,.9);box-shadow:0 1px 2px rgba(0,0,0,.12);
    transition:left .15s cubic-bezier(.3,.7,.4,1),width .15s}
  .twk-seg.dragging .twk-seg-thumb{transition:none}
  .twk-seg button{appearance:none;position:relative;z-index:1;flex:1;border:0;
    background:transparent;color:inherit;font:inherit;font-weight:500;min-height:22px;
    border-radius:6px;cursor:default;padding:4px 6px;line-height:1.2;
    overflow-wrap:anywhere}

  .twk-toggle{position:relative;width:32px;height:18px;border:0;border-radius:999px;
    background:rgba(0,0,0,.15);transition:background .15s;cursor:default;padding:0}
  .twk-toggle[data-on="1"]{background:#34c759}
  .twk-toggle i{position:absolute;top:2px;left:2px;width:14px;height:14px;border-radius:50%;
    background:#fff;box-shadow:0 1px 2px rgba(0,0,0,.25);transition:transform .15s}
  .twk-toggle[data-on="1"] i{transform:translateX(14px)}

  .twk-num{display:flex;align-items:center;box-sizing:border-box;min-width:0;height:26px;padding:0 0 0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;background:rgba(255,255,255,.6)}
  .twk-num-lbl{font-weight:500;color:rgba(41,38,27,.6);cursor:ew-resize;
    user-select:none;padding-right:8px}
  .twk-num input{flex:1;min-width:0;height:100%;border:0;background:transparent;
    font:inherit;font-variant-numeric:tabular-nums;text-align:right;padding:0 8px 0 0;
    outline:none;color:inherit;-moz-appearance:textfield}
  .twk-num input::-webkit-inner-spin-button,.twk-num input::-webkit-outer-spin-button{
    -webkit-appearance:none;margin:0}
  .twk-num-unit{padding-right:8px;color:rgba(41,38,27,.45)}

  .twk-btn{appearance:none;height:26px;padding:0 12px;border:0;border-radius:7px;
    background:rgba(0,0,0,.78);color:#fff;font:inherit;font-weight:500;cursor:default}
  .twk-btn:hover{background:rgba(0,0,0,.88)}
  .twk-btn.secondary{background:rgba(0,0,0,.06);color:inherit}
  .twk-btn.secondary:hover{background:rgba(0,0,0,.1)}

  .twk-swatch{appearance:none;-webkit-appearance:none;width:56px;height:22px;
    border:.5px solid rgba(0,0,0,.1);border-radius:6px;padding:0;cursor:default;
    background:transparent;flex-shrink:0}
  .twk-swatch::-webkit-color-swatch-wrapper{padding:0}
  .twk-swatch::-webkit-color-swatch{border:0;border-radius:5.5px}
  .twk-swatch::-moz-color-swatch{border:0;border-radius:5.5px}

  .twk-chips{display:flex;gap:6px}
  .twk-chip{position:relative;appearance:none;flex:1;min-width:0;height:46px;
    padding:0;border:0;border-radius:6px;overflow:hidden;cursor:default;
    box-shadow:0 0 0 .5px rgba(0,0,0,.12),0 1px 2px rgba(0,0,0,.06);
    transition:transform .12s cubic-bezier(.3,.7,.4,1),box-shadow .12s}
  .twk-chip:hover{transform:translateY(-1px);
    box-shadow:0 0 0 .5px rgba(0,0,0,.18),0 4px 10px rgba(0,0,0,.12)}
  .twk-chip[data-on="1"]{box-shadow:0 0 0 1.5px rgba(0,0,0,.85),
    0 2px 6px rgba(0,0,0,.15)}
  .twk-chip>span{position:absolute;top:0;bottom:0;right:0;width:34%;
    display:flex;flex-direction:column;box-shadow:-1px 0 0 rgba(0,0,0,.1)}
  .twk-chip>span>i{flex:1;box-shadow:0 -1px 0 rgba(0,0,0,.1)}
  .twk-chip>span>i:first-child{box-shadow:none}
  .twk-chip svg{position:absolute;top:6px;left:6px;width:13px;height:13px;
    filter:drop-shadow(0 1px 1px rgba(0,0,0,.3))}
`;

// ── useTweaks ───────────────────────────────────────────────────────────────
// Single source of truth for tweak values. setTweak persists via the host
// (__edit_mode_set_keys → host rewrites the EDITMODE block on disk).
function useTweaks(defaults) {
  const [values, setValues] = React.useState(defaults);
  // Accepts either setTweak('key', value) or setTweak({ key: value, ... }) so a
  // useState-style call doesn't write a "[object Object]" key into the persisted
  // JSON block.
  const setTweak = React.useCallback((keyOrEdits, val) => {
    const edits = typeof keyOrEdits === 'object' && keyOrEdits !== null ? keyOrEdits : {
      [keyOrEdits]: val
    };
    setValues(prev => ({
      ...prev,
      ...edits
    }));
    window.parent.postMessage({
      type: '__edit_mode_set_keys',
      edits
    }, '*');
    // Same-window signal so in-page listeners (deck-stage rail thumbnails)
    // can react — the parent message only reaches the host, not peers.
    window.dispatchEvent(new CustomEvent('tweakchange', {
      detail: edits
    }));
  }, []);
  return [values, setTweak];
}

// ── TweaksPanel ─────────────────────────────────────────────────────────────
// Floating shell. Registers the protocol listener BEFORE announcing
// availability — if the announce ran first, the host's activate could land
// before our handler exists and the toolbar toggle would silently no-op.
// The close button posts __edit_mode_dismissed so the host's toolbar toggle
// flips off in lockstep; the host echoes __deactivate_edit_mode back which
// is what actually hides the panel.
function TweaksPanel({
  title = 'Tweaks',
  children
}) {
  const [open, setOpen] = React.useState(false);
  const dragRef = React.useRef(null);
  const offsetRef = React.useRef({
    x: 16,
    y: 16
  });
  const PAD = 16;
  const clampToViewport = React.useCallback(() => {
    const panel = dragRef.current;
    if (!panel) return;
    const w = panel.offsetWidth,
      h = panel.offsetHeight;
    const maxRight = Math.max(PAD, window.innerWidth - w - PAD);
    const maxBottom = Math.max(PAD, window.innerHeight - h - PAD);
    offsetRef.current = {
      x: Math.min(maxRight, Math.max(PAD, offsetRef.current.x)),
      y: Math.min(maxBottom, Math.max(PAD, offsetRef.current.y))
    };
    panel.style.right = offsetRef.current.x + 'px';
    panel.style.bottom = offsetRef.current.y + 'px';
  }, []);
  React.useEffect(() => {
    if (!open) return;
    clampToViewport();
    if (typeof ResizeObserver === 'undefined') {
      window.addEventListener('resize', clampToViewport);
      return () => window.removeEventListener('resize', clampToViewport);
    }
    const ro = new ResizeObserver(clampToViewport);
    ro.observe(document.documentElement);
    return () => ro.disconnect();
  }, [open, clampToViewport]);
  React.useEffect(() => {
    const onMsg = e => {
      const t = e?.data?.type;
      if (t === '__activate_edit_mode') setOpen(true);else if (t === '__deactivate_edit_mode') setOpen(false);
    };
    window.addEventListener('message', onMsg);
    window.parent.postMessage({
      type: '__edit_mode_available'
    }, '*');
    return () => window.removeEventListener('message', onMsg);
  }, []);
  const dismiss = () => {
    setOpen(false);
    window.parent.postMessage({
      type: '__edit_mode_dismissed'
    }, '*');
  };
  const onDragStart = e => {
    const panel = dragRef.current;
    if (!panel) return;
    const r = panel.getBoundingClientRect();
    const sx = e.clientX,
      sy = e.clientY;
    const startRight = window.innerWidth - r.right;
    const startBottom = window.innerHeight - r.bottom;
    const move = ev => {
      offsetRef.current = {
        x: startRight - (ev.clientX - sx),
        y: startBottom - (ev.clientY - sy)
      };
      clampToViewport();
    };
    const up = () => {
      window.removeEventListener('mousemove', move);
      window.removeEventListener('mouseup', up);
    };
    window.addEventListener('mousemove', move);
    window.addEventListener('mouseup', up);
  };

  // data-om-starter: inert presence marker — Claude Design's starter-usage
  // probe reads it. The closed panel renders nothing, so the marker rides
  // the <html> element as an attribute instead of a rendered node — zero
  // elements added, so page CSS (even structural selectors like
  // :nth-child) can never observe it. It records that the page WIRES a
  // tweaks panel, whether or not the panel is open. Keep this effect.
  React.useEffect(() => {
    document.documentElement.setAttribute('data-om-starter', 'tweaks-panel');
    return () => document.documentElement.removeAttribute('data-om-starter');
  }, []);
  if (!open) return null;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("style", null, __TWEAKS_STYLE), /*#__PURE__*/React.createElement("div", {
    ref: dragRef,
    className: "twk-panel",
    "data-omelette-chrome": "",
    style: {
      right: offsetRef.current.x,
      bottom: offsetRef.current.y
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-hd",
    onMouseDown: onDragStart
  }, /*#__PURE__*/React.createElement("b", null, title), /*#__PURE__*/React.createElement("button", {
    className: "twk-x",
    "aria-label": "Close tweaks",
    onMouseDown: e => e.stopPropagation(),
    onClick: dismiss
  }, "\u2715")), /*#__PURE__*/React.createElement("div", {
    className: "twk-body"
  }, children)));
}

// ── Layout helpers ──────────────────────────────────────────────────────────

function TweakSection({
  label,
  children
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "twk-sect"
  }, label), children);
}
function TweakRow({
  label,
  value,
  children,
  inline = false
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: inline ? 'twk-row twk-row-h' : 'twk-row'
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-lbl"
  }, /*#__PURE__*/React.createElement("span", null, label), value != null && /*#__PURE__*/React.createElement("span", {
    className: "twk-val"
  }, value)), children);
}

// ── Controls ────────────────────────────────────────────────────────────────

function TweakSlider({
  label,
  value,
  min = 0,
  max = 100,
  step = 1,
  unit = '',
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label,
    value: `${value}${unit}`
  }, /*#__PURE__*/React.createElement("input", {
    type: "range",
    className: "twk-slider",
    min: min,
    max: max,
    step: step,
    value: value,
    onChange: e => onChange(Number(e.target.value))
  }));
}
function TweakToggle({
  label,
  value,
  onChange
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: "twk-row twk-row-h"
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-lbl"
  }, /*#__PURE__*/React.createElement("span", null, label)), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "twk-toggle",
    "data-on": value ? '1' : '0',
    role: "switch",
    "aria-checked": !!value,
    onClick: () => onChange(!value)
  }, /*#__PURE__*/React.createElement("i", null)));
}
function TweakRadio({
  label,
  value,
  options,
  onChange
}) {
  const trackRef = React.useRef(null);
  const [dragging, setDragging] = React.useState(false);
  // The active value is read by pointer-move handlers attached for the lifetime
  // of a drag — ref it so a stale closure doesn't fire onChange for every move.
  const valueRef = React.useRef(value);
  valueRef.current = value;

  // Segments wrap mid-word once per-segment width runs out. The track is
  // ~248px (280 panel − 28 body pad − 4 seg pad), each button loses 12px
  // to its own padding, and 11.5px system-ui averages ~6.3px/char — so 2
  // options fit ~16 chars each, 3 fit ~10. Past that (or >3 options), fall
  // back to a dropdown rather than wrap.
  const labelLen = o => String(typeof o === 'object' ? o.label : o).length;
  const maxLen = options.reduce((m, o) => Math.max(m, labelLen(o)), 0);
  const fitsAsSegments = maxLen <= ({
    2: 16,
    3: 10
  }[options.length] ?? 0);
  if (!fitsAsSegments) {
    // <select> emits strings — map back to the original option value so the
    // fallback stays type-preserving (numbers, booleans) like the segment path.
    const resolve = s => {
      const m = options.find(o => String(typeof o === 'object' ? o.value : o) === s);
      return m === undefined ? s : typeof m === 'object' ? m.value : m;
    };
    return /*#__PURE__*/React.createElement(TweakSelect, {
      label: label,
      value: value,
      options: options,
      onChange: s => onChange(resolve(s))
    });
  }
  const opts = options.map(o => typeof o === 'object' ? o : {
    value: o,
    label: o
  });
  const idx = Math.max(0, opts.findIndex(o => o.value === value));
  const n = opts.length;
  const segAt = clientX => {
    const r = trackRef.current.getBoundingClientRect();
    const inner = r.width - 4;
    const i = Math.floor((clientX - r.left - 2) / inner * n);
    return opts[Math.max(0, Math.min(n - 1, i))].value;
  };
  const onPointerDown = e => {
    setDragging(true);
    const v0 = segAt(e.clientX);
    if (v0 !== valueRef.current) onChange(v0);
    const move = ev => {
      if (!trackRef.current) return;
      const v = segAt(ev.clientX);
      if (v !== valueRef.current) onChange(v);
    };
    const up = () => {
      setDragging(false);
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("div", {
    ref: trackRef,
    role: "radiogroup",
    onPointerDown: onPointerDown,
    className: dragging ? 'twk-seg dragging' : 'twk-seg'
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-seg-thumb",
    style: {
      left: `calc(2px + ${idx} * (100% - 4px) / ${n})`,
      width: `calc((100% - 4px) / ${n})`
    }
  }), opts.map(o => /*#__PURE__*/React.createElement("button", {
    key: o.value,
    type: "button",
    role: "radio",
    "aria-checked": o.value === value
  }, o.label))));
}
function TweakSelect({
  label,
  value,
  options,
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("select", {
    className: "twk-field",
    value: value,
    onChange: e => onChange(e.target.value)
  }, options.map(o => {
    const v = typeof o === 'object' ? o.value : o;
    const l = typeof o === 'object' ? o.label : o;
    return /*#__PURE__*/React.createElement("option", {
      key: v,
      value: v
    }, l);
  })));
}
function TweakText({
  label,
  value,
  placeholder,
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("input", {
    className: "twk-field",
    type: "text",
    value: value,
    placeholder: placeholder,
    onChange: e => onChange(e.target.value)
  }));
}
function TweakNumber({
  label,
  value,
  min,
  max,
  step = 1,
  unit = '',
  onChange
}) {
  const clamp = n => {
    if (min != null && n < min) return min;
    if (max != null && n > max) return max;
    return n;
  };
  const startRef = React.useRef({
    x: 0,
    val: 0
  });
  const onScrubStart = e => {
    e.preventDefault();
    startRef.current = {
      x: e.clientX,
      val: value
    };
    const decimals = (String(step).split('.')[1] || '').length;
    const move = ev => {
      const dx = ev.clientX - startRef.current.x;
      const raw = startRef.current.val + dx * step;
      const snapped = Math.round(raw / step) * step;
      onChange(clamp(Number(snapped.toFixed(decimals))));
    };
    const up = () => {
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: "twk-num"
  }, /*#__PURE__*/React.createElement("span", {
    className: "twk-num-lbl",
    onPointerDown: onScrubStart
  }, label), /*#__PURE__*/React.createElement("input", {
    type: "number",
    value: value,
    min: min,
    max: max,
    step: step,
    onChange: e => onChange(clamp(Number(e.target.value)))
  }), unit && /*#__PURE__*/React.createElement("span", {
    className: "twk-num-unit"
  }, unit));
}

// Relative-luminance contrast pick — checkmarks drawn over a swatch need to
// read on both #111 and #fafafa without per-option configuration. Hex input
// only (#rgb / #rrggbb); named or rgb()/hsl() colors fall through to "light".
function __twkIsLight(hex) {
  const h = String(hex).replace('#', '');
  const x = h.length === 3 ? h.replace(/./g, c => c + c) : h.padEnd(6, '0');
  const n = parseInt(x.slice(0, 6), 16);
  if (Number.isNaN(n)) return true;
  const r = n >> 16 & 255,
    g = n >> 8 & 255,
    b = n & 255;
  return r * 299 + g * 587 + b * 114 > 148000;
}
const __TwkCheck = ({
  light
}) => /*#__PURE__*/React.createElement("svg", {
  viewBox: "0 0 14 14",
  "aria-hidden": "true"
}, /*#__PURE__*/React.createElement("path", {
  d: "M3 7.2 5.8 10 11 4.2",
  fill: "none",
  strokeWidth: "2.2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  stroke: light ? 'rgba(0,0,0,.78)' : '#fff'
}));

// TweakColor — curated color/palette picker. Each option is either a single
// hex string or an array of 1-5 hex strings; the card adapts — a lone color
// renders solid, a palette renders colors[0] as the hero (left ~2/3) with the
// rest stacked in a sharp column on the right. onChange emits the
// option in the shape it was passed (string stays string, array stays array).
// Without options it falls back to the native color input for back-compat.
function TweakColor({
  label,
  value,
  options,
  onChange
}) {
  if (!options || !options.length) {
    return /*#__PURE__*/React.createElement("div", {
      className: "twk-row twk-row-h"
    }, /*#__PURE__*/React.createElement("div", {
      className: "twk-lbl"
    }, /*#__PURE__*/React.createElement("span", null, label)), /*#__PURE__*/React.createElement("input", {
      type: "color",
      className: "twk-swatch",
      value: value,
      onChange: e => onChange(e.target.value)
    }));
  }
  // Native <input type=color> emits lowercase hex per the HTML spec, so
  // compare case-insensitively. String() guards JSON.stringify(undefined),
  // which returns the primitive undefined (no .toLowerCase).
  const key = o => String(JSON.stringify(o)).toLowerCase();
  const cur = key(value);
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-chips",
    role: "radiogroup"
  }, options.map((o, i) => {
    const colors = Array.isArray(o) ? o : [o];
    const [hero, ...rest] = colors;
    const sup = rest.slice(0, 4);
    const on = key(o) === cur;
    return /*#__PURE__*/React.createElement("button", {
      key: i,
      type: "button",
      className: "twk-chip",
      role: "radio",
      "aria-checked": on,
      "data-on": on ? '1' : '0',
      "aria-label": colors.join(', '),
      title: colors.join(' · '),
      style: {
        background: hero
      },
      onClick: () => onChange(o)
    }, sup.length > 0 && /*#__PURE__*/React.createElement("span", null, sup.map((c, j) => /*#__PURE__*/React.createElement("i", {
      key: j,
      style: {
        background: c
      }
    }))), on && /*#__PURE__*/React.createElement(__TwkCheck, {
      light: __twkIsLight(hero)
    }));
  })));
}
function TweakButton({
  label,
  onClick,
  secondary = false
}) {
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: secondary ? 'twk-btn secondary' : 'twk-btn',
    onClick: onClick
  }, label);
}
Object.assign(window, {
  useTweaks,
  TweaksPanel,
  TweakSection,
  TweakRow,
  TweakSlider,
  TweakToggle,
  TweakRadio,
  TweakSelect,
  TweakText,
  TweakNumber,
  TweakColor,
  TweakButton
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/kid_app/tweaks-panel.jsx", error: String((e && e.message) || e) }); }

// ui_kits/mac_app/MacShell.jsx
try { (() => {
const DS = window.DailyBreadDesignSystem_2a4964;
const {
  Icon,
  GlassCard,
  SectionHeader
} = DS;

/**
 * macOS NavigationSplitView shell. Sidebar sections come from MainView.swift's
 * Section enum, so the parent and kid sidebars are the same component with a
 * different list — one design language, two audiences.
 */
function MacWindow({
  sections,
  active,
  badges = {},
  onSelect,
  title,
  toolbar,
  back,
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      height: '100vh',
      overflow: 'hidden',
      background: 'var(--ds-background)',
      color: 'var(--ds-label)'
    }
  }, /*#__PURE__*/React.createElement("aside", {
    style: {
      width: 'var(--ds-mac-sidebar)',
      flex: '0 0 auto',
      display: 'flex',
      flexDirection: 'column',
      background: 'color-mix(in srgb, var(--ds-card) 45%, transparent)',
      backdropFilter: 'blur(30px)',
      WebkitBackdropFilter: 'blur(30px)',
      borderRight: '.5px solid var(--ds-hairline)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      padding: '18px 20px 22px'
    }
  }, ['#FF5F57', '#FEBC2E', '#28C840'].map(c => /*#__PURE__*/React.createElement("span", {
    key: c,
    style: {
      width: 12,
      height: 12,
      borderRadius: '50%',
      background: c
    }
  }))), /*#__PURE__*/React.createElement("nav", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2,
      padding: '0 10px'
    }
  }, sections.map(s => {
    const on = s.key === active;
    return /*#__PURE__*/React.createElement("button", {
      key: s.key,
      type: "button",
      onClick: () => onSelect(s.key),
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        width: '100%',
        minHeight: 44,
        padding: '10px 12px',
        cursor: 'pointer',
        fontFamily: 'inherit',
        textAlign: 'left',
        border: 'none',
        borderRadius: 'var(--ds-radius-swatch)',
        background: on ? 'var(--ds-accent)' : 'transparent',
        color: on ? 'var(--ds-on-accent)' : 'var(--ds-label)',
        fontSize: 'var(--ds-text-body)',
        fontWeight: on ? 'var(--ds-weight-semibold)' : 'var(--ds-weight-regular)',
        transition: 'background var(--ds-snappy)'
      }
    }, /*#__PURE__*/React.createElement(Icon, {
      name: s.icon,
      size: 18,
      color: on ? 'var(--ds-on-accent)' : 'var(--ds-accent)'
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        overflow: 'hidden',
        textOverflow: 'ellipsis',
        whiteSpace: 'nowrap'
      }
    }, s.label), badges[s.key] ? /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 'var(--ds-text-caption)',
        fontWeight: 'var(--ds-weight-bold)',
        color: on ? 'var(--ds-on-accent)' : 'var(--ds-label-2)',
        fontVariantNumeric: 'tabular-nums'
      }
    }, badges[s.key]) : null);
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("header", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      padding: '14px 24px',
      flex: '0 0 auto',
      borderBottom: '.5px solid var(--ds-hairline)'
    }
  }, back ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: back,
    "aria-label": "Back",
    style: {
      width: 34,
      height: 34,
      flex: '0 0 auto',
      cursor: 'pointer',
      border: 'none',
      borderRadius: '50%',
      display: 'grid',
      placeItems: 'center',
      background: 'color-mix(in srgb, var(--ds-label) 8%, transparent)',
      color: 'var(--ds-label)'
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "chevron.left",
    size: 16
  })) : null, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-title3)',
      fontWeight: 'var(--ds-weight-bold)'
    }
  }, title), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), toolbar ? /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 4,
      padding: 4,
      borderRadius: 'var(--ds-radius-capsule)',
      background: 'color-mix(in srgb, var(--ds-label) 7%, transparent)'
    }
  }, toolbar) : null), /*#__PURE__*/React.createElement("main", {
    style: {
      flex: 1,
      overflowY: 'auto',
      padding: '20px 24px 32px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 'var(--ds-mac-content)',
      margin: '0 auto'
    }
  }, children))));
}

/** Round toolbar button — the macOS header's only control shape. */
function ToolbarButton({
  icon,
  label,
  onClick
}) {
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onClick,
    "aria-label": label,
    style: {
      width: 34,
      height: 34,
      cursor: 'pointer',
      border: 'none',
      borderRadius: '50%',
      display: 'grid',
      placeItems: 'center',
      background: 'transparent',
      color: 'var(--ds-accent)'
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: icon,
    size: 17
  }));
}
const PARENT_SECTIONS = [{
  key: 'home',
  label: 'Home',
  icon: 'house'
}, {
  key: 'planner',
  label: 'Planner',
  icon: 'checklist'
}, {
  key: 'approvals',
  label: 'Approvals',
  icon: 'checkmark.circle'
}, {
  key: 'settings',
  label: 'Settings',
  icon: 'gearshape'
}];
const KID_SECTIONS = [{
  key: 'kidHome',
  label: 'Home',
  icon: 'house'
}, {
  key: 'today',
  label: 'Today',
  icon: 'sun.max'
}, {
  key: 'earnings',
  label: 'Earnings',
  icon: 'dollarsign.circle'
}, {
  key: 'awards',
  label: 'Awards',
  icon: 'trophy'
}, {
  key: 'settings',
  label: 'Settings',
  icon: 'gearshape'
}];
Object.assign(window, {
  MacWindow,
  ToolbarButton,
  PARENT_SECTIONS,
  KID_SECTIONS
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/mac_app/MacShell.jsx", error: String((e && e.message) || e) }); }

// ui_kits/parent_app/ParentScreens.jsx
try { (() => {
const DS = window.DailyBreadDesignSystem_2a4964;
const {
  GlassCard,
  SectionHeader,
  Chip,
  Tag,
  StatTile,
  ProgressRing,
  Button,
  EmptyState,
  InlineError,
  Icon,
  ChildRow,
  ChildTile,
  WeekStrip,
  YearHeatmapCard,
  ApprovalRow,
  HelpRow,
  ExpandRow,
  ConfirmRow,
  PlannerGrid,
  PlannerChoreRow,
  SegmentedPicker,
  ChoreRow,
  AtRiskCard,
  ScreenTimeCard,
  SheetHeader,
  SheetCard,
  SheetField,
  SheetActionBar,
  TextField,
  ToggleRow,
  Slider,
  Stepper,
  DayPicker,
  ALL_DAYS
} = DS;
const money = n => '$' + n.toFixed(2);

// ================= PARENT HOME =================
function ParentHome({
  dash,
  onDrill,
  onNav
}) {
  const kids = dash.children;
  const singleChild = kids.length === 1;
  const waiting = dash.approvals.length + dash.help.length;
  const doneToday = kids.reduce((s, c) => s + c.done, 0);
  const totalToday = kids.reduce((s, c) => s + c.total, 0);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(GlassCard, {
    padding: "16px"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-title2)',
      fontWeight: 'var(--ds-weight-bold)'
    }
  }, "Good evening, ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ds-accent)'
    }
  }, "Shaun")), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)',
      whiteSpace: 'nowrap'
    }
  }, "Friday, July 24, 2026")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement(Chip, {
    tone: "var(--ds-gold)",
    emphasized: dash.approvals.length > 0
  }, "\u2726 ", dash.approvals.length, " awaiting approval"), /*#__PURE__*/React.createElement(Chip, {
    emphasized: false
  }, "\u2713 ", doneToday, " done today"), /*#__PURE__*/React.createElement(Chip, {
    tone: "var(--ds-help)",
    emphasized: dash.help.length > 0
  }, "! ", dash.help.length, " needs help")))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(StatTile, {
    value: waiting,
    label: "waiting on you",
    color: "var(--ds-accent)"
  }), /*#__PURE__*/React.createElement(StatTile, {
    value: money(dash.weekEarnings),
    label: "earned this week",
    color: "var(--ds-gold)"
  }), /*#__PURE__*/React.createElement(StatTile, {
    value: doneToday + '/' + totalToday,
    label: "chores today"
  })), /*#__PURE__*/React.createElement(SectionHeader, null, "Today"), kids.length <= 2 ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, kids.map(c => /*#__PURE__*/React.createElement(ChildRow, {
    key: c.name,
    child: c,
    onTap: () => onDrill(c)
  }))) : /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 10
    }
  }, kids.map(c => /*#__PURE__*/React.createElement(ChildTile, {
    key: c.name,
    child: c,
    onTap: () => onDrill(c)
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(SectionHeader, null, "Quick view \u2014 this week"), /*#__PURE__*/React.createElement(WeekStrip, {
    days: dash.week
  })), /*#__PURE__*/React.createElement(SectionHeader, null, singleChild ? 'Balance' : 'Balances'), /*#__PURE__*/React.createElement(GlassCard, null, kids.map((c, i) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: c.name
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      width: '100%',
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      fontFamily: 'inherit',
      textAlign: 'left',
      padding: '10px 0',
      fontSize: 'var(--ds-text-body)',
      color: 'var(--ds-label)',
      minHeight: 44
    }
  }, /*#__PURE__*/React.createElement("span", null, c.name), c.balance >= 25 ? /*#__PURE__*/React.createElement(Tag, {
    tone: "var(--ds-success)"
  }, "Cash out ready") : null, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-semibold)',
      color: 'var(--ds-gold)'
    }
  }, money(c.balance)), /*#__PURE__*/React.createElement(Icon, {
    name: "slider.horizontal.3",
    size: 13,
    color: "var(--ds-label-3)"
  })), i < kids.length - 1 ? /*#__PURE__*/React.createElement("div", {
    style: {
      height: '.5px',
      background: 'var(--ds-hairline)'
    }
  }) : null))), /*#__PURE__*/React.createElement(YearHeatmapCard, {
    title: kids[0].name + "'s year"
  }));
}

// ================= APPROVALS =================
function ParentApprovals({
  dash,
  onApprove,
  onApproveAll,
  onHelp
}) {
  const [showAllApprovals, setShowAllApprovals] = React.useState(false);
  const [showAllHelp, setShowAllHelp] = React.useState(false);
  const [confirming, setConfirming] = React.useState(false);
  const [justApproved, setJustApproved] = React.useState(null);
  const cap = 3;
  const singleChild = dash.children.length === 1;
  const total = dash.approvals.reduce((s, a) => s + a.value, 0);
  if (dash.approvals.length === 0 && dash.help.length === 0) {
    return /*#__PURE__*/React.createElement(EmptyState, {
      icon: "checkmark.seal",
      title: "All caught up",
      description: "Nothing needs your approval right now."
    });
  }
  const shownHelp = showAllHelp ? dash.help : dash.help.slice(0, cap);
  const shownApprovals = showAllApprovals ? dash.approvals : dash.approvals.slice(0, cap);
  const approve = a => {
    setJustApproved(a.id);
    setTimeout(() => {
      setJustApproved(null);
      onApprove(a.id);
    }, 900);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, dash.help.length > 0 ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(SectionHeader, null, "Help raised"), /*#__PURE__*/React.createElement(GlassCard, null, shownHelp.map((h, i) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: h.id
  }, /*#__PURE__*/React.createElement(HelpRow, {
    choreName: h.choreName,
    reason: h.reason,
    childName: h.childName,
    onOpen: () => onHelp(h)
  }), i < shownHelp.length - 1 ? /*#__PURE__*/React.createElement("div", {
    style: {
      height: '.5px',
      background: 'var(--ds-hairline)'
    }
  }) : null)), dash.help.length > cap ? /*#__PURE__*/React.createElement(ExpandRow, {
    expanded: showAllHelp,
    moreCount: dash.help.length - cap,
    label: "more help requests",
    color: "var(--ds-help)",
    onToggle: () => setShowAllHelp(s => !s)
  }) : null)) : null, dash.approvals.length > 0 ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(SectionHeader, null, "Waiting for approval"), /*#__PURE__*/React.createElement(GlassCard, null, dash.approvals.length > 1 ? confirming ? /*#__PURE__*/React.createElement(ConfirmRow, {
    message: 'Approve ' + dash.approvals.length + ' chores?',
    confirmTitle: 'Approve — ' + money(total),
    keepTitle: "Cancel",
    tone: "var(--ds-gold)",
    onKeep: () => setConfirming(false),
    onConfirm: () => {
      setConfirming(false);
      onApproveAll();
    }
  }) : /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => setConfirming(true),
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 7,
      width: '100%',
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      fontFamily: 'inherit',
      textAlign: 'left',
      padding: '10px 0',
      minHeight: 44,
      fontSize: 'var(--ds-text-subheadline)',
      fontWeight: 'var(--ds-weight-bold)',
      color: 'var(--ds-gold)'
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "checkmark.seal.fill",
    size: 15
  }), "Approve all (", dash.approvals.length, ") \u2014 ", money(total)) : null, shownApprovals.map(a => /*#__PURE__*/React.createElement(ApprovalRow, {
    key: a.id,
    choreName: a.choreName,
    childName: singleChild ? undefined : a.childName,
    value: money(a.value),
    approved: justApproved === a.id,
    onApprove: () => approve(a)
  })), dash.approvals.length > cap ? /*#__PURE__*/React.createElement(ExpandRow, {
    expanded: showAllApprovals,
    moreCount: dash.approvals.length - cap,
    label: "more waiting",
    color: "var(--ds-gold)",
    onToggle: () => setShowAllApprovals(s => !s)
  }) : null)) : null);
}

// ================= HELP RESPOND SHEET =================
function HelpRespondSheet({
  request,
  onChoose,
  onClose
}) {
  const choice = (title, subtitle, prominent, kind) => /*#__PURE__*/React.createElement("button", {
    key: kind,
    type: "button",
    onClick: () => onChoose(kind),
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: 2,
      width: '100%',
      padding: '12px 10px',
      border: 'none',
      cursor: 'pointer',
      borderRadius: 'var(--ds-radius-swatch)',
      fontFamily: 'inherit',
      background: prominent ? 'var(--ds-gold)' : 'color-mix(in srgb, var(--ds-label-2) 35%, transparent)',
      color: prominent ? 'rgb(0 0 0 / .8)' : 'var(--ds-label)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontWeight: 'var(--ds-weight-bold)'
    }
  }, title), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      opacity: .8
    }
  }, subtitle));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      height: '100%',
      padding: 16,
      gap: 20
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 9,
      height: 9,
      borderRadius: '50%',
      background: 'var(--ds-help)'
    }
  }), /*#__PURE__*/React.createElement(SectionHeader, {
    kerning: "eyebrow",
    color: "var(--ds-help)",
    style: {
      fontWeight: 'var(--ds-weight-heavy)'
    }
  }, "Help raised")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-title2)',
      fontWeight: 'var(--ds-weight-bold)'
    }
  }, request.choreName), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), request.value ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-headline)',
      color: 'var(--ds-gold)'
    }
  }, money(request.value)) : null), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      color: 'var(--ds-label-2)'
    }
  }, request.childName, " \xB7 Friday, July 24, 2026 \xB7 raised 20m ago")), request.reason ? /*#__PURE__*/React.createElement(GlassCard, null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-body)',
      fontStyle: 'italic',
      color: 'var(--ds-label-2)',
      lineHeight: 1.45
    }
  }, "\u201C", request.reason, "\u201D")) : null, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, choice('✓ Fulfill for them', 'You did it — ' + request.childName + ' receives full credit', true, 'completedByParent'), choice('Grant dispensation', 'Excused for today. No penalty, no earning', false, 'excused'), choice('↺ They must try again', 'Back to pending — ' + request.childName + ' does it themselves', false, 'denied')));
}

// ================= PLANNER =================
function ParentPlanner({
  chores,
  onToggleDay,
  onEdit,
  onAdd,
  singleChild
}) {
  const [kind, setKind] = React.useState('Task');
  const [isGrid, setIsGrid] = React.useState(true);
  const [confirmingId, setConfirmingId] = React.useState(null);
  const visible = chores.filter(c => c.kind === kind);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(SegmentedPicker, {
    value: kind,
    onChange: setKind,
    options: [{
      value: 'Task',
      label: 'Tasks'
    }, {
      value: 'Routine',
      label: 'Routines'
    }]
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, visible.length, " ", kind === 'Task' ? 'task' : 'routine', visible.length === 1 ? '' : 's'), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => setIsGrid(g => !g),
    "aria-label": isGrid ? 'List view' : 'Grid view',
    style: {
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      color: 'var(--ds-accent)',
      padding: 4
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: isGrid ? 'list.bullet' : 'square.grid.2x2',
    size: 18
  })), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onAdd,
    "aria-label": "Add chore",
    style: {
      background: 'transparent',
      border: 'none',
      cursor: 'pointer',
      color: 'var(--ds-accent)',
      padding: 4
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "plus",
    size: 18
  }))), visible.length === 0 ? /*#__PURE__*/React.createElement(EmptyState, {
    icon: "checklist",
    title: "No chores yet",
    description: "Add the first one. Tasks earn money; Routines are just expected.",
    action: /*#__PURE__*/React.createElement(Button, {
      icon: "plus",
      onClick: onAdd
    }, "Add a chore")
  }) : isGrid ? /*#__PURE__*/React.createElement(GlassCard, {
    padding: "8px"
  }, /*#__PURE__*/React.createElement(PlannerGrid, {
    chores: visible,
    showAssignee: !singleChild,
    onToggle: onToggleDay,
    onEdit: onEdit
  })) : /*#__PURE__*/React.createElement(GlassCard, null, visible.map((c, i) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: c.id
  }, confirmingId === c.id ? /*#__PURE__*/React.createElement(ConfirmRow, {
    message: 'Delete “' + c.name + '”? This can\'t be undone.',
    onKeep: () => setConfirmingId(null),
    onConfirm: () => setConfirmingId(null)
  }) : /*#__PURE__*/React.createElement(PlannerChoreRow, {
    name: c.name,
    icon: c.icon,
    value: c.value,
    isTask: c.kind === 'Task',
    schedule: c.weeklyTarget ? 'up to ' + c.weeklyTarget + '× a week' : scheduleLabel(c.days),
    assignee: singleChild ? undefined : c.assignee,
    importance: c.importance,
    active: c.active !== false,
    onTap: () => onEdit(c)
  }), i < visible.length - 1 ? /*#__PURE__*/React.createElement("div", {
    style: {
      height: '.5px',
      background: 'var(--ds-hairline)'
    }
  }) : null))));
}
function scheduleLabel(days) {
  if (!days || days.length === 0) return 'No days';
  if (days.length === 7) return 'Every day';
  const short = {
    Sunday: 'Sun',
    Monday: 'Mon',
    Tuesday: 'Tue',
    Wednesday: 'Wed',
    Thursday: 'Thu',
    Friday: 'Fri',
    Saturday: 'Sat'
  };
  if (days.length === 5 && !days.includes('Sunday') && !days.includes('Saturday')) return 'Weekdays';
  return ALL_DAYS.filter(d => days.includes(d)).map(d => short[d]).join(', ');
}

// ================= CHORE EDITOR SHEET =================
function ChoreEditorSheet({
  chore,
  singleChild,
  onSave,
  onCancel
}) {
  const isEditing = !!chore;
  const [name, setName] = React.useState(chore ? chore.name : '');
  const [icon, setIcon] = React.useState(chore ? chore.icon : '');
  const [isTask, setIsTask] = React.useState(chore ? chore.kind === 'Task' : true);
  const [earn, setEarn] = React.useState(chore && chore.value ? chore.value.replace('$', '') : '1.00');
  const [isWeekly, setIsWeekly] = React.useState(chore ? !!chore.weeklyTarget : false);
  const [days, setDays] = React.useState(chore && chore.days ? chore.days : ALL_DAYS);
  const [weeklyTarget, setWeeklyTarget] = React.useState(chore && chore.weeklyTarget ? chore.weeklyTarget : 3);
  const [repeatable, setRepeatable] = React.useState(false);
  const [importance, setImportance] = React.useState(chore ? chore.importance || 0 : 0);
  const [autoApprove, setAutoApprove] = React.useState(true);
  const [notes, setNotes] = React.useState('');
  const canSave = name.trim().length > 0 && (isWeekly || days.length > 0);
  const noun = isTask ? 'Task' : 'Routine';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      height: '100%'
    }
  }, /*#__PURE__*/React.createElement(SheetHeader, {
    title: (isEditing ? 'Edit ' : 'New ') + noun
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      overflowY: 'auto',
      padding: '4px 16px 12px',
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement(SheetCard, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement(TextField, {
    value: icon,
    onChange: v => setIcon(v.slice(0, 2)),
    placeholder: "\uD83E\uDDFA",
    width: 46,
    align: "center"
  }), /*#__PURE__*/React.createElement(TextField, {
    value: name,
    onChange: setName,
    placeholder: "What's the chore?"
  }))), /*#__PURE__*/React.createElement(SheetCard, null, /*#__PURE__*/React.createElement(SegmentedPicker, {
    value: isTask ? 'task' : 'routine',
    onChange: v => setIsTask(v === 'task'),
    options: [{
      value: 'task',
      label: 'Earns'
    }, {
      value: 'routine',
      label: 'Expected'
    }]
  }), isTask ? /*#__PURE__*/React.createElement(SheetField, {
    label: "Amount"
  }, /*#__PURE__*/React.createElement(TextField, {
    prefix: "$",
    value: earn,
    onChange: setEarn,
    placeholder: "1.00"
  })) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, isTask ? "Earns money when it's done and approved." : 'Just expected — no money attached.')), /*#__PURE__*/React.createElement(SheetCard, {
    title: "Schedule rule"
  }, /*#__PURE__*/React.createElement(SegmentedPicker, {
    value: isWeekly ? 'weekly' : 'days',
    onChange: v => setIsWeekly(v === 'weekly'),
    options: [{
      value: 'days',
      label: 'Fixed days'
    }, {
      value: 'weekly',
      label: 'Weekly goal'
    }]
  }), isWeekly ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Stepper, {
    label: 'up to ' + weeklyTarget + '× a week',
    value: weeklyTarget,
    min: 1,
    max: 7,
    onChange: setWeeklyTarget
  }), /*#__PURE__*/React.createElement(ToggleRow, {
    label: "Bonus reps allowed",
    checked: repeatable,
    onChange: setRepeatable
  })) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(DayPicker, {
    selected: days,
    onChange: setDays
  }), days.length === 0 ? /*#__PURE__*/React.createElement(InlineError, {
    icon: "exclamationmark.circle",
    style: {
      color: 'var(--ds-help)'
    }
  }, "Pick at least one day.") : null)), /*#__PURE__*/React.createElement(SheetCard, {
    title: "Screen time"
  }, /*#__PURE__*/React.createElement(SheetField, {
    label: importance === 0 ? 'No screen-time impact' : 'Importance',
    value: importance === 0 ? null : importance + ' of 10',
    valueColor: "var(--ds-accent)"
  }, /*#__PURE__*/React.createElement(Slider, {
    value: importance,
    max: 10,
    onChange: setImportance
  })), importance > 0 ? /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 'var(--ds-text-caption)',
      color: 'var(--ds-label-2)'
    }
  }, "Missing it costs a share of the weekly screen-time budget.") : null), /*#__PURE__*/React.createElement(SheetCard, {
    title: "Options"
  }, /*#__PURE__*/React.createElement(ToggleRow, {
    label: "Auto-approve completions",
    description: "No parent check needed",
    checked: autoApprove,
    onChange: setAutoApprove
  }), /*#__PURE__*/React.createElement(SheetField, {
    label: "Notes (optional)"
  }, /*#__PURE__*/React.createElement(TextField, {
    multiline: true,
    rows: 2,
    value: notes,
    onChange: setNotes,
    placeholder: "e.g. don't forget under the bed"
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 16
    }
  }, /*#__PURE__*/React.createElement(SheetActionBar, {
    saveTitle: isEditing ? 'Save' : 'Create',
    canSave: canSave,
    onCancel: onCancel,
    onSave: () => onSave({
      id: chore ? chore.id : Date.now(),
      name: name.trim(),
      icon: icon || undefined,
      kind: isTask ? 'Task' : 'Routine',
      value: isTask ? '$' + Number(earn || 0).toFixed(2) : undefined,
      days: isWeekly ? undefined : days,
      weeklyTarget: isWeekly ? weeklyTarget : undefined,
      importance,
      active: true
    })
  })));
}

// ================= KID'S DAY (parent drill-in) =================
function ParentChildDay({
  child
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement(GlassCard, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(ProgressRing, {
    progress: child.total ? child.done / child.total : 0,
    label: child.done + '/' + child.total
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 'var(--ds-text-subheadline)',
      color: 'var(--ds-label-2)'
    }
  }, "Friday, July 24, 2026"), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 6,
      fontSize: 'var(--ds-text-subheadline)'
    }
  }, /*#__PURE__*/React.createElement("b", {
    style: {
      color: 'var(--ds-gold)'
    }
  }, money(3.5)), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ds-label-2)'
    }
  }, "today"), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ds-label-3)'
    }
  }, "\xB7"), /*#__PURE__*/React.createElement("b", {
    style: {
      color: 'var(--ds-gold)',
      fontWeight: 'var(--ds-weight-semibold)'
    }
  }, money(child.balance)), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--ds-label-2)'
    }
  }, "saved"))))), /*#__PURE__*/React.createElement(AtRiskCard, {
    items: [{
      name: 'Dishes',
      detail: 'Due tonight',
      urgency: 'DueTonight',
      money: '$2.00',
      minutes: 20
    }]
  }), /*#__PURE__*/React.createElement(GlassCard, null, /*#__PURE__*/React.createElement(ChoreRow, {
    icon: "\uD83D\uDECF",
    name: "Make your bed",
    done: true,
    allowHelp: false
  }), /*#__PURE__*/React.createElement(ChoreRow, {
    icon: "\uD83C\uDF7D",
    name: "Dishes",
    value: "$2.00",
    allowHelp: false,
    onToggle: () => {}
  }), /*#__PURE__*/React.createElement(ChoreRow, {
    icon: "\uD83D\uDDD1",
    name: "Take out the trash",
    value: "$1.50",
    allowHelp: false,
    onToggle: () => {}
  }), /*#__PURE__*/React.createElement(ChoreRow, {
    icon: "\uD83D\uDCDA",
    name: "Read 20 minutes",
    value: "$1.00",
    weekly: {
      done: 4,
      target: 7
    },
    allowHelp: false,
    onToggle: () => {}
  })), /*#__PURE__*/React.createElement(ScreenTimeCard, {
    weekday: {
      minutes: '2h 30m',
      floor: '1h 30m',
      atRisk: 60
    },
    weekend: {
      minutes: '4h',
      floor: '3h',
      atRisk: 60
    },
    prices: [{
      name: 'Take out the trash',
      minutes: 15
    }, {
      name: 'Dishes',
      minutes: 20
    }],
    onHistory: () => {},
    onSettings: () => {}
  }), /*#__PURE__*/React.createElement(YearHeatmapCard, {
    title: "The year"
  }));
}
Object.assign(window, {
  ParentHome,
  ParentApprovals,
  HelpRespondSheet,
  ParentPlanner,
  ChoreEditorSheet,
  ParentChildDay,
  scheduleLabel,
  money
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/parent_app/ParentScreens.jsx", error: String((e && e.message) || e) }); }

// ui_kits/widgets/Widgets.jsx
try { (() => {
const {
  GlassCard,
  ProgressRing,
  Tag,
  ChoreRow,
  Icon
} = window.DailyBreadDesignSystem_2a4964;

/* Rainbow-year maths — IMPLEMENTATION.md §1.2.
   Hue comes from the REAL day-of-year, never from position within the window,
   so a 4-week widget shows a 4-week slice of the wheel and a March day is a
   March colour on every surface. */
const TODAY_DOY = 205;
const dayHue = doy => doy % 365 / 365 * 360;
const rnd = n => n * 2654435761 % 1000 / 1000;

/* ---------- Expressive config (driven by the Tweaks panel) ----------
   Three controls that change what the widget set IS, not how big its bits are:
   reading   = how much year you show vs how countable a day is
   shortfall = what a bad day looks like (the emotional message of the whole wall)
   perfect   = how hard a perfect day celebrates on the wall
*/
const WIDGET_MODES = {
  ambient: {
    counts: false,
    small: {
      weeks: 8,
      cell: 7.5
    },
    medium: {
      weeks: 26,
      cell: 9.5
    },
    large: {
      weeks: 52,
      cell: 4.8,
      bands: 1
    },
    parentLarge: {
      weeks: 26,
      cell: 7.5
    },
    standby: {
      weeks: 26,
      cell: 8.5
    }
  },
  balanced: {
    counts: true,
    small: {
      weeks: 4,
      cell: 12
    },
    medium: {
      weeks: 12,
      cell: 15
    },
    large: {
      weeks: 26,
      cell: 10.5,
      bands: 2
    },
    parentLarge: {
      weeks: 12,
      cell: 10
    },
    standby: {
      weeks: 26,
      cell: 8.5
    }
  },
  countable: {
    counts: true,
    small: {
      weeks: 3,
      cell: 13
    },
    medium: {
      weeks: 8,
      cell: 16
    },
    large: {
      weeks: 13,
      cell: 12,
      bands: 2
    },
    parentLarge: {
      weeks: 8,
      cell: 12
    },
    standby: {
      weeks: 17,
      cell: 9.5
    }
  }
};
const CURVES = {
  forgiving: {
    e: 1.0,
    sat: 34
  },
  // a shortfall still looks like a colour you'd keep
  honest: {
    e: 1.6,
    sat: 18
  },
  // §1.2 as specced
  stark: {
    e: 2.6,
    sat: 8
  } // only near-perfect days carry real colour
};
const DEFAULT_WCFG = {
  reading: 'balanced',
  shortfall: 'honest',
  perfect: 'glow'
};
const wcfg = () => Object.assign({}, DEFAULT_WCFG, window.DB_WIDGET_CFG || {});
const wmode = () => WIDGET_MODES[wcfg().reading] || WIDGET_MODES.balanced;

/** Deterministic stand-in for real data: required-ratio + optional bloom. */
function dayState(doy) {
  const v = rnd(doy + 11);
  if (doy > TODAY_DOY) return {
    ratio: -1,
    bloom: false
  };
  if (v < 0.13) return {
    ratio: 0,
    bloom: false
  };
  if (v < 0.34) return {
    ratio: 0.45,
    bloom: false
  };
  if (v < 0.62) return {
    ratio: 0.78,
    bloom: false
  };
  if (v < 0.86) return {
    ratio: 1,
    bloom: false
  };
  return {
    ratio: 1,
    bloom: true
  };
}

/** One cell. Intensity ramp is non-linear (§1.2) so a shortfall reads as one. */
function cellStyle(doy, dark, cell) {
  const cfg = wcfg();
  const cv = CURVES[cfg.shortfall] || CURVES.honest;
  const {
    ratio,
    bloom
  } = dayState(doy);
  const hue = dayHue(doy);
  if (ratio < 0) return {
    background: 'transparent',
    boxShadow: 'inset 0 0 0 1px rgb(128 128 128 / .16)'
  };
  if (ratio === 0) return {
    background: `hsl(${hue} ${Math.max(6, cv.sat * 0.6)}% ${dark ? 20 : 88}%)`
  };
  const e = Math.pow(ratio, cv.e);
  const s = {
    background: `hsl(${hue} ${cv.sat + e * (86 - cv.sat)}% ${dark ? 16 + e * 40 : 84 - e * 40}%)`
  };
  // bloom scales with the cell or it smears into its neighbours on the year wall
  if (bloom && cfg.perfect !== 'flat') {
    s.boxShadow = cfg.perfect === 'ember' ? `0 0 ${(cell * 1.15).toFixed(1)}px hsl(${hue} 96% 58% / .85), inset 0 0 0 1px hsl(45 100% 80% / .9)` : `0 0 ${Math.max(2, cell * 0.55).toFixed(1)}px hsl(${hue} 90% 55% / .7)`;
  }
  return s;
}

/** weeks of real days ending today. Columns are weeks, rows are weekdays. */
// A day cell MUST be square — the whole read (embers, a streak as a burning line)
// depends on it. So the caller passes an explicit cell edge and the grid sizes to
// content; never `flex: 1`, which stretches the two axes independently and turns
// the calendar into bars or stripes.
function RainbowGrid({
  weeks,
  dark,
  cell = 9,
  gap = 2,
  radius,
  endDoy = TODAY_DOY,
  style
}) {
  const start = endDoy - weeks * 7 + 1;
  const r0 = radius != null ? radius : Math.max(1, +(cell * 0.28).toFixed(1));
  const cells = [];
  for (let c = 0; c < weeks; c++) {
    for (let r = 0; r < 7; r++) {
      const doy = start + c * 7 + r;
      cells.push(/*#__PURE__*/React.createElement("div", {
        key: `${c}-${r}`,
        style: {
          width: cell,
          height: cell,
          borderRadius: r0,
          ...cellStyle(doy, dark, cell)
        }
      }));
    }
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: `repeat(${weeks}, ${cell}px)`,
      gridTemplateRows: `repeat(7, ${cell}px)`,
      gridAutoFlow: 'column',
      gap,
      width: 'max-content',
      ...style
    }
  }, cells);
}
const Label = ({
  children,
  dark
}) => /*#__PURE__*/React.createElement("div", {
  style: {
    font: '700 9.5px var(--ds-font)',
    letterSpacing: '.9px',
    textTransform: 'uppercase',
    color: dark ? 'rgb(255 255 255 / .55)' : 'var(--ds-label-3)'
  }
}, children);
const Money = ({
  children,
  size = 13
}) => /*#__PURE__*/React.createElement("span", {
  style: {
    font: `800 ${size}px var(--ds-font)`,
    color: 'var(--ds-gold)'
  }
}, children);

/** Widget-scale chore row (~27px). The app's ChoreRow is 46px and cannot fit three
    rows plus a header and footer in a 158px medium tile — don't reuse it here. */
const WidgetChore = ({
  icon,
  name,
  done
}) => /*#__PURE__*/React.createElement("div", {
  style: {
    display: 'flex',
    alignItems: 'center',
    gap: 8,
    height: 27,
    minWidth: 0
  }
}, /*#__PURE__*/React.createElement("span", {
  style: {
    fontFamily: 'var(--ds-font-emoji)',
    fontSize: 14,
    flex: '0 0 auto'
  }
}, icon), /*#__PURE__*/React.createElement("span", {
  style: {
    font: '600 12px var(--ds-font)',
    flex: 1,
    minWidth: 0,
    whiteSpace: 'nowrap',
    overflow: 'hidden',
    textOverflow: 'ellipsis',
    color: done ? 'var(--ds-label-3)' : 'var(--ds-label)',
    textDecoration: done ? 'line-through' : 'none'
  }
}, name), /*#__PURE__*/React.createElement("span", {
  style: {
    width: 15,
    height: 15,
    borderRadius: '50%',
    flex: '0 0 auto',
    display: 'grid',
    placeItems: 'center',
    background: done ? 'var(--ds-accent)' : 'transparent',
    boxShadow: done ? 'none' : 'inset 0 0 0 1.5px var(--ds-label-3)'
  }
}, done ? /*#__PURE__*/React.createElement("span", {
  style: {
    font: '800 9px var(--ds-font)',
    color: 'var(--ds-on-accent)'
  }
}, "\u2713") : null));

/** The widget tile itself — GlassCard gives us the real card surface + stroke. */
const Tile = ({
  w,
  h,
  children,
  pad = 15
}) => /*#__PURE__*/React.createElement(GlassCard, {
  padding: pad,
  style: {
    width: w,
    height: h,
    borderRadius: 22,
    display: 'flex',
    flexDirection: 'column',
    overflow: 'hidden',
    boxShadow: '0 6px 20px rgb(0 0 0 / .22)'
  }
}, children);

/* ---------- Home screen, kid ---------- */

/** §1.2a Small — last 4 weeks + today's ring. */
function SmallKid({
  done,
  total
}) {
  const m = wmode();
  return /*#__PURE__*/React.createElement(Tile, {
    w: 158,
    h: 158
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'flex-start'
    }
  }, /*#__PURE__*/React.createElement(Label, null, "Today"), m.counts ? /*#__PURE__*/React.createElement(Money, {
    size: 12
  }, "\xA34.50") : null), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement(ProgressRing, {
    progress: total ? done / total : 0,
    size: 48,
    stroke: 6,
    label: m.counts ? `${done}/${total}` : undefined
  }), /*#__PURE__*/React.createElement(RainbowGrid, {
    weeks: m.small.weeks,
    cell: m.small.cell,
    gap: 2
  })));
}

/** §1.2a Medium (Year) — last 12 weeks, matching the Today card exactly. */
function MediumYear({
  done,
  total
}) {
  const m = wmode();
  // Ambient drops the numbers entirely and gives the whole tile to the wall — a stats
  // column cannot survive 26 columns of grid beside it, and shouldn't try to.
  if (!m.counts) {
    return /*#__PURE__*/React.createElement(Tile, {
      w: 338,
      h: 158
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'baseline'
      }
    }, /*#__PURE__*/React.createElement(Label, null, "Last ", m.medium.weeks, " weeks"), /*#__PURE__*/React.createElement("span", {
      style: {
        font: '650 11px var(--ds-font)',
        color: 'var(--ds-label-2)'
      }
    }, "9 day streak")), /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        display: 'grid',
        placeItems: 'center'
      }
    }, /*#__PURE__*/React.createElement(RainbowGrid, {
      weeks: m.medium.weeks,
      cell: m.medium.cell,
      gap: 2
    })));
  }
  return /*#__PURE__*/React.createElement(Tile, {
    w: 338,
    h: 158
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      gap: 14,
      alignItems: 'stretch'
    }
  }, /*#__PURE__*/React.createElement(RainbowGrid, {
    weeks: m.medium.weeks,
    cell: m.medium.cell,
    gap: 2,
    style: {
      alignSelf: 'center'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '1 0 92px',
      minWidth: 92,
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(Label, null, "Last ", m.medium.weeks, " weeks"), /*#__PURE__*/React.createElement("div", {
    style: {
      font: '650 11px var(--ds-font)',
      color: 'var(--ds-label-2)',
      marginTop: 3
    }
  }, "9 day streak")), /*#__PURE__*/React.createElement(Money, {
    size: 19
  }, "\xA362.40"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(ProgressRing, {
    progress: total ? done / total : 0,
    size: 26,
    stroke: 4
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      font: '650 11.5px var(--ds-font)',
      color: 'var(--ds-label-2)'
    }
  }, done, " of ", total)))));
}

/** Second medium kind — interactive, App Intents. Not in place of the year; alongside it. */
function MediumToday({
  done,
  total,
  chores
}) {
  return /*#__PURE__*/React.createElement(Tile, {
    w: 338,
    h: 158,
    pad: 13
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline'
    }
  }, /*#__PURE__*/React.createElement(Label, null, "Today"), /*#__PURE__*/React.createElement(Money, {
    size: 12
  }, "\xA34.50")), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      overflow: 'hidden',
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'center',
      gap: 1,
      minWidth: 0
    }
  }, chores.slice(0, 3).map((c, i) => /*#__PURE__*/React.createElement(WidgetChore, {
    key: c.name,
    name: c.name,
    icon: c.icon,
    done: i < done
  }))), total > 3 ? /*#__PURE__*/React.createElement("div", {
    style: {
      font: '600 10.5px var(--ds-font)',
      color: 'var(--ds-label-3)'
    }
  }, "+", total - 3, " more \xB7 ", done, " of ", total, " done") : null);
}

/** §1.2a Large — the full year. */
function LargeKid({
  done,
  total
}) {
  return /*#__PURE__*/React.createElement(Tile, {
    w: 338,
    h: 338
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'flex-start',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(Label, null, "This year"), /*#__PURE__*/React.createElement("div", {
    style: {
      font: '800 24px var(--ds-font)',
      color: 'var(--ds-label)',
      letterSpacing: '-.02em'
    }
  }, "128 days")), /*#__PURE__*/React.createElement(ProgressRing, {
    progress: total ? done / total : 0,
    size: 54,
    stroke: 5,
    label: `${done}/${total}`
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'center',
      gap: 9
    }
  }, wmode().large.bands === 1 ? /*#__PURE__*/React.createElement(RainbowGrid, {
    weeks: wmode().large.weeks,
    cell: wmode().large.cell,
    gap: 1,
    radius: 1
  }) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(RainbowGrid, {
    weeks: wmode().large.weeks,
    cell: wmode().large.cell,
    gap: 1.5,
    endDoy: TODAY_DOY - wmode().large.weeks * 7
  }), /*#__PURE__*/React.createElement(RainbowGrid, {
    weeks: wmode().large.weeks,
    cell: wmode().large.cell,
    gap: 1.5
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      marginTop: 12
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(Label, null, "Balance"), /*#__PURE__*/React.createElement("div", {
    style: {
      font: '800 20px var(--ds-font)',
      color: 'var(--ds-gold)',
      lineHeight: 1.1
    }
  }, "\xA362.40")), /*#__PURE__*/React.createElement("div", {
    style: {
      textAlign: 'right'
    }
  }, /*#__PURE__*/React.createElement(Label, null, "Streak"), /*#__PURE__*/React.createElement("div", {
    style: {
      font: '800 20px var(--ds-font)',
      color: 'var(--ds-label)',
      lineHeight: 1.1
    }
  }, "9 days"))));
}

/* ---------- Home screen, parent ---------- */

function SmallParent() {
  return /*#__PURE__*/React.createElement(Tile, {
    w: 158,
    h: 158
  }, /*#__PURE__*/React.createElement(Label, null, "Approvals"), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      font: '800 46px var(--ds-font)',
      color: 'var(--ds-gold)',
      lineHeight: 1,
      letterSpacing: '-.03em'
    }
  }, "2"), /*#__PURE__*/React.createElement("div", {
    style: {
      font: '650 11.5px var(--ds-font)',
      color: 'var(--ds-label-2)',
      marginTop: 2
    }
  }, "waiting on you")), /*#__PURE__*/React.createElement(Tag, {
    tone: "var(--ds-help)",
    solid: true
  }, "1 HELP"));
}
function MediumParent({
  done,
  total
}) {
  const rows = [['Take the bins out', '£1.20 · due 6pm', 'var(--ds-gold)'], ['Practice piano', '20 min screen time', 'var(--ds-label-3)']];
  return /*#__PURE__*/React.createElement(Tile, {
    w: 338,
    h: 158
  }, /*#__PURE__*/React.createElement(Label, null, "At risk today"), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      flexDirection: 'column',
      gap: 9,
      justifyContent: 'center'
    }
  }, rows.map(([n, s, c]) => /*#__PURE__*/React.createElement("div", {
    key: n,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 9
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: '50%',
      background: c,
      flex: '0 0 auto'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      font: '650 12.5px var(--ds-font)',
      color: 'var(--ds-label)'
    }
  }, n), /*#__PURE__*/React.createElement("div", {
    style: {
      font: '600 10.5px var(--ds-font)',
      color: 'var(--ds-label-3)'
    }
  }, s))))), /*#__PURE__*/React.createElement("div", {
    style: {
      font: '650 11.5px var(--ds-font)',
      color: 'var(--ds-label-2)'
    }
  }, done, " of ", total, " done today"));
}
function LargeParent({
  done,
  total
}) {
  const m = wmode();
  const week = [1, 0.83, 1, total ? done / total : 0, -1, -1, -1];
  return /*#__PURE__*/React.createElement(Tile, {
    w: 338,
    h: 338
  }, /*#__PURE__*/React.createElement(Label, null, "Waiting on you"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 13,
      margin: '6px 0 14px'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: '800 52px var(--ds-font)',
      color: 'var(--ds-gold)',
      lineHeight: .9,
      letterSpacing: '-.04em'
    }
  }, "2"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6,
      alignItems: 'flex-start'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: '650 13px var(--ds-font)',
      color: 'var(--ds-label-2)'
    }
  }, "approvals to review"), /*#__PURE__*/React.createElement(Tag, {
    tone: "var(--ds-help)",
    solid: true
  }, "1 HELP"))), /*#__PURE__*/React.createElement("div", {
    style: {
      height: '.5px',
      background: 'var(--ds-hairline)',
      marginBottom: 13
    }
  }), /*#__PURE__*/React.createElement(Label, null, "This week"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(7, 1fr)',
      gap: 6,
      margin: '9px 0 14px'
    }
  }, ['M', 'T', 'W', 'T', 'F', 'S', 'S'].map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: 5
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: '700 10px var(--ds-font)',
      color: 'var(--ds-label-3)'
    }
  }, d), /*#__PURE__*/React.createElement(ProgressRing, {
    progress: Math.max(week[i], 0),
    size: 30,
    stroke: 4,
    color: i === 3 ? 'var(--ds-accent)' : 'var(--ds-secondary)'
  })))), /*#__PURE__*/React.createElement(Label, null, "Last ", m.parentLarge.weeks, " weeks"), /*#__PURE__*/React.createElement(RainbowGrid, {
    weeks: m.parentLarge.weeks,
    cell: m.parentLarge.cell,
    gap: 2,
    style: {
      margin: '9px 0 0'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 7
    }
  }, /*#__PURE__*/React.createElement(Money, {
    size: 20
  }, "\xA318.60"), /*#__PURE__*/React.createElement("span", {
    style: {
      font: '650 12px var(--ds-font)',
      color: 'var(--ds-label-2)'
    }
  }, "earned this week")));
}

/* ---------- Lock Screen (monochrome — iOS tints these) ---------- */

function AccCircular({
  done,
  total
}) {
  const p = total ? done / total : 0;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: 52,
      height: 52
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      borderRadius: '50%',
      background: `conic-gradient(#fff ${p}turn, rgb(255 255 255 / .28) ${p}turn)`,
      WebkitMask: 'radial-gradient(circle, transparent 19px, #000 19.5px)',
      mask: 'radial-gradient(circle, transparent 19px, #000 19.5px)'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      display: 'grid',
      placeItems: 'center',
      font: '800 15px var(--ds-font)',
      color: '#fff'
    }
  }, done));
}
function AccRect({
  done,
  total,
  who
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2,
      color: '#fff',
      minWidth: 150
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      font: '700 10px var(--ds-font)',
      letterSpacing: '.8px',
      opacity: .7
    }
  }, "DAILY BREAD"), /*#__PURE__*/React.createElement("div", {
    style: {
      font: '800 16px var(--ds-font)'
    }
  }, who === 'kid' ? `${done} of ${total} done` : '2 waiting on you'), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 3,
      marginTop: 3
    }
  }, Array.from({
    length: total
  }, (_, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      flex: 1,
      height: 4,
      borderRadius: 2,
      background: i < done ? '#fff' : 'rgb(255 255 255 / .3)'
    }
  }))));
}
function AccInline({
  done,
  total
}) {
  const p = total ? done / total : 0;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      color: '#fff',
      font: '600 13px var(--ds-font)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 11,
      height: 11,
      borderRadius: '50%',
      border: '2px solid #fff',
      background: `conic-gradient(#fff ${p}turn, transparent ${p}turn)`
    }
  }), /*#__PURE__*/React.createElement("span", null, done, " of ", total, " chores"));
}
function StandBy() {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: 290,
      height: 130,
      borderRadius: 18,
      background: '#0B0F13',
      padding: 14,
      display: 'flex',
      flexDirection: 'column',
      gap: 9,
      border: '1px solid rgb(255 255 255 / .08)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline'
    }
  }, /*#__PURE__*/React.createElement(Label, {
    dark: true
  }, "Last 26 weeks"), /*#__PURE__*/React.createElement(Money, {
    size: 13
  }, "9 day streak")), /*#__PURE__*/React.createElement(RainbowGrid, {
    weeks: wmode().standby.weeks,
    dark: true,
    cell: wmode().standby.cell,
    gap: 1.5,
    radius: 1.5,
    style: {
      alignSelf: 'center'
    }
  }));
}
Object.assign(window, {
  RainbowGrid,
  SmallKid,
  MediumYear,
  MediumToday,
  LargeKid,
  SmallParent,
  MediumParent,
  LargeParent,
  AccCircular,
  AccRect,
  AccInline,
  StandBy,
  wmode,
  WIDGET_MODES
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/widgets/Widgets.jsx", error: String((e && e.message) || e) }); }

// ui_kits/widgets/tweaks-panel.jsx
try { (() => {
// @ds-adherence-ignore -- omelette starter scaffold (raw elements/hex/px by design)
// Copied omelette starter. Re-running copy_starter_component with this kind overwrites this file with the latest version (page content is unaffected).

/* BEGIN USAGE */
// tweaks-panel.jsx
// Reusable Tweaks shell + form-control helpers.
// Exports (to window): useTweaks, TweaksPanel, TweakSection, TweakRow, TweakSlider,
//   TweakToggle, TweakRadio, TweakSelect, TweakText, TweakNumber, TweakColor, TweakButton.
//
// Owns the host protocol (listens for __activate_edit_mode / __deactivate_edit_mode,
// posts __edit_mode_available / __edit_mode_set_keys / __edit_mode_dismissed) so
// individual prototypes don't re-roll it. Ships a consistent set of controls so you
// don't hand-draw <input type="range">, segmented radios, steppers, etc.
//
// Usage (in an HTML file that loads React + Babel):
//
//   const TWEAK_DEFAULTS = /*EDITMODE-BEGIN*/{
//     "primaryColor": "#D97757",
//     "palette": ["#D97757", "#29261b", "#f6f4ef"],
//     "fontSize": 16,
//     "density": "regular",
//     "dark": false
//   }/*EDITMODE-END*/;
//
//   function App() {
//     const [t, setTweak] = useTweaks(TWEAK_DEFAULTS);
//     return (
//       <div style={{ fontSize: t.fontSize, color: t.primaryColor }}>
//         Hello
//         <TweaksPanel>
//           <TweakSection label="Typography" />
//           <TweakSlider label="Font size" value={t.fontSize} min={10} max={32} unit="px"
//                        onChange={(v) => setTweak('fontSize', v)} />
//           <TweakRadio  label="Density" value={t.density}
//                        options={['compact', 'regular', 'comfy']}
//                        onChange={(v) => setTweak('density', v)} />
//           <TweakSection label="Theme" />
//           <TweakColor  label="Primary" value={t.primaryColor}
//                        options={['#D97757', '#2A6FDB', '#1F8A5B', '#7A5AE0']}
//                        onChange={(v) => setTweak('primaryColor', v)} />
//           <TweakColor  label="Palette" value={t.palette}
//                        options={[['#D97757', '#29261b', '#f6f4ef'],
//                                  ['#475569', '#0f172a', '#f1f5f9']]}
//                        onChange={(v) => setTweak('palette', v)} />
//           <TweakToggle label="Dark mode" value={t.dark}
//                        onChange={(v) => setTweak('dark', v)} />
//         </TweaksPanel>
//       </div>
//     );
//   }
//
// TweakRadio is the segmented control for 2–3 short options (auto-falls-back to
// TweakSelect past ~16/~10 chars per label); reach for TweakSelect directly when
// options are many or long. For color tweaks always curate 3-4 options rather than
// a free picker; an option can also be a whole 2–5 color palette (the stored value
// is the array). The Tweak* controls are a floor, not a ceiling — build custom
// controls inside the panel if a tweak calls for UI they don't cover.
/* END USAGE */
// ─────────────────────────────────────────────────────────────────────────────

const __TWEAKS_STYLE = `
  .twk-panel{position:fixed;right:16px;bottom:16px;z-index:2147483646;width:280px;
    max-height:calc(100vh - 32px);display:flex;flex-direction:column;
    transform:scale(var(--dc-inv-zoom,1));transform-origin:bottom right;
    background:rgba(250,249,247,.78);color:#29261b;
    -webkit-backdrop-filter:blur(24px) saturate(160%);backdrop-filter:blur(24px) saturate(160%);
    border:.5px solid rgba(255,255,255,.6);border-radius:14px;
    box-shadow:0 1px 0 rgba(255,255,255,.5) inset,0 12px 40px rgba(0,0,0,.18);
    font:11.5px/1.4 ui-sans-serif,system-ui,-apple-system,sans-serif;overflow:hidden}
  .twk-hd{display:flex;align-items:center;justify-content:space-between;
    padding:10px 8px 10px 14px;cursor:move;user-select:none}
  .twk-hd b{font-size:12px;font-weight:600;letter-spacing:.01em}
  .twk-x{appearance:none;border:0;background:transparent;color:rgba(41,38,27,.55);
    width:22px;height:22px;border-radius:6px;cursor:default;font-size:13px;line-height:1}
  .twk-x:hover{background:rgba(0,0,0,.06);color:#29261b}
  .twk-body{padding:2px 14px 14px;display:flex;flex-direction:column;gap:10px;
    overflow-y:auto;overflow-x:hidden;min-height:0;
    scrollbar-width:thin;scrollbar-color:rgba(0,0,0,.15) transparent}
  .twk-body::-webkit-scrollbar{width:8px}
  .twk-body::-webkit-scrollbar-track{background:transparent;margin:2px}
  .twk-body::-webkit-scrollbar-thumb{background:rgba(0,0,0,.15);border-radius:4px;
    border:2px solid transparent;background-clip:content-box}
  .twk-body::-webkit-scrollbar-thumb:hover{background:rgba(0,0,0,.25);
    border:2px solid transparent;background-clip:content-box}
  .twk-row{display:flex;flex-direction:column;gap:5px}
  .twk-row-h{flex-direction:row;align-items:center;justify-content:space-between;gap:10px}
  .twk-lbl{display:flex;justify-content:space-between;align-items:baseline;
    color:rgba(41,38,27,.72)}
  .twk-lbl>span:first-child{font-weight:500}
  .twk-val{color:rgba(41,38,27,.5);font-variant-numeric:tabular-nums}

  .twk-sect{font-size:10px;font-weight:600;letter-spacing:.06em;text-transform:uppercase;
    color:rgba(41,38,27,.45);padding:10px 0 0}
  .twk-sect:first-child{padding-top:0}

  .twk-field{appearance:none;box-sizing:border-box;width:100%;min-width:0;height:26px;padding:0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;
    background:rgba(255,255,255,.6);color:inherit;font:inherit;outline:none}
  .twk-field:focus{border-color:rgba(0,0,0,.25);background:rgba(255,255,255,.85)}
  select.twk-field{padding-right:22px;
    background-image:url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='10' height='6' viewBox='0 0 10 6'><path fill='rgba(0,0,0,.5)' d='M0 0h10L5 6z'/></svg>");
    background-repeat:no-repeat;background-position:right 8px center}

  .twk-slider{appearance:none;-webkit-appearance:none;width:100%;height:4px;margin:6px 0;
    border-radius:999px;background:rgba(0,0,0,.12);outline:none}
  .twk-slider::-webkit-slider-thumb{-webkit-appearance:none;appearance:none;
    width:14px;height:14px;border-radius:50%;background:#fff;
    border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}
  .twk-slider::-moz-range-thumb{width:14px;height:14px;border-radius:50%;
    background:#fff;border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}

  .twk-seg{position:relative;display:flex;padding:2px;border-radius:8px;
    background:rgba(0,0,0,.06);user-select:none}
  .twk-seg-thumb{position:absolute;top:2px;bottom:2px;border-radius:6px;
    background:rgba(255,255,255,.9);box-shadow:0 1px 2px rgba(0,0,0,.12);
    transition:left .15s cubic-bezier(.3,.7,.4,1),width .15s}
  .twk-seg.dragging .twk-seg-thumb{transition:none}
  .twk-seg button{appearance:none;position:relative;z-index:1;flex:1;border:0;
    background:transparent;color:inherit;font:inherit;font-weight:500;min-height:22px;
    border-radius:6px;cursor:default;padding:4px 6px;line-height:1.2;
    overflow-wrap:anywhere}

  .twk-toggle{position:relative;width:32px;height:18px;border:0;border-radius:999px;
    background:rgba(0,0,0,.15);transition:background .15s;cursor:default;padding:0}
  .twk-toggle[data-on="1"]{background:#34c759}
  .twk-toggle i{position:absolute;top:2px;left:2px;width:14px;height:14px;border-radius:50%;
    background:#fff;box-shadow:0 1px 2px rgba(0,0,0,.25);transition:transform .15s}
  .twk-toggle[data-on="1"] i{transform:translateX(14px)}

  .twk-num{display:flex;align-items:center;box-sizing:border-box;min-width:0;height:26px;padding:0 0 0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;background:rgba(255,255,255,.6)}
  .twk-num-lbl{font-weight:500;color:rgba(41,38,27,.6);cursor:ew-resize;
    user-select:none;padding-right:8px}
  .twk-num input{flex:1;min-width:0;height:100%;border:0;background:transparent;
    font:inherit;font-variant-numeric:tabular-nums;text-align:right;padding:0 8px 0 0;
    outline:none;color:inherit;-moz-appearance:textfield}
  .twk-num input::-webkit-inner-spin-button,.twk-num input::-webkit-outer-spin-button{
    -webkit-appearance:none;margin:0}
  .twk-num-unit{padding-right:8px;color:rgba(41,38,27,.45)}

  .twk-btn{appearance:none;height:26px;padding:0 12px;border:0;border-radius:7px;
    background:rgba(0,0,0,.78);color:#fff;font:inherit;font-weight:500;cursor:default}
  .twk-btn:hover{background:rgba(0,0,0,.88)}
  .twk-btn.secondary{background:rgba(0,0,0,.06);color:inherit}
  .twk-btn.secondary:hover{background:rgba(0,0,0,.1)}

  .twk-swatch{appearance:none;-webkit-appearance:none;width:56px;height:22px;
    border:.5px solid rgba(0,0,0,.1);border-radius:6px;padding:0;cursor:default;
    background:transparent;flex-shrink:0}
  .twk-swatch::-webkit-color-swatch-wrapper{padding:0}
  .twk-swatch::-webkit-color-swatch{border:0;border-radius:5.5px}
  .twk-swatch::-moz-color-swatch{border:0;border-radius:5.5px}

  .twk-chips{display:flex;gap:6px}
  .twk-chip{position:relative;appearance:none;flex:1;min-width:0;height:46px;
    padding:0;border:0;border-radius:6px;overflow:hidden;cursor:default;
    box-shadow:0 0 0 .5px rgba(0,0,0,.12),0 1px 2px rgba(0,0,0,.06);
    transition:transform .12s cubic-bezier(.3,.7,.4,1),box-shadow .12s}
  .twk-chip:hover{transform:translateY(-1px);
    box-shadow:0 0 0 .5px rgba(0,0,0,.18),0 4px 10px rgba(0,0,0,.12)}
  .twk-chip[data-on="1"]{box-shadow:0 0 0 1.5px rgba(0,0,0,.85),
    0 2px 6px rgba(0,0,0,.15)}
  .twk-chip>span{position:absolute;top:0;bottom:0;right:0;width:34%;
    display:flex;flex-direction:column;box-shadow:-1px 0 0 rgba(0,0,0,.1)}
  .twk-chip>span>i{flex:1;box-shadow:0 -1px 0 rgba(0,0,0,.1)}
  .twk-chip>span>i:first-child{box-shadow:none}
  .twk-chip svg{position:absolute;top:6px;left:6px;width:13px;height:13px;
    filter:drop-shadow(0 1px 1px rgba(0,0,0,.3))}
`;

// ── useTweaks ───────────────────────────────────────────────────────────────
// Single source of truth for tweak values. setTweak persists via the host
// (__edit_mode_set_keys → host rewrites the EDITMODE block on disk).
function useTweaks(defaults) {
  const [values, setValues] = React.useState(defaults);
  // Accepts either setTweak('key', value) or setTweak({ key: value, ... }) so a
  // useState-style call doesn't write a "[object Object]" key into the persisted
  // JSON block.
  const setTweak = React.useCallback((keyOrEdits, val) => {
    const edits = typeof keyOrEdits === 'object' && keyOrEdits !== null ? keyOrEdits : {
      [keyOrEdits]: val
    };
    setValues(prev => ({
      ...prev,
      ...edits
    }));
    window.parent.postMessage({
      type: '__edit_mode_set_keys',
      edits
    }, '*');
    // Same-window signal so in-page listeners (deck-stage rail thumbnails)
    // can react — the parent message only reaches the host, not peers.
    window.dispatchEvent(new CustomEvent('tweakchange', {
      detail: edits
    }));
  }, []);
  return [values, setTweak];
}

// ── TweaksPanel ─────────────────────────────────────────────────────────────
// Floating shell. Registers the protocol listener BEFORE announcing
// availability — if the announce ran first, the host's activate could land
// before our handler exists and the toolbar toggle would silently no-op.
// The close button posts __edit_mode_dismissed so the host's toolbar toggle
// flips off in lockstep; the host echoes __deactivate_edit_mode back which
// is what actually hides the panel.
function TweaksPanel({
  title = 'Tweaks',
  children
}) {
  const [open, setOpen] = React.useState(false);
  const dragRef = React.useRef(null);
  const offsetRef = React.useRef({
    x: 16,
    y: 16
  });
  const PAD = 16;
  const clampToViewport = React.useCallback(() => {
    const panel = dragRef.current;
    if (!panel) return;
    const w = panel.offsetWidth,
      h = panel.offsetHeight;
    const maxRight = Math.max(PAD, window.innerWidth - w - PAD);
    const maxBottom = Math.max(PAD, window.innerHeight - h - PAD);
    offsetRef.current = {
      x: Math.min(maxRight, Math.max(PAD, offsetRef.current.x)),
      y: Math.min(maxBottom, Math.max(PAD, offsetRef.current.y))
    };
    panel.style.right = offsetRef.current.x + 'px';
    panel.style.bottom = offsetRef.current.y + 'px';
  }, []);
  React.useEffect(() => {
    if (!open) return;
    clampToViewport();
    if (typeof ResizeObserver === 'undefined') {
      window.addEventListener('resize', clampToViewport);
      return () => window.removeEventListener('resize', clampToViewport);
    }
    const ro = new ResizeObserver(clampToViewport);
    ro.observe(document.documentElement);
    return () => ro.disconnect();
  }, [open, clampToViewport]);
  React.useEffect(() => {
    const onMsg = e => {
      const t = e?.data?.type;
      if (t === '__activate_edit_mode') setOpen(true);else if (t === '__deactivate_edit_mode') setOpen(false);
    };
    window.addEventListener('message', onMsg);
    window.parent.postMessage({
      type: '__edit_mode_available'
    }, '*');
    return () => window.removeEventListener('message', onMsg);
  }, []);
  const dismiss = () => {
    setOpen(false);
    window.parent.postMessage({
      type: '__edit_mode_dismissed'
    }, '*');
  };
  const onDragStart = e => {
    const panel = dragRef.current;
    if (!panel) return;
    const r = panel.getBoundingClientRect();
    const sx = e.clientX,
      sy = e.clientY;
    const startRight = window.innerWidth - r.right;
    const startBottom = window.innerHeight - r.bottom;
    const move = ev => {
      offsetRef.current = {
        x: startRight - (ev.clientX - sx),
        y: startBottom - (ev.clientY - sy)
      };
      clampToViewport();
    };
    const up = () => {
      window.removeEventListener('mousemove', move);
      window.removeEventListener('mouseup', up);
    };
    window.addEventListener('mousemove', move);
    window.addEventListener('mouseup', up);
  };

  // data-om-starter: inert presence marker — Claude Design's starter-usage
  // probe reads it. The closed panel renders nothing, so the marker rides
  // the <html> element as an attribute instead of a rendered node — zero
  // elements added, so page CSS (even structural selectors like
  // :nth-child) can never observe it. It records that the page WIRES a
  // tweaks panel, whether or not the panel is open. Keep this effect.
  React.useEffect(() => {
    document.documentElement.setAttribute('data-om-starter', 'tweaks-panel');
    return () => document.documentElement.removeAttribute('data-om-starter');
  }, []);
  if (!open) return null;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("style", null, __TWEAKS_STYLE), /*#__PURE__*/React.createElement("div", {
    ref: dragRef,
    className: "twk-panel",
    "data-omelette-chrome": "",
    style: {
      right: offsetRef.current.x,
      bottom: offsetRef.current.y
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-hd",
    onMouseDown: onDragStart
  }, /*#__PURE__*/React.createElement("b", null, title), /*#__PURE__*/React.createElement("button", {
    className: "twk-x",
    "aria-label": "Close tweaks",
    onMouseDown: e => e.stopPropagation(),
    onClick: dismiss
  }, "\u2715")), /*#__PURE__*/React.createElement("div", {
    className: "twk-body"
  }, children)));
}

// ── Layout helpers ──────────────────────────────────────────────────────────

function TweakSection({
  label,
  children
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "twk-sect"
  }, label), children);
}
function TweakRow({
  label,
  value,
  children,
  inline = false
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: inline ? 'twk-row twk-row-h' : 'twk-row'
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-lbl"
  }, /*#__PURE__*/React.createElement("span", null, label), value != null && /*#__PURE__*/React.createElement("span", {
    className: "twk-val"
  }, value)), children);
}

// ── Controls ────────────────────────────────────────────────────────────────

function TweakSlider({
  label,
  value,
  min = 0,
  max = 100,
  step = 1,
  unit = '',
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label,
    value: `${value}${unit}`
  }, /*#__PURE__*/React.createElement("input", {
    type: "range",
    className: "twk-slider",
    min: min,
    max: max,
    step: step,
    value: value,
    onChange: e => onChange(Number(e.target.value))
  }));
}
function TweakToggle({
  label,
  value,
  onChange
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: "twk-row twk-row-h"
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-lbl"
  }, /*#__PURE__*/React.createElement("span", null, label)), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "twk-toggle",
    "data-on": value ? '1' : '0',
    role: "switch",
    "aria-checked": !!value,
    onClick: () => onChange(!value)
  }, /*#__PURE__*/React.createElement("i", null)));
}
function TweakRadio({
  label,
  value,
  options,
  onChange
}) {
  const trackRef = React.useRef(null);
  const [dragging, setDragging] = React.useState(false);
  // The active value is read by pointer-move handlers attached for the lifetime
  // of a drag — ref it so a stale closure doesn't fire onChange for every move.
  const valueRef = React.useRef(value);
  valueRef.current = value;

  // Segments wrap mid-word once per-segment width runs out. The track is
  // ~248px (280 panel − 28 body pad − 4 seg pad), each button loses 12px
  // to its own padding, and 11.5px system-ui averages ~6.3px/char — so 2
  // options fit ~16 chars each, 3 fit ~10. Past that (or >3 options), fall
  // back to a dropdown rather than wrap.
  const labelLen = o => String(typeof o === 'object' ? o.label : o).length;
  const maxLen = options.reduce((m, o) => Math.max(m, labelLen(o)), 0);
  const fitsAsSegments = maxLen <= ({
    2: 16,
    3: 10
  }[options.length] ?? 0);
  if (!fitsAsSegments) {
    // <select> emits strings — map back to the original option value so the
    // fallback stays type-preserving (numbers, booleans) like the segment path.
    const resolve = s => {
      const m = options.find(o => String(typeof o === 'object' ? o.value : o) === s);
      return m === undefined ? s : typeof m === 'object' ? m.value : m;
    };
    return /*#__PURE__*/React.createElement(TweakSelect, {
      label: label,
      value: value,
      options: options,
      onChange: s => onChange(resolve(s))
    });
  }
  const opts = options.map(o => typeof o === 'object' ? o : {
    value: o,
    label: o
  });
  const idx = Math.max(0, opts.findIndex(o => o.value === value));
  const n = opts.length;
  const segAt = clientX => {
    const r = trackRef.current.getBoundingClientRect();
    const inner = r.width - 4;
    const i = Math.floor((clientX - r.left - 2) / inner * n);
    return opts[Math.max(0, Math.min(n - 1, i))].value;
  };
  const onPointerDown = e => {
    setDragging(true);
    const v0 = segAt(e.clientX);
    if (v0 !== valueRef.current) onChange(v0);
    const move = ev => {
      if (!trackRef.current) return;
      const v = segAt(ev.clientX);
      if (v !== valueRef.current) onChange(v);
    };
    const up = () => {
      setDragging(false);
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("div", {
    ref: trackRef,
    role: "radiogroup",
    onPointerDown: onPointerDown,
    className: dragging ? 'twk-seg dragging' : 'twk-seg'
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-seg-thumb",
    style: {
      left: `calc(2px + ${idx} * (100% - 4px) / ${n})`,
      width: `calc((100% - 4px) / ${n})`
    }
  }), opts.map(o => /*#__PURE__*/React.createElement("button", {
    key: o.value,
    type: "button",
    role: "radio",
    "aria-checked": o.value === value
  }, o.label))));
}
function TweakSelect({
  label,
  value,
  options,
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("select", {
    className: "twk-field",
    value: value,
    onChange: e => onChange(e.target.value)
  }, options.map(o => {
    const v = typeof o === 'object' ? o.value : o;
    const l = typeof o === 'object' ? o.label : o;
    return /*#__PURE__*/React.createElement("option", {
      key: v,
      value: v
    }, l);
  })));
}
function TweakText({
  label,
  value,
  placeholder,
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("input", {
    className: "twk-field",
    type: "text",
    value: value,
    placeholder: placeholder,
    onChange: e => onChange(e.target.value)
  }));
}
function TweakNumber({
  label,
  value,
  min,
  max,
  step = 1,
  unit = '',
  onChange
}) {
  const clamp = n => {
    if (min != null && n < min) return min;
    if (max != null && n > max) return max;
    return n;
  };
  const startRef = React.useRef({
    x: 0,
    val: 0
  });
  const onScrubStart = e => {
    e.preventDefault();
    startRef.current = {
      x: e.clientX,
      val: value
    };
    const decimals = (String(step).split('.')[1] || '').length;
    const move = ev => {
      const dx = ev.clientX - startRef.current.x;
      const raw = startRef.current.val + dx * step;
      const snapped = Math.round(raw / step) * step;
      onChange(clamp(Number(snapped.toFixed(decimals))));
    };
    const up = () => {
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: "twk-num"
  }, /*#__PURE__*/React.createElement("span", {
    className: "twk-num-lbl",
    onPointerDown: onScrubStart
  }, label), /*#__PURE__*/React.createElement("input", {
    type: "number",
    value: value,
    min: min,
    max: max,
    step: step,
    onChange: e => onChange(clamp(Number(e.target.value)))
  }), unit && /*#__PURE__*/React.createElement("span", {
    className: "twk-num-unit"
  }, unit));
}

// Relative-luminance contrast pick — checkmarks drawn over a swatch need to
// read on both #111 and #fafafa without per-option configuration. Hex input
// only (#rgb / #rrggbb); named or rgb()/hsl() colors fall through to "light".
function __twkIsLight(hex) {
  const h = String(hex).replace('#', '');
  const x = h.length === 3 ? h.replace(/./g, c => c + c) : h.padEnd(6, '0');
  const n = parseInt(x.slice(0, 6), 16);
  if (Number.isNaN(n)) return true;
  const r = n >> 16 & 255,
    g = n >> 8 & 255,
    b = n & 255;
  return r * 299 + g * 587 + b * 114 > 148000;
}
const __TwkCheck = ({
  light
}) => /*#__PURE__*/React.createElement("svg", {
  viewBox: "0 0 14 14",
  "aria-hidden": "true"
}, /*#__PURE__*/React.createElement("path", {
  d: "M3 7.2 5.8 10 11 4.2",
  fill: "none",
  strokeWidth: "2.2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  stroke: light ? 'rgba(0,0,0,.78)' : '#fff'
}));

// TweakColor — curated color/palette picker. Each option is either a single
// hex string or an array of 1-5 hex strings; the card adapts — a lone color
// renders solid, a palette renders colors[0] as the hero (left ~2/3) with the
// rest stacked in a sharp column on the right. onChange emits the
// option in the shape it was passed (string stays string, array stays array).
// Without options it falls back to the native color input for back-compat.
function TweakColor({
  label,
  value,
  options,
  onChange
}) {
  if (!options || !options.length) {
    return /*#__PURE__*/React.createElement("div", {
      className: "twk-row twk-row-h"
    }, /*#__PURE__*/React.createElement("div", {
      className: "twk-lbl"
    }, /*#__PURE__*/React.createElement("span", null, label)), /*#__PURE__*/React.createElement("input", {
      type: "color",
      className: "twk-swatch",
      value: value,
      onChange: e => onChange(e.target.value)
    }));
  }
  // Native <input type=color> emits lowercase hex per the HTML spec, so
  // compare case-insensitively. String() guards JSON.stringify(undefined),
  // which returns the primitive undefined (no .toLowerCase).
  const key = o => String(JSON.stringify(o)).toLowerCase();
  const cur = key(value);
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-chips",
    role: "radiogroup"
  }, options.map((o, i) => {
    const colors = Array.isArray(o) ? o : [o];
    const [hero, ...rest] = colors;
    const sup = rest.slice(0, 4);
    const on = key(o) === cur;
    return /*#__PURE__*/React.createElement("button", {
      key: i,
      type: "button",
      className: "twk-chip",
      role: "radio",
      "aria-checked": on,
      "data-on": on ? '1' : '0',
      "aria-label": colors.join(', '),
      title: colors.join(' · '),
      style: {
        background: hero
      },
      onClick: () => onChange(o)
    }, sup.length > 0 && /*#__PURE__*/React.createElement("span", null, sup.map((c, j) => /*#__PURE__*/React.createElement("i", {
      key: j,
      style: {
        background: c
      }
    }))), on && /*#__PURE__*/React.createElement(__TwkCheck, {
      light: __twkIsLight(hero)
    }));
  })));
}
function TweakButton({
  label,
  onClick,
  secondary = false
}) {
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: secondary ? 'twk-btn secondary' : 'twk-btn',
    onClick: onClick
  }, label);
}
Object.assign(window, {
  useTweaks,
  TweaksPanel,
  TweakSection,
  TweakRow,
  TweakSlider,
  TweakToggle,
  TweakRadio,
  TweakSelect,
  TweakText,
  TweakNumber,
  TweakColor,
  TweakButton
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/widgets/tweaks-panel.jsx", error: String((e && e.message) || e) }); }

__ds_ns.Button = __ds_scope.Button;

__ds_ns.Chip = __ds_scope.Chip;

__ds_ns.Tag = __ds_scope.Tag;

__ds_ns.EmptyState = __ds_scope.EmptyState;

__ds_ns.GlassCard = __ds_scope.GlassCard;

__ds_ns.SF_TO_LUCIDE = __ds_scope.SF_TO_LUCIDE;

__ds_ns.Icon = __ds_scope.Icon;

__ds_ns.InlineError = __ds_scope.InlineError;

__ds_ns.ProgressBar = __ds_scope.ProgressBar;

__ds_ns.ProgressRing = __ds_scope.ProgressRing;

__ds_ns.SectionHeader = __ds_scope.SectionHeader;

__ds_ns.StatTile = __ds_scope.StatTile;

__ds_ns.KID_TABS = __ds_scope.KID_TABS;

__ds_ns.PARENT_TABS = __ds_scope.PARENT_TABS;

__ds_ns.TabBar = __ds_scope.TabBar;

__ds_ns.RARITY = __ds_scope.RARITY;

__ds_ns.AchievementCard = __ds_scope.AchievementCard;

__ds_ns.AchievementDefRow = __ds_scope.AchievementDefRow;

__ds_ns.ApprovalRow = __ds_scope.ApprovalRow;

__ds_ns.AtRiskCard = __ds_scope.AtRiskCard;

__ds_ns.BalanceHero = __ds_scope.BalanceHero;

__ds_ns.CalendarMonth = __ds_scope.CalendarMonth;

__ds_ns.ChildPicker = __ds_scope.ChildPicker;

__ds_ns.ChildRow = __ds_scope.ChildRow;

__ds_ns.ChildTile = __ds_scope.ChildTile;

__ds_ns.ChoreRow = __ds_scope.ChoreRow;

__ds_ns.DrivingTotals = __ds_scope.DrivingTotals;

__ds_ns.DrivingLogRow = __ds_scope.DrivingLogRow;

__ds_ns.ExpandRow = __ds_scope.ExpandRow;

__ds_ns.ConfirmRow = __ds_scope.ConfirmRow;

__ds_ns.FamilyMemberRow = __ds_scope.FamilyMemberRow;

__ds_ns.GoalCard = __ds_scope.GoalCard;

__ds_ns.HelpRow = __ds_scope.HelpRow;

__ds_ns.LEVEL_TIERS = __ds_scope.LEVEL_TIERS;

__ds_ns.TierFor = __ds_scope.TierFor;

__ds_ns.LevelBadge = __ds_scope.LevelBadge;

__ds_ns.XPBar = __ds_scope.XPBar;

__ds_ns.PlannerChoreRow = __ds_scope.PlannerChoreRow;

__ds_ns.PlannerGrid = __ds_scope.PlannerGrid;

__ds_ns.ScreenTimeCard = __ds_scope.ScreenTimeCard;

__ds_ns.DB_THEMES = __ds_scope.DB_THEMES;

__ds_ns.ThemeSwatch = __ds_scope.ThemeSwatch;

__ds_ns.ThemeRow = __ds_scope.ThemeRow;

__ds_ns.TransactionRow = __ds_scope.TransactionRow;

__ds_ns.WeekStrip = __ds_scope.WeekStrip;

__ds_ns.YearHeatmapCard = __ds_scope.YearHeatmapCard;

__ds_ns.ALL_DAYS = __ds_scope.ALL_DAYS;

__ds_ns.DayPicker = __ds_scope.DayPicker;

__ds_ns.SegmentedPicker = __ds_scope.SegmentedPicker;

__ds_ns.SheetActionBar = __ds_scope.SheetActionBar;

__ds_ns.SheetCard = __ds_scope.SheetCard;

__ds_ns.SheetField = __ds_scope.SheetField;

__ds_ns.SheetHeader = __ds_scope.SheetHeader;

__ds_ns.Slider = __ds_scope.Slider;

__ds_ns.Stepper = __ds_scope.Stepper;

__ds_ns.TextField = __ds_scope.TextField;

__ds_ns.ToggleRow = __ds_scope.ToggleRow;

})();
