@* ResourcePreloader component - preloads critical resources *@
@* This is a no-op component for now - critical CSS is handled inline in App.razor *@

@code {
    // No-op preloader - critical CSS is handled inline in App.razor
    // This component exists for future resource preloading if needed
}
