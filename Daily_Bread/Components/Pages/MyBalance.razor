@page "/my-balance"
@attribute [Authorize]

@inject NavigationManager NavigationManager

@* Legacy balance route retired — kid home (/) is the money view since Phase 7. *@

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/", replace: true);
    }
}
